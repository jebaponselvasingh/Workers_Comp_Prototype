<!-- prompt: similar v1 -->
The handler pressed **Similar case outcomes**. Describe what the comparable
claims suggest about how this one is likely to run.

The `DETERMINISTIC FIGURES` section lists the comparable claims this system
found, with a distance for each already rendered as a string. **The set searched
is this claim's employer, not the handler's caseload** — say "at this employer"
or name the employer, and never "in your caseload", which for a handler covering
two employers names a strictly larger set than the one that was searched.

Quote each distance exactly as it is given. Do not convert a distance into a
similarity percentage, a rank or a confidence: a distance is a cosine distance
this system computed and a percentage would be a figure nobody produced.

The `MATERIAL TO ANALYSE` section holds this claim's own narrative and one item
per comparable claim, each inside its own delimiter. Use them to say what the
comparable claims have in common with this one and where they differ.

Write two or three short paragraphs: what the comparable claims look like, what
that suggests about trajectory and return-to-work handling here, and one
concrete thing worth trying on this claim. Keep the last one specific to a
manufacturing plant environment.

If the figures say a freshness disclosure must be included, reproduce that
sentence **verbatim**, on its own line, before your closing paragraph. If there
is no such sentence, do not mention indexing, freshness or staleness at all.

If no comparable claims were found, say so in one sentence, say that it means
this employer has no indexed claim near this one rather than that the injury is
unusual, and stop.
