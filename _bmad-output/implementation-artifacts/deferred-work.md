# Deferred Work

Items raised in review that were consciously deferred. Each carries the reason it was not actionable at the time.

---

## Triage — Epic 9 assignment (2026-08-24)

**245 entries across 49 sections (Stories 1.3 -> 8.4). Every entry below has a destination.**
Assigned by Sprint Change Proposal `_bmad-output/planning-artifacts/sprint-change-proposal-2026-08-24.md`, approved 2026-08-24, under a production-readiness lens.

**Exit criterion for this file:** no entry lacks either a `resolution:` block or a placement under *Accepted limits*. Each Epic 9 story writes `resolution:` blocks into the entries it answers, following the format the Story 2.2 fraud-band entry already establishes.

| Epic 9 story | What it collects | Go-live gate |
|---|---|---|
| **9-1** prod-profile-boots-as-prod | `ENV=prod` boot refusal and the `ENV: dev` overlay; locally-built `lineworker/postgres:pg18-pgaudit` and `lineworker/backup:pg18` tags with no registry; `CREATE EXTENSION pgaudit` superuser requirement; GPU overlay never booted; NFR-5 at-rest encryption unevidenced; ollama `start_period` wall; `ci.yaml` missing `concurrency:`; branch protection unassertable; bijection lint blind to `epics.md`; `docs/Architecture-LINEWORKER.md § 8` stale CI bullet | **Yes** |
| **9-2** credential-transport-privilege-hardening | DB password in `pg_dump`/`pg_basebackup`/`pg_receivewal` argv; backup container runs as root; TLS to Postgres permitted not enforced (`host` vs `hostssl`, incl. 8.3's new records); `GRANT audit_redactor TO CURRENT_USER`; daily redactor assumption on the owner credential; PHI blocked-value processor invisible and blind to foreign stdlib records; pgaudit role-class capture nothing reads; `log_min_error_statement=panic` costs | **Yes** |
| **9-3** purge-cascade-actually-purges | Free text copied into `audit_event.after` by `notes.py`/`meetings.py`/`emails.py`; `export.*` rows unreachable by claim; two-connection/two-transaction crash window; `purge_user` over-breadth across claims not being purged; checkpoint deletion irreversible mid-cascade; in-flight copilot re-writing checkpoints; claim edit between cascade commit and `_redact`; shared `blob_key`; AD-11 guard evasions; JSONB `None` -> JSON `null`; retention floor not GUC-driven | **Yes** |
| **9-4** disaster-recovery-proven | SSH transport never exercised; Procedure B never executed; no cross-cluster restore test; WAL spool ceiling firing before the copy; `status.json` read by nothing; `wal_restarts` cumulative; 60s receiver-death detection lag; missed window skipped not caught up; invalidated-slot WAL gap as count not range; e2e cannot assert decryption; log rotation bounded in days not bytes; `log_truncate_on_rotation` on restart; post-restore purge re-run untracked | **Yes** |
| **9-5** session-lifecycle-revocation-policy | No session reaper; login does not revoke the presented cookie; idle SPA blind to server-side expiry | No |
| **9-6** validated-statutory-content | No fatality indicator (a fatal claim is Path B); NY WCB / FNSB placeholder form URLs served across 15 plants in a dozen states; 17 illustrative rate schedules with an inferred `EFFECTIVE_FROM`; `state_rate_schedule` single-row-per-state shape | **Yes** |
| **9-7** read-paths-that-write | `materialize_schedule` auditing under the reading caller (supervisor/analyst named as actor); two GET endpoints writing; six export GETs performing durable non-idempotent writes with no delivery reconciliation; no operator batch trigger or last-run visibility; daily audit sweep as two unbatched full-table scans | No |
| **9-8** paging-correctness-real-pagination | Offset cursors on worklist and drill-through (silent row skip); page-2 actions against a stale pinned `as_of`; `expanded` never reset; failed refetch replacing walked rows; fake pagination on `/api/glossary` and `/personas`; `list_meetings` recounting per page; four-group recompute per "Show more"; no cursor field pinning a written-table verdict; midnight-UTC oracle divergence | No |
| **9-9** deploy-skew-survival-contract-hygiene | Every `Record<Enum, ...>` label map as a `TypeError` that unmounts the case file; missing `TimelineTag` label map (deferred since 2.3, now four members wider); `openapi.json` gitignored; `ApiModel` `extra` policy; one rule value with two wire names; `rulesVersion` naming different documents per route; thresholds published and unread; `lineworker/web` has no formatter; `noDerivation.test.ts` guard limits | No |
| **9-10** o-scope-fold-push-down | The eight recorded O(scope) fold endpoints; case-file per-request reads added by 3.1/3.2; copilot panel running full `claim_detail` for a greeting; quick actions reading it twice; per-call `httpx.AsyncClient` in embedding and probe clients; 30s model poll for non-copilot users; Recharts 3 dependency graph; unread `uv.lock` transitive set; `IN ()` empty-book reads; pgvector HNSW post-filter k-shortfall | No |
| **9-11** mark-stale-wiring-rag-loose-ends | AD-12 mark-stale unwired across five AD-4 commands; `nomic-embed-text` 768 dims into `vector(1024)`; `search_knowledge` unreachable from the browser; chat model pulled with no caller; no non-e2e embedding-pass trigger; staleness threshold reuse; scope-loss resume wedging the thread; `create_document` not bumping `claim.version`; I/O matrix vs shipped checkpoint sweep | No |
| **9-12** correction-paths-phi-class-rows | No edit path for meetings, diary notes or secondary injuries; `delete_meeting` audit `before` unsoundness; unsurfaced orphaned decided week (deferred 3.3 -> 3.4, still open); one-way `reviewed`/`confirmed`/`osha_logged`; seam row missing its disabled control; toast primitive shipped but the two asking surfaces unmigrated (UX-DR11); unticked past meeting indistinguishable from completed; hung POST with no exit; `MeetingParticipant` -> `Stakeholder` rename | No |
| **9-13** product-placement-decisions-register | "Total incurred" basis (now on four figures); Total Paid KPI excluding $335,985; line-item vs `paid_*` reconciliation (13x gap, WC-20051); TTD vs TPD for a recovered worker; secondary-injury severity aggregation; the two body-part vocabularies; clinical edits after settlement; calendar marking an unapproved week `paid`; AD-8 tier placement for eight constant sets; AD-10 registry boundary for the reserve check; seed-migration rule; two hash-bucket derivations; absent document/photo bytes and ingest path; `photo.source` provenance sentence; scheduler mechanism; whether a narrowed analyst is demonstrated at HTTP level | Partly |
| **Accepted limits** | 66 entries recording a documented limit with no work attached. **Partitioned 2026-08-25 by Story 9.8**, the first Epic 9 story to land — moved verbatim under [`## Accepted limits`](#accepted-limits), grouped by the heading each came from, each with a one-line `accepted:` statement of what was accepted and why. Nothing was deleted or rewritten | — |

### One entry left unclassified, on purpose

The 2.5 entry beginning *"The AD-12 mark-stale obligation and the
`timeline_event` grant item both now touch `document`…"* was **not** moved and
carries no `resolution:`. It is two findings in one paragraph pointing at two
different destinations: the mark-stale half is in 9-11's payload, while the
`timeline_event` grant narrowing is collected by no story's cell and is declared
by the entry itself to be *independently actionable now* — the document write
path it was waiting on never arrived. Splitting somebody's recorded reasoning
across two rows, or filing an actionable migration under *Accepted limits*, is
not a dev-time call. Raised by Story 9.8 and left where it stands; it needs a
triage decision, not a classification guess.

### Entries already closed (do not re-open)

Five entries in this file record their own closure and need no Epic 9 assignment: the 4.1 `nextCursor`-unconsumed correction (built during the review pass), `FieldFeedback.notFound` (closed by 4.2), the meeting Delete inline two-step (closed by 4.3), the Trends empty-window dead controls (closed by 7.3), and the `fraud_band` derivation half of the 2.2 fraud-tint entry (closed by 7.1 — its remaining half, tinting the investigation card, sits in 9-13's payload-contract decision).

---

## Deferred from: code review of 1-3-role-persona-login (2026-08-10)

- **No session reaper; login never revokes existing sessions.** Every `POST /auth/login` inserts a row unconditionally and expired rows are deleted only "on contact" in `resolve_session`, so a persona can hold an arbitrary number of simultaneously-valid tokens and the `session` table is append-mostly. Deferred: how many concurrent sessions a persona may hold (multi-device vs single-session) is a policy decision beyond this story, and the cleanup job needs a scheduler that does not exist yet.
  - *Re-raised by the 2026-08-10 code review of Story 1.4*, with the sharper case: `login` does not revoke **the session presented in its own request cookie**, so a login that skips `POST /auth/logout` (a second tab, or the guard's redirect after an unrelated failure) leaves the previous token live and replayable for the full TTL even though the browser has discarded it. Still deferred for the same reason — revoking the presented cookie is a one-line change, but it *is* the single-session policy decision, made silently. The "↩ Switch" button, the only persona-switch path in the UI, does revoke.
- **`/personas` has no `filter[…]`/`sort`/cursor parameters.** The Lists convention specifies these for all list endpoints; the persona picker returns a fixed ten rows in the `{items, nextCursor, total}` envelope without them, and the role partition happens in the SPA. Deferred: the convention's pagination/filter machinery should be built once, with Epic 2's first genuinely pageable list endpoint, rather than improvised for a ten-row public picker.
  resolution: |
    **Closed by Story 9.8.** `/personas` now declares `cursor`, `limit`,
    `filter[role]` and `sort`; it pages with a keyset over `(sort value, id)` and
    sources `total` from a `COUNT(*)` over the same predicate the page applies.

    The deferral's stated condition — "built once, with Epic 2's first genuinely
    pageable list endpoint" — was never met and could not have been: five epics of
    pageable endpoints shipped, each with its own cursor codec beside its own
    service, and this picker was still returning a fixed ten rows inside an envelope
    that claimed otherwise.

    The open question the entry implied has an answer. Paging a **pre-auth**
    endpoint needed neither a session nor a scope decision: a keyset over stored
    identity columns knows nothing about who is asking, and the cursor carries only
    the request's own shape plus a position in rows this endpoint publishes in full.
    `filter[role]` is typed on a `LoginRole` enum that excludes the machine actor,
    so naming `system` is a 422 from FastAPI's validator rather than an empty page a
    caller could probe with. `DEFAULT_PERSONA_PAGE_LIMIT` is far above the seeded
    directory and `test_the_seeded_personas_fit_inside_the_default_page` pins that,
    so `usePersonas`' single unparameterised read still returns everybody.
- **An idle SPA never notices server-side session expiry.** With `staleTime: 30_000`, `refetchOnWindowFocus: false` and a guard that checks only on mount, a user left on `/dashboard` past `expires_at` keeps seeing the authenticated shell until their next navigation. Deferred: the right fix (periodic revalidation, or a refresh-on-focus policy) is coupled to the session-lifetime decision above.
- **`ApiModel` has no `extra` policy.** With `populate_by_name=True` and Pydantic's default, request models accept both `personaId` and `persona_id` and silently ignore unknown fields, so the camelCase boundary is a convention rather than an enforced contract. Deferred: strictness here is a project-wide decision affecting every future endpoint, not a defect of this story.

## Deferred from: code review of 1-6-domain-glossary (2026-08-10)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-domain-glossary.md`
  summary: The list envelope on `GET /api/glossary` asserts a pagination that does not exist — `nextCursor` is structurally always null, the repository read has no `LIMIT`, and `total` is `len(items)` computed from the very list it is returned beside, so a truncation could never be detected.
  evidence: Real, but pre-existing convention rather than a defect of this story: `/personas` shipped the identical shape in Story 1.3, and item 2 of this file already records that the convention's pagination/filter machinery should be built once, with Epic 2's first genuinely pageable list endpoint. The concrete hazard is deferred with it — if a later story adds a `LIMIT` to `list_terms` for any reason, `total` silently changes meaning from "rows that exist" to "rows on this page", and `test_every_persona_gets_every_term_in_order` would fail on `items` first and mask the semantic break. When that machinery lands, `total` should come from a `COUNT(*)` or be dropped (the convention writes it as optional).
  resolution: |
    **Closed by Story 9.8, and the hazard this entry predicted was handled
    rather than hit.** `list_terms` takes a `LIMIT` and an optional keyset position,
    the router mints a real cursor, and `total` comes from `COUNT(*)`;
    `filter[abbreviation]` and `sort` are declared.

    The entry's specific warning was that adding a `LIMIT` would silently change
    `total`'s meaning from "rows that exist" to "rows on this page", and that
    `test_every_persona_gets_every_term_in_order` would fail on `items` first and
    mask the semantic break. Both moved in the same change: the count is a
    `COUNT(*)` over the same predicate the page applies, and
    `test_the_endpoint_takes_no_parameters` was **renamed** to
    `test_the_endpoint_takes_exactly_the_lists_conventions_parameters` — an
    allowlist rather than an assertion of emptiness — so the contract change is
    visible in the diff instead of being absorbed by a passing test.

    `DEFAULT_PAGE_LIMIT` is four times the seeded vocabulary and
    `test_the_seeded_glossary_fits_inside_the_default_page` pins it, so
    `GlossaryPanel`'s single read is still complete and a migration that outgrows
    the default fails CI rather than quietly shortening the panel.

## Deferred from: code review of 2-1-prioritized-filterable-claim-queue (2026-08-11)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-prioritized-filterable-claim-queue.md`
  summary: Every queue request reads, derives and scores the caller's entire scoped portfolio before slicing — including page-2 requests, which redo all of it — because the sort key is Python arithmetic and cannot be pushed into SQL.
  evidence: Real and by design at this size (100 claims, and a `scope_all` persona is still the whole table). It is recorded because the cost is proportional to the portfolio and the queue is on the handler's critical path: the first portfolio that makes this matter needs either a materialised score column refreshed on write (which AD-10 forbids as a user-writable column but would permit as a derived cache) or a scoring pass pushed into SQL from the same JDM parameters. Neither is a decision this story should make.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-prioritized-filterable-claim-queue.md`
  summary: Both queue oracles resolve "today" independently of the server, so a test run that straddles midnight UTC disagrees with the stack by a day on every `days_open`, score, order and marker expectation.
  evidence: Real and acknowledged in `e2e/fixtures/seed.ts`'s docstring but not in `tests/seed_fixture.py`'s. The exposure is a few seconds per day, and the alternative — pinning a date the running stack does not share — trades a rare flake for a permanent fiction. Recorded because the honest fix is for the API to report the `asOf` it resolved and for both oracles to consume it, which is a small wire change better made when a second date-sensitive endpoint needs it. **Partly landed by Story 7.2**: `/dashboard/trends` publishes the `asOf` it resolved, and both of that story's oracles read the clock off the response rather than out of their own process — so the pattern now exists and is proven. What remains is retro-fitting the field onto the queue, the worklist and the drill-through, whose cursors also pin an `as_of` that no payload states.
  resolution: |
    **The wire change this entry asked for has landed on all four
    date-sensitive lists; the oracle change it asked for has not.**

    `GET /claims/queue`, `GET /dashboard/priority-claims` and
    `GET /dashboard/claims` each publish the `asOf` they resolved, using the
    `today = as_of or utc_today()` service seam and the `as_of: date` response field
    Story 7.2 proved on `/dashboard/trends` — verbatim, not a variant. The published
    value is the day the claims were **aged** against rather than the day the request
    was served, because on every page but the first those differ by whatever the
    cursor pinned; publishing the latter would have stated the one date that decided
    nothing on the page.

    What remains is the half this story did not touch: `tests/seed_fixture.py` and
    `e2e/fixtures/seed.ts` still resolve "today" in their own processes, so the
    few-seconds-a-day midnight flake window is unchanged. The field they need now
    exists on every list they check, which is the whole of what this entry was
    waiting for. Converting the two oracles is a self-contained follow-up.

## Deferred from: code review of 2-1-prioritized-filterable-claim-queue (2026-08-11)

Second review pass — three layers (adversarial, edge-case, acceptance audit) against `e889089..b4a5494`. Three items below restate deferrals already recorded from the first pass and are noted as such rather than duplicated in substance.

- source_spec: `_bmad-output/implementation-artifacts/2-1-prioritized-filterable-claim-queue.md`
  summary: `pageLimit` lives in the priority-weights JDM document, so changing the queue's default page size requires an Alembic migration seeding a new document version — and, because the cursor records the weights version, doing so invalidates every outstanding cursor.
  evidence: Real and a consequence of an argument made deliberately (a rule element lives in one tier, and how many claims a page holds is worklist tuning). The cost was not priced when the placement was chosen: page size is a transport concern that operators tune far more often than they retune weights, and coupling the two means a routine paging change re-ranks nothing but invalidates every open queue. Revisit when a second consumer needs a page size — the honest split is probably `pageLimit` in config and the marker parameters in the document.

- source_spec: `_bmad-output/implementation-artifacts/2-1-prioritized-filterable-claim-queue.md`
  summary: Every "Show more" request re-computes and re-serialises the first page of all four stage groups, and the client keeps one and discards three.
  evidence: Real. `useStageGroupPages`'s query function ends `return data!.groups[stage]`, and the endpoint's "every group is always the truth" rule — well argued for the initial load, since a response with three blank groups is indistinguishable from a caseload that has nothing in them — was never revisited for the paging path, where the same endpoint serves a fundamentally different request. At `pageLimit: 50` an incremental page can cost up to 200 fully-derived cards to deliver 50. Deferred rather than patched because the fix is a second response shape, and choosing between "a `groups` filter parameter" and "a separate per-group endpoint" is a contract decision that should be made once, with Story 5.4's top-30 list in view.
  resolution: |
    **Closed by Story 9.8, taking the option Story 5.4's verdict recommended.**
    `GET /claims/queue` accepts a `groups` parameter naming the stage groups to
    compute and return. `useStageGroupPages` sends `groups=<stage>` beside the
    `stage` it already sent; `useClaimQueue` — the initial load — sends neither, so
    "every group is always the truth" survives exactly where the entry says it was
    well argued.

    The distinction the entry worried about is preserved structurally rather than by
    convention: an unnamed group is **absent** (`null`) from `StageGroupsResponse`,
    never an empty one, so "you did not ask" and "there is nothing in this stage"
    cannot be confused. `unfilteredTotal` and `filteredTotal` are counted over the
    whole scored book rather than summed from the four group totals — that sum was
    correct while all four were always present and would have become a lie the
    moment one request held one group.

    The fold itself is untouched: the caller's whole scoped book is still read,
    derived and scored on every page. That is Story 9.10's payload, not this one's.

- source_spec: `_bmad-output/implementation-artifacts/2-1-prioritized-filterable-claim-queue.md`
  summary: `uv.lock` was excluded from the review diff as a generated artifact, so the transitive dependency set that `zen-engine==0.53.0` introduced has not been read by anyone.
  evidence: True by construction — both review passes excluded it. `zen-engine` is a Rust/pyo3 extension and the first non-pure-Python runtime dependency in the server, which makes its transitive set worth one deliberate look rather than a permanent exclusion. Belongs with a supply-chain review of the whole lock file, not with this story.

## Deferred from: implementation of 2-2-case-header-stage-adaptive-overview (2026-08-12)

Decisions taken during implementation that a later story or a product owner should revisit. Recorded here rather than only in the Dev Agent Record because each is a question left open, not a choice made and closed.

- source_spec: `_bmad-output/implementation-artifacts/2-2-case-header-stage-adaptive-overview.md`
  summary: The prototype labels `paidIndemnity + paidMedical + paidExpense` "Total incurred", but in insurance practice *incurred* is paid **plus reserve** — so the label names a different quantity from the one it computes, on the investigation card, the settled banner and the settled payout breakdown.
  evidence: Real, and ported deliberately: the prototype is this story's design contract and the figure it shows was what stakeholders reviewed. The derivation is named for what it actually adds up (`total_paid`) so the code does not repeat the error, and the label is the prototype's. Changing the *number* is a product decision — it moves every "total incurred" figure in the console and will move Epic 5's KPI cards and Epic 7's decomposition with it — so it wants an owner rather than a dev-time correction.

- source_spec: `_bmad-output/implementation-artifacts/2-2-case-header-stage-adaptive-overview.md`
  summary: The prototype tints the fraud score on the investigation card at 55 and 35; this story renders it uncoloured, because those two cut-offs appear in no rule document.
  evidence: Real. Colouring it would have put an unversioned threshold in the browser — precisely what AD-8 moved the risk bands out of `config.py` to prevent, and what `noDerivation.test.ts` fails a build over. The honest fix is a `fraud_band` derivation with its cut-offs in `derivation_thresholds`, which is a new registered derivation and outside this story's task list. Epic 7's fraud analytics workspace (7.1) is the natural place: it needs the same bands for its own charts.
  resolution: |
    **Half closed by Story 7.1, exactly as this entry predicted, and the other half is still open.**
    `fraud_band` now exists as a registered derivation
    (`services/derivations/fraud_score_band.py`) with both cut-offs in
    `derivation_thresholds` v6 as `fraudBandMedMin` / `fraudBandHighMin`, and the
    analyst workspace's distribution is drawn from it. So the blocker this entry
    named — "those two cut-offs appear in no rule document" — is gone.

    What is **not** done is the thing the entry is actually about: the
    investigation card's fraud score is still uncoloured. That card is Story 2.2's
    surface, its payload (`ClaimDetailResponse`) publishes neither the band nor
    the edges, and adding them is a contract change to a story 7.1 does not own —
    so tinting it would have meant either a second banding in the browser (the
    thing the entry refuses) or a payload change made from an unrelated story.
    The remaining work is small and now unblocked: publish `fraudBand` on the
    claim-detail payload from the registered derivation, and read it through a
    tone map. Worth doing on the next story that touches that card.

## Deferred from: code review of 2-2-case-header-stage-adaptive-overview (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-2-case-header-stage-adaptive-overview.md`
  summary: Migration 0011's `downgrade()` runs `DELETE FROM document` / `DELETE FROM timeline_event` unqualified, so it destroys rows it never inserted.
  evidence: Real, and the pattern is pre-existing — 0004 and 0007 clear their tables the same way. It is sharper here because `timeline_event` is not only a seeded table: 0010 grants the app role INSERT precisely so Story 2.3's edits, Epic 3's approvals, Epic 4's meetings and Epic 6's letters can append to it, and `upgrade()` restores only the seed. A `downgrade 0010` months from now would silently drop every runtime-appended event. Deferred rather than patched because the right fix is a decision about the whole family of seed migrations — a marker column distinguishing seeded from appended rows, or a refusal when the row count exceeds the seeded total — not a one-off `WHERE` clause in this revision.

- source_spec: `_bmad-output/implementation-artifacts/2-2-case-header-stage-adaptive-overview.md`
  summary: `Record<Enum, …>` lookups for the phase and coordination accents have no runtime fallback, so a value the deployed bundle does not know throws and blanks the case-file pane.
  evidence: TypeScript makes these exhaustive at compile time, so the only way to reach it is deploy skew — a server sending a new enum member to a browser holding cached older JavaScript. That is a real deployment mode and not one this project has decided a policy for (cache-busting, a version handshake, or defensive fallbacks at every enum site). Recorded so the decision is made once for the whole SPA rather than as a defensive `??` in this one card.

## Deferred from: implementation of 2-3-audited-inline-field-editing (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-3-audited-inline-field-editing.md`
  summary: AD-12 requires that an AD-4 command mutating an *embedded source field* call `services/rag`'s mark-stale command in the same transaction; `update_claim_fields` writes `injury_type`, `cause` and `body_part` — three fields any similar-case embedding will be built from — and calls nothing.
  evidence: `services/rag` does not exist yet (Epic 6 creates it, and Story 6.1 defines what a claim embedding is built from), so there is no command to call and no staleness to track. Recorded here rather than left to be noticed later because the obligation attaches to *this* command the moment 6.1 lands: the seam is one call inside the existing transaction, and the story that adds embeddings has to walk every AD-4 command written before it. The other direction — embedding source fields chosen so that no 2.3-editable column is one — is also a legitimate resolution, and is 6.1's to make.

- source_spec: `_bmad-output/implementation-artifacts/2-3-audited-inline-field-editing.md`
  summary: The edit command is not stage-scoped: a handler can PATCH the six clinical fields on a settled claim as easily as on an investigation one, even though the only UI that offers the editors is the investigation overview.
  evidence: Deliberate, and the alternative was worse. Story 2.4 edits the body part from the injury-diagram tab, which is available at every stage, so a stage gate here would have to be relaxed one story later. Nothing in the epics' acceptance criteria restricts the command by stage, and "a settled claim's ICD-10 was recorded wrong" is a correction a handler legitimately makes. If a lifecycle rule is wanted — say, no clinical edits after settlement without a reopen — it is a real business rule with a real owner, and it belongs in the rules tier beside the other lifecycle decisions (3.5's approvals), not as an `if` in the command.

- source_spec: `_bmad-output/implementation-artifacts/2-3-audited-inline-field-editing.md`
  summary: Editing the body part rewrites the stored `body_part` label from the diagram's vocabulary, so an edited claim reads "Right Hand" where an untouched one reads "Wrist(s) & Hand(s)" — two vocabularies in one column.
  evidence: The prototype's own behaviour (`updateBodyPart` sets `c.bodyPart = opt.label`), ported deliberately. The dataset's `body_part` describes the injury in the carrier's wording; `body_key` names a region on the SVG; nothing in the data maps between them, and inventing a mapping would be this codebase answering a clinical question the dataset did not. The honest resolutions are a product decision — either the diagram labels become the vocabulary, or `body_part` stops being writable and the two live side by side — and Story 2.4, which puts both on screen at once, is where it will be visible enough to decide.

## Deferred from: code review of 2-3-audited-inline-field-editing (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-3-audited-inline-field-editing.md`
  summary: The `edit` timeline tag renders as the raw lowercase token (`edit`) because there is no UI label map for timeline tags.
  evidence: Pre-existing from Story 2.2 — `Cards.tsx` renders `{entry.tag}` directly, and the seeded tags (`intake`, `froi`, `rtw`, …) read acceptably as uppercase chips. Story 2.3 adds the first tag the seed does not contain, which is what makes the absence of a label map visible. The stored snake_case value is convention-correct; what is missing is the UI-owned display map every other enum in the case file already has (`STAGE_LABEL`, `DISABILITY_LABEL`, `COMM_STATUS_LABEL`). Deferred because Epic 3, 4 and 6 each add their own tags, and the map wants writing once against the full set rather than growing a member per story.

## Deferred from: implementation of 2-4-interactive-injury-diagram (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-4-interactive-injury-diagram.md`
  summary: A secondary injury's severity score does not reach the risk band, the queue's priority score, the High Risk tile or any other portfolio figure — a claim whose primary injury scores 20 and whose secondary scores 95 is still banded `low` everywhere outside the diagram.
  evidence: Correct for this story and probably wrong for the product. `risk` is defined as a band of `claim.severity_score` (Story 1.4) and every consumer of it — the gauge, the queue dot, the top-bar tile, Epic 5's KPIs — reads that one column; nothing in 2.4's acceptance criteria asks for the aggregate to change, and inventing one (max? sum? weighted?) would be this codebase answering a clinical question the dataset did not. The prototype has the same behaviour and the same gap. The honest resolutions are a product decision — either secondary injuries are clinical detail that does not move the claim's risk, or `severity_score` becomes a *derived* figure over the injury set, which is a new registered derivation with a rules-tier parameter and a re-think of what the editable number on the severity card means. Epic 7's fraud/severity analytics is the first place the difference will show up in a chart.

- source_spec: `_bmad-output/implementation-artifacts/2-4-interactive-injury-diagram.md`
  summary: A secondary injury can be captured and removed but not corrected: there is no command to change its body part, injury type or severity score once it exists.
  evidence: The story's AC 2 asks for add and remove and nothing else, and the prototype has no edit path either (`removeInjury` filters the array; nothing mutates an entry). The row carries `version` precisely so an update command can compare-and-swap on it when one is written. Recorded because "delete it and add it again" loses the audit continuity that the `additional_injury` id represents — the log will read as two unrelated events rather than one correction — and because a handler correcting a typo is a likelier action than the pair of writes it currently takes.

- source_spec: `_bmad-output/implementation-artifacts/2-4-interactive-injury-diagram.md`
  summary: Story 2.3's open question about the two body-part vocabularies is now *visible* — the diagram's labels and the carrier's `body_part` wording are on screen together — and is still unresolved.
  evidence: Restated rather than duplicated: 2.3 recorded it and named this story as the place it would become visible. It has. The Injury Diagram's select reads "Right Hand" while the case header reads "Wrist(s) & Hand(s)" until somebody edits the region, at which point the server rewrites the label from the diagram's list and the two agree — so a claim's wording changes as a side effect of an unrelated correction. Still a product decision (either the diagram's labels become the vocabulary, or `body_part` stops being writable and the two live side by side), and still not one a dev-time choice should close.

- source_spec: `_bmad-output/implementation-artifacts/2-4-interactive-injury-diagram.md`
  summary: The AD-12 mark-stale obligation recorded for `update_claim_fields` now attaches to three more commands: `add_additional_injury`, `remove_additional_injury` and `update_claim_severity` all write fields a similar-case embedding would be built from.
  evidence: Extends the item recorded from Story 2.3's implementation rather than replacing it. `services/rag` still does not exist (Epic 6 creates it), so there is still no command to call — but the surface that Story 6.1 has to walk has grown from one command to four, and a secondary injury is exactly the kind of clinical detail a similar-case search would want to have indexed. The seam is one call inside each existing transaction.

## Deferred from: implementation of 2-5-documents-employee-id-statutory-forms (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-5-documents-employee-id-statutory-forms.md`
  summary: **The schema carries no fatality indicator, so Path C is recognised by a severity/outcome proxy that no real claim can currently meet.** A fatal claim in production would be classified Path B and its handler would never see the death-benefit filings.
  evidence: The most consequential gap this story leaves, and it is a data-model gap rather than a rules one. Story 1.2's schema has no `fatality` flag, no `deceased` outcome and no death-benefit status; `ReturnStatus` offers `under_treatment`, `returned_and_under_therapy` and `returned_and_fully_recovered`, none of which means the worker died. The story permitted a "JDM-parameterized severity/outcome rule", and `claim_path` uses the strongest statement the schema can make — maximum severity, permanent disability, never returned — with `pathFatalitySeverityMin` at the top of `severity_score`'s domain. That cut-off is deliberately high because the seeded book's seven `permanent` claims are *all* amputations scoring 75-98 and several of them **returned to work fully recovered**: any lower threshold would file a living worker as a death-benefit claim and show their handler the AFF-1, C-64 and C-65. The resolution is a column (a `fatality` boolean, or a fourth `ReturnStatus` member) plus a migration and a one-line change to the derivation's first branch — not a re-tuning of the threshold, which is why `test_documents_tab.py::test_no_seeded_claim_is_classified_a_fatality` says so in its failure message.

- source_spec: `_bmad-output/implementation-artifacts/2-5-documents-employee-id-statutory-forms.md`
  summary: The nine seeded `download_url` values are NY WCB and FNSB placeholders and are presented to handlers as if they were the jurisdiction's forms.
  evidence: NFR-4 and an explicit Deferred architecture decision ("State statutory coverage… and sourcing of real statutory form PDFs"), so this is a restatement rather than a discovery — recorded here because the story's instruction was to surface the caveat in code comments and *not* in the UI, which means the only thing a handler sees is a Download button that behaves exactly like a real one. Every claim in the portfolio is filed under one of fifteen plants across a dozen states; the forms are New York's. Before go-live the table needs a jurisdiction dimension (`path_required_form` currently keys on path alone) and validated per-state form references. The caveat lives in `data/models/core.py`, migration 0017 and 0018, and the extractor.

- source_spec: `_bmad-output/implementation-artifacts/2-5-documents-employee-id-statutory-forms.md`
  summary: No document has bytes: `blob_key` is null on all 563 rows, nothing ingests a file, and the viewer renders a generated sheet rather than the filing itself.
  evidence: Correct for this story — the prototype has no files behind its document rows, and the MinIO-versus-volume decision is explicitly Deferred — but worth recording as what it is: a document viewer that has never displayed a document. What this story lands is the *boundary* (`services/blobstore`, four operations, conformance-tested against a volume implementation) and its wiring through `document_content`, so attaching a real PDF is a change to the store and to an ingest path that does not exist yet, not to the route, the payload shape or the dialog. The story that adds upload is also the story that chooses the backend and configures it.

- source_spec: `_bmad-output/implementation-artifacts/2-5-documents-employee-id-statutory-forms.md`
  summary: The AD-12 mark-stale obligation and the `timeline_event` grant item both now touch `document`, whose write path this story was expected to add and did not.
  evidence: The 2.3 code-review item deferring the `timeline_event` grant narrowing says the split "is 2.2's decision to revisit alongside the `document` write path Story 2.5 will add". Story 2.5 adds no write path — it reads the table, and the story's own scope note is explicit that upload and review→confirm write-backs are out of scope (Epic 3's action worklist owns the latter). So `lineworker_app` still holds `UPDATE`/`DELETE` on `timeline_event` because it was granted together with `document`, and the pairing that justified deferring the fix has not materialised. The narrowing migration is now independent of any document story and can be written on its own.

## Deferred from: implementation of 2-6-incident-photos (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-6-incident-photos.md`
  summary: **No photo has bytes, and nothing in Epics 1–8 ingests one** — the console's evidence grid can only ever show the 293 seeded caption/source rows, and the "viewer" has never displayed a photograph.
  evidence: Correct for this story and narrower than 2.5's equivalent gap, which is why it is recorded separately. The prototype's thumbnails are the 📷 emoji, so `blob_key` is null on all 293 rows and the placeholder is the designed state rather than a degradation. But where a *document* viewer at least assembles a real sheet from stored columns, a photo viewer with no bytes has nothing to show — it renders a caption, a source line and a claim id. What this story lands is the resolution path: `has_blob` and `blob_url` are separate fields on the contract precisely so the two states a real deployment produces (a presigning store, and a mounted volume whose `url()` answers null by design) are already distinguishable, and `photos_block` takes an optional `BlobStore` so wiring one in is a change to one line of `claim_detail`. The story that adds ingest is also the story that chooses the backend — the same story 2.5 is waiting on.

- source_spec: `_bmad-output/implementation-artifacts/2-6-incident-photos.md`
  summary: `photo.source` is a provenance *sentence* ("Plant safety, 03/22") holding a month/day fragment with no year, so photographs cannot be sorted, filtered or aged by when they were taken.
  evidence: Deliberate, and the alternative was worse: the fragment carries no year, its day is not always the incident's, and 232 distinct source strings across the book mix an author with a date in one human-written field. Splitting it into a `DATE` column would have produced a column that is wrong a good fraction of the time beside a `source` that had lost half its meaning — so the extractor stores the sentence the prototype prints. The resolution belongs with the ingest path above: a real capture flow records a photographer and a timestamp as two fields, and the seeded rows become the migration's problem at that point rather than a parse nobody can validate today.

## Deferred from: implementation of 3-1-statutory-benefit-calculation (2026-08-14)

- source_spec: `_bmad-output/implementation-artifacts/3-1-statutory-benefit-calculation.md`
  summary: **`state_rate_schedule` holds one row per state, so `effective_date` is provenance rather than a selector** — the table is shaped for effective-dating and cannot yet hold two years of a jurisdiction's figures.
  evidence: The story specifies `state_code` unique, which is the right shape for what exists: one current schedule per jurisdiction, cited on the card as the date it took effect. But statutory weekly bounds are set per benefit year, and the architecture's Deferred item on "state statutory coverage and refresh cadence" is precisely about the process that adds next year's. The change is small and named in the model docstring — the constraint moves to `(state_code, effective_date)` and `rate_for_state` grows an `effective_date <= as_of` predicate with `ORDER BY effective_date DESC LIMIT 1`, which is the shape `rules/engine.py`'s `load` already uses for rule documents. Until then a second row for one state is refused by the database (asserted), which is the safe failure but not the useful one.

- source_spec: `_bmad-output/implementation-artifacts/3-1-statutory-benefit-calculation.md`
  summary: **The comp rate's 0–150% domain is a Python constant, not a rules-tier parameter**, so a carrier that wants to cap handler overrides at 100% needs a deploy rather than a rules migration.
  evidence: Deliberate, and it follows `SEVERITY_MIN`/`SEVERITY_MAX`'s precedent: 0–150% is the prototype's own input bounds ported as the *domain* of the value, and the story asks for it as command validation rather than as one of the four JDM tunables. The honest counter-argument is that a maximum an operator might want to move is tuning by definition — unlike `severity_score`'s 0–100, which is fixed by a column and a CHECK constraint. It is recorded rather than decided because moving it into `benefit_params` also has a cost: `rules/parameters.py` validates the document's *default* rate against this bound, so the bound cannot itself come from the document being validated. The duplication that exists today (the rules tier restates the numbers, since it must not import from `services/`) is pinned by `test_rule_parameters.py::test_the_rules_tiers_comp_rate_domain_agrees_with_the_services_one`.

- source_spec: `_bmad-output/implementation-artifacts/3-1-statutory-benefit-calculation.md`
  summary: **The seventeen seeded statutory rate schedules are the prototype's illustrative figures, and their effective date is a reading of the dataset rather than a sourced fact.**
  evidence: A restatement of NFR-4 and of the architecture's Deferred "state statutory coverage" item, recorded here because this story is the first surface where a handler reads a *number* out of that data rather than a form link (Story 2.5's equivalent gap). The prototype's own card carries a disclaimer ("Illustrative figures for prototype purposes — verify against the current WC board benefit schedule"); the console replaces it with the schedule's effective date, which is more honest about *what* is on file and no more validated. `EFFECTIVE_FROM = 2026-01-01` in migration 0023 is inferred from the dataset being 2026 claims with a first date of injury of 2026-01-12 — defensible, and not something a benefit board told anybody. Before go-live the table needs sourced per-jurisdiction figures with their real effective dates, at which point the multi-row question above becomes live too.

- source_spec: `_bmad-output/implementation-artifacts/3-1-statutory-benefit-calculation.md`
  summary: **A temporarily disabled worker who has returned fully recovered is still classified TTD, not TPD** — the prototype's behaviour, ported deliberately, and a question for a product owner.
  evidence: `computeBenefit` splits the temporary branch on `(c.returnStatus||"").includes("Therapy")`, so only `returned_and_under_therapy` reads as partial. The reading that justifies it is real — `disability` is what the *claim* pays on, and a temporary total disability that has since resolved was still total while it lasted — but it means the console labels a recovered worker's claim with the same indemnity type as one still off work entirely. The alternative (a third branch for `returned_and_fully_recovered`) is a clinical/actuarial question the dataset does not answer, so it is recorded rather than guessed. `tests/test_benefit_calculation.py` pins the current behaviour explicitly so a change is a decision rather than a drift.

- source_spec: `_bmad-output/implementation-artifacts/3-1-statutory-benefit-calculation.md`
  summary: The case file now performs **two extra reads per request** — the `benefit_params` rule document and the claim's `state_rate_schedule` row — on the console's most-fetched payload, at every stage including the two that do not render the card.
  evidence: Real and small: one indexed single-row lookup and one rule-document load whose evaluation is already process-cached (`rules/engine.py`'s `_evaluated`). It is recorded for the reason Story 2.1's queue-cost item is — the case file is re-read after each of the five AD-4 commands and embedded in every 409 body, so per-request costs there compound. The obvious economy is that `state_rate_schedule` is seventeen rows of immutable reference data that could be cached per process exactly as compiled rule documents are; the reason not to do it now is that the caching story changes the moment the table becomes multi-row and effective-dated (the first item above), and a cache keyed on the wrong thing is worse than a query.

- source_spec: `_bmad-output/implementation-artifacts/3-1-statutory-benefit-calculation.md`
  summary: The AD-12 mark-stale obligation recorded from Stories 2.3 and 2.4 now attaches to a **fifth** command, `update_comp_rate_override` — though a comp rate is the least likely of the five to be an embedding source.
  evidence: Extends the existing item rather than replacing it. `services/rag` still does not exist (Epic 6 creates it), so there is still no command to call. The reason to record it anyway is that Story 6.1 has to walk every AD-4 command written before it, and the surface has grown again; the reason it is qualified is that the other four write *clinical* fields (injury type, cause, body part, severity, secondary injuries) that a similar-case search would obviously index, while a comp-rate override is a financial decision about one claim and plausibly not part of what makes two claims similar. That is 6.1's call, and it should be a call rather than an omission.

## Deferred from: implementation of 3-2-reserve-adequacy-check (2026-08-14)

- source_spec: `_bmad-output/implementation-artifacts/3-2-reserve-adequacy-check.md`
  summary: **The payment schedule's own constants are Python literals, not JDM parameters** — the per-window week counts, the 4-week floor and the 20-week ceiling can only be retuned by a deploy.
  evidence: Scoped that way deliberately: the story's Task 3 gives `reserve_bands` exactly two parameters, and the schedule surface belongs to Story 3.3. But "how many weeks of indemnity does a 6-8 week recovery window schedule" is tuning by any reading, and it now feeds a *verdict* — a shorter ceiling lowers projected exposure on every long-recovery claim and moves it toward `heavy`. `SCHEDULE_WEEKS`, `MIN_WEEKS` and `MAX_WEEKS` in `services/financials/schedule.py` are named together so the promotion is one edit; 3.3 should make it a decision rather than inherit them by accident.

- source_spec: `_bmad-output/implementation-artifacts/3-2-reserve-adequacy-check.md`
  summary: **The reserve check is not a registered derivation**, so it sits outside the AD-10 registry that every other derived value on the case file goes through.
  evidence: Argued rather than overlooked, and recorded because the argument is not obviously right. `Derivation.build` takes `DerivationThresholds` and returns a pure computer over claim values; the reserve check reads a rule document of its own and — from 3.3 — a second table, which is a service *query* and not what that contract describes. Folding it in would mean either widening the registry's argument to "whatever anybody needed" (the thing `IntakeRequirements` and `InjuryCapture` were kept out for) or giving the registry database access. The cost of staying out is real: `derivations.get("reserve_check")` does not resolve, so Epic 6's copilot tool registry has to reach `services/financials` directly. `tests/test_reserve_block.py::test_no_module_outside_the_classifier_reads_a_reserve_band` is what enforces the single computer in the registry's absence.

- source_spec: `_bmad-output/implementation-artifacts/3-2-reserve-adequacy-check.md`
  summary: The case file now performs **one further read per request** — the `reserve_bands` rule document — extending the two Story 3.1 added on the console's most-fetched payload.
  evidence: Extends 3.1's item rather than replacing it, and is smaller than either of that story's two: the document's evaluation is process-cached (`rules/engine.py`'s `_evaluated`), so what remains per request is one indexed row read. The projection itself adds no query at all — it is pure arithmetic over columns already loaded, sharing the `Benefit` the case file had computed anyway. Recorded for the same compounding reason: the case file is re-read after each AD-4 command and embedded in every 409 body. The obvious economy is the same one — rule documents are immutable per `(key, version)` and their *loads* are deliberately not cached so that effective dating works without a restart, which is a decision to revisit as a whole rather than per document.

## Deferred from: implementation of 3-3-bills-indemnity-payment-schedule (2026-08-14)

- source_spec: `_bmad-output/implementation-artifacts/3-3-bills-indemnity-payment-schedule.md`
  summary: **The seeded line items and the `paid_*` columns are two unreconciled sets of numbers about one claim, and on a settled claim both are on screen *within one tab*, about 40px apart.**
  evidence: *(Sharpened by the second code review of this story, which measured it properly — the first version of this note described a smaller and less visible version of the same defect.)* The prototype invented `buildBills`/`buildExpenses` from a hash of the claim id and inherited `paidIndemnity`/`paidMedical`/`paidExpense` from its dataset, and never reconciled them. On a settled claim the effective breakdown resolves to the columns, so the Bills tab's **cost-bar legend shows the columns while the three cards beneath it show the rows**. Measured on the migrated database — WC-20051: legend "Indemnity $3,639" against the schedule sub-heading "$5,498.96 disbursed of $5,498.96 scheduled" (and "Installments Paid 20 of 20"); legend "Medical $475" against the bills card "$6,144 paid", a **13× gap**. WC-20136 inverts on indemnity ($21,455 against $5,848). The indemnity half is not a line-item problem at all — it is schedule rows against a column. Across the 62 settled claims the expense row total is a median 1.49× the column. Faithfully ported rather than introduced (the prototype's own two cards diverge the same way) and invisible on open claims, whose columns are zero. **Partially mitigated**: the summary now renders a provenance line whenever the columns answered, so the two sources are legible as two records rather than as one of them being wrong. Not reconciled, because that means either overwriting the dataset's paid columns from generated line items or generating line items that sum to them — both are decisions about what the demo data *means*. The cheapest honest fix is the second: have `extract_prototype_line_items.py` scale a settled claim's amounts so each category totals to its column.

- source_spec: `_bmad-output/implementation-artifacts/3-3-bills-indemnity-payment-schedule.md`
  summary: **A supervisor or analyst opening a case file can trigger a write and an audit row**, because the schedule refresh runs under the reading caller's context.
  evidence: Raised by the second code review of this story. `materialize_schedule` audits and commits under whoever's `CallerContext` reached it, and `GET /claims/{id}` has no role gate — every role check in this system lives in the write commands, which refuse anything but `handler`. So the two roles that can write nothing through any command can nonetheless cause INSERT/UPDATE/DELETE on `payment_schedule_week` plus an `AuditEvent` naming them as actor. **Mitigated rather than fixed**: the audit `after` now carries `"trigger": "calendar_refresh"`, so the row says what it is instead of reading as "a supervisor rewrote a payment schedule", and a test pins the key. The actor is still recorded, because `AuditEvent.actor_id` is not nullable and "under whose request did this happen" is a real question. The proper fix needs something this system does not have — a system actor, or a background refresher on the scheduler that Story 1.3's session-reaper item is also waiting on — and either is a decision wider than one story.

- source_spec: `_bmad-output/implementation-artifacts/3-3-bills-indemnity-payment-schedule.md`
  summary: **Two GET endpoints can now write**, because the schedule's calendar statuses have to be refreshed before either financial surface reads them.
  evidence: `GET /claims/{id}` and `GET /claims/{id}/financials` both call `materialize_schedule` before reading, which is what makes AC 4's "both surfaces show identical figures" structural rather than a race — three of the five week statuses move with the clock, so a refresh ahead of one surface and not the other is exactly how the two drift apart. The write is idempotent and audits nothing when nothing has moved, so the cost is one indexed SELECT on all but the ~1 request per claim per week that crosses a boundary. What is deferred is the shape question this opens: a read path that writes cannot be served from a read replica, and it means the case file — already the console's most-fetched payload, re-read after each of the five AD-4 commands and embedded in every 409 body — can now open a write transaction. The alternatives are a background refresher (needs the scheduler that Story 1.3's session-reaper item is also waiting on) or deriving the display status at read time and persisting only decisions, which is a schema change the story's own Task 2 rules out by specifying the column and the status-preserving regenerator.

- source_spec: `_bmad-output/implementation-artifacts/3-3-bills-indemnity-payment-schedule.md`
  summary: **Migration 0028 is the only migration that imports from `services/`**, deliberately, and the exception is worth revisiting if a second one ever wants it.
  evidence: Every other seed migration loads a JSON file, and 0013/0014 write their vocabularies out as literals under an explicit rule — a migration is a frozen record and must not read a live constant. 0028 does the opposite because it seeds *derived* data whose defining property is that exactly one function computes it (AD-2), and the story forbids forking the projection in as many words; a frozen copy would be the second generator rather than a preserved history. The cost is that a change to `project_payments` or `compute_benefit` silently changes what a historical migration does, which `tests/test_financial_tables_migration.py::test_the_seeded_schedule_is_the_live_generators_answer` converts into a test failure rather than a wrong figure. If a second migration ever needs this, the rule should be written down as a rule ("seeds of derived data go through the generator; seeds of vocabularies are frozen") rather than re-argued per file.

- source_spec: `_bmad-output/implementation-artifacts/3-3-bills-indemnity-payment-schedule.md`
  summary: **The schedule's own tunables are still Python literals** — Story 3.2 asked this story to promote them deliberately, and it has not.
  evidence: 3.2's deferred item names `SCHEDULE_WEEKS`, `MIN_WEEKS` and `MAX_WEEKS` in `services/financials/schedule.py` and asks 3.3 to "make it a decision rather than inherit them by accident". This is that decision, and it is to leave them: the story's task list scopes no rule document to this story, the three constants now have a *second* consumer that would have to be versioned with them (migration 0028's sanity bound), and a `schedule_params` document introduced without a story behind it would be the fourth JDM document in an epic that has three. The argument for promoting them is unchanged and still good — "how many weeks does a 6-8 week window schedule" is tuning by any reading, and it feeds a verdict — so it is re-recorded rather than closed, for whichever story first needs to retune a schedule without a deploy.

- source_spec: `_bmad-output/implementation-artifacts/3-3-bills-indemnity-payment-schedule.md`
  summary: **A `payment_scheduled` or `paid` week survives a schedule that shortens past it**, which is the safe answer and not obviously the right one.
  evidence: `plan_materialization` deletes surplus weeks only when they are undecided: a handler correcting a recovery window from "over 1 year" to "2-4 weeks" leaves any approved or paid week past the new end in place, because deleting it would erase the record of money that moved or was authorised. The result is a claim whose schedule shows week 19 of a four-week schedule — a state that wants a human rather than a tidy-up, and one nothing currently surfaces. What is deferred is the surfacing: the Bills table renders such a week with no indication that it is orphaned, and `week_count` counts it, so the heading reads "4-week schedule shown" above five rows. The fix is a flag on the read model and a chip, and it belongs with Story 3.4's approval flow, which is what makes an orphan reachable in the first place.

## Deferred from: 3-4-payment-approval-batch (2026-08-14)

- source_spec: `_bmad-output/implementation-artifacts/3-4-payment-approval-batch.md`
  summary: **The calendar still marks an *unapproved* week `paid`**, so the story's "nothing else ever sets `paid`" is exactly true for line items and needs one qualification for schedule weeks.
  evidence: `services/financials/schedule.py::_status` returns `paid` for a week the reference date has passed (and for every week of a settled claim), and `materialize_schedule` writes what the projection produced — which is the prototype's own rule and the reason the seeded portfolio has any disbursement history at all. So a stored `upcoming` week becomes `paid` when seven days elapse, with no batch involved. The half of the invariant the story is actually about *is* absolute and is enforced: no approval command anywhere writes `paid` (`tests/test_payment_ownership.py::test_no_approval_command_names_paid_as_a_destination`), nothing outside the batch assigns a `LineItemStatus.paid` at all, and an **approved** week can only be paid by the batch because `plan_materialization` never touches a row in `DECIDED_STATUSES` (held over the whole status cross-product). What is deferred is closing the residual, and it is a data decision rather than a code one: making the projection stop emitting `paid` would show every open claim as having disbursed nothing until somebody clicked through twenty weeks of approvals, which changes what the demo portfolio *means*. The honest fixes are to seed elapsed weeks as `paid` once (in a migration) and have the projection emit only `pending_approval`/`due_this_week`/`upcoming`, or to accept the calendar as a second, documented payer. Either belongs with whoever owns the dataset.

- source_spec: `_bmad-output/implementation-artifacts/3-4-payment-approval-batch.md`
  summary: **Approval feedback is the updated sheet and a `role="status"` region, not the toast UX-DR11 names.**
  evidence: The UX note asks for "the updated chip + a non-blocking toast". The chip and the sheet's state both ship; the toast does not, because there is no toast infrastructure in `web/` — no provider, no queue, no live-region host — and inventing one inside a payments story would put a cross-cutting UI primitive in the diff of a financial command. What shipped instead satisfies the *property* the note is after: feedback is non-blocking, it appears where the handler is already looking (the sheet stays open rather than closing as the prototype's does), and it is announced politely by `role="status"`. Refusals are inline at the control, which is the part UX-DR11 is explicit about. A real toast system belongs with the first story that has two surfaces needing one — Epic 4's diary and email writes are the obvious candidates.

- source_spec: `_bmad-output/implementation-artifacts/3-4-payment-approval-batch.md`
  summary: **An orphaned decided week is still unsurfaced** — Story 3.3 deferred this to 3.4 on the grounds that 3.4 makes orphans reachable, and 3.4 has not surfaced it either.
  evidence: 3.3's note asks for a flag and a chip on a `payment_scheduled`/`paid` week that survives a schedule shortening past it, "and it belongs with Story 3.4's approval flow, which is what makes an orphan reachable in the first place". That reasoning is right and the work is still not done: this story's task list scopes the approval, the batch and the figure refresh, and adding an orphan flag means a new field on the read model, a chip, a label and a rule for when a week counts as orphaned (`week_no > projection.week_count` is the obvious one and is not obviously right for a claim whose recovery window was corrected twice). It is now *more* reachable than it was, because approving a week is the cheapest way to create one. Re-recorded rather than closed.

- source_spec: `_bmad-output/implementation-artifacts/3-4-payment-approval-batch.md`
  summary: **The system actor exists now, so Story 3.3's "a supervisor appears to have rewritten a payment schedule" item has a fix available that this story did not apply.**
  evidence: 3.3 mitigated the read-path refresh by stamping `"trigger": "calendar_refresh"` into the audit diff and recorded that "the proper fix needs something this system does not have — a system actor, or a background refresher". This story adds the system actor (`UserRole.system`, migration 0029) and the scheduled-jobs hook the background refresher would run on. Both halves of the proper fix are therefore now available, and neither was used: `materialize_schedule` still audits under the reading caller. Not done here because it is a change to Story 3.3's surface with its own test surface — the refresh would either move to a scheduled job (changing when statuses update, which AC 4's "both surfaces agree" depends on) or run under the system actor inside a read request (an audit row naming an actor who did not make the request, which is a different kind of dishonest). Recorded with the enabling pieces named, for Epic 8's audit review.

- source_spec: `_bmad-output/implementation-artifacts/3-4-payment-approval-batch.md`
  summary: **The scheduler has no operator-facing "run the batch now", and no visibility beyond a log line.**
  evidence: `POST /admin/payment-batch` exists only under `ENV=e2e` and is not served in dev or prod, deliberately: an operator trigger needs a role gate, an audit story of its own and a rate limit, none of which belong in the story that builds the batch. What that leaves is a job whose only evidence of having run is a `scheduler.job_failed`/`payment_batch.completed` structlog line — no last-run timestamp on a health endpoint, no run history, and `JobRunner.last_run` is per process and lost on restart. Acceptable at a single-site demo scale and named here because it is the thing an operator will ask for first: "did Tuesday's batch run?" is currently answered by grepping logs.

## Deferred from: implementation of 3-5-auto-generated-action-checklist-approval (2026-08-17)

- source_spec: `_bmad-output/implementation-artifacts/3-5-auto-generated-action-checklist-approval.md`
  summary: **Completions are one-way — `reviewed`, `confirmed` and `osha_logged` are set and never cleared** — where the prototype's `confirmDocument` toggles.
  evidence: The prototype flips `confirmed` on every click (line 881), and that is the one part of `docReviewState` not ported. An audited completion a second click silently withdraws is a worse record than no completion at all: the audit log would carry two events with no way to tell an undo from a correction, and the checklist row would reappear with nothing saying why. "I confirmed the wrong document" is a real need and it wants an author and a reason — an un-confirm command with its own audit action and its own timeline sentence — rather than the same button pressed twice. Not built here because this story's task list scopes three commands and a fourth would need its own refusal ladder, its own control on a card that is already dense, and a decision about whether an un-confirm re-opens the trigger. Recorded so the asymmetry with the prototype is on the record rather than discovered.

- source_spec: `_bmad-output/implementation-artifacts/3-5-auto-generated-action-checklist-approval.md`
  summary: **`GET /claims/{id}/actions` reads the stored schedule statuses without refreshing them**, so the payment-confirmation trigger can be one week-boundary behind the Bills tab.
  evidence: Three of the five `payment_schedule_week` statuses move with the calendar and `services/financials/materialize.py` is what advances them — but it writes, and Story 3.3 already recorded "a GET that writes" as debt rather than as a pattern to spread (the case file and the financials read both do it, and both audit under a *reading* caller). Rather than add a third such path, the generator reads what is stored and the row's deep link lands on the Bills tab, which does refresh. The worst case is a handler sent to look at a schedule the tab then advances under them, which is a cosmetic ordering difference rather than a wrong figure; and `payment_due` — the registered derivation the rule also consumes — is unaffected. The real fix is Story 3.4's own deferred item: move the refresh to a scheduled job under the system actor, at which point every reader sees the same statuses without any of them writing.

- source_spec: `_bmad-output/implementation-artifacts/3-5-auto-generated-action-checklist-approval.md`
  summary: **`TimelineTag` gained `document` and `compliance` and the SPA still renders tags as raw uppercase tokens** — Story 2.3's missing label map, now two members wider.
  evidence: Pre-existing and re-recorded, with the count that matters: `Cards.tsx` renders `{entry.tag}` directly, and the deferred item from Story 2.3 argued the map "wants writing once against the full set rather than growing a member per story". This story adds the fourth and fifth runtime-only tags, so a handler now sees `DOCUMENT` and `COMPLIANCE` chips beside `APPROVAL` and `BENEFIT`. All five read acceptably as uppercase words, which is why this is still deferred rather than fixed — but the set is now large enough that the "write it once" argument has stopped getting cheaper, and Epic 4's meetings and diary notes are the next two.

- source_spec: `_bmad-output/implementation-artifacts/3-5-auto-generated-action-checklist-approval.md`
  summary: **Two of the eleven triggers ride demo hash-bucket derivations, so the checklist names actions on claims that do not need them.**
  evidence: Consumed as-is, on the story's own instruction, and named here because a reader will otherwise assume the list means something on every row. `rtw_blocked` and `payment_due` are hashes of the claim id (`services/derivations/queue_flags.py` says so at length), so "Confirm the indemnity payment for this week" appears on roughly two thirds of treatment claims because of their digits. Both rules are *unions* — the RTW rule also reads the claim's own recommended and actual return dates, and the payment rule also reads the stored week statuses — so the half with real content fires correctly regardless. Replacing the demo definitions is a change to one module and to no caller, which is the whole point of them being registered; it is the architecture's own Deferred item and this story does not move it.

## Deferred from: code review of 3-5-auto-generated-action-checklist-approval (2026-08-17)

- source_spec: `_bmad-output/implementation-artifacts/spec-3-5-auto-generated-action-checklist-approval.md`
  summary: **The story's "disabled toggle with a tooltip" for seam completions was not built — a seam row carries no completion control at all, only a disabled deep link.**
  evidence: Task 4's second bullet asks that "toggles whose backing entity ships later (diary check-in completion → Epic 4 note) render disabled with the same tooltip pattern as their deep links". `_action` (`services/worklist/actions.py`) sets `command=None` for every target in `SEAM_REASONS`, so the diary row renders its disabled "Log Diary Entry" link and nothing else. The deviation is argued in code — a completion button for work nobody can do would write a false record, and there is no entity behind it to write to — and it is the better call, but the implementation pass recorded five other deviations and missed this one, so Epic 4 had no note that a control it was told to expect does not exist. Recorded now: Story 4.2 enables the diary target, and whoever does it should decide then whether the row gains a completion control or stays a link.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-5-auto-generated-action-checklist-approval.md`
  summary: **An enum member the SPA build predates is a `TypeError` that unmounts the case file, not a degraded row — and this is true of every label map in the console, not just the checklist's.**
  evidence: `ActionsCard` reads `COMMAND_LABEL[command].idle` directly, so a server that starts sending a sixth `ActionCommand` crashes the pane for any browser holding an older bundle across a deploy. That is the house convention working as designed — `labels.ts` states that each map is a `Record<Enum, string>` with no fallback precisely so a new member is a compile error rather than `undefined` on screen — and eighteen existing maps share it. It is therefore not this story's defect and must not be fixed only here: a fallback in one component would make the console inconsistent about a rule it currently keeps everywhere. The real question is whether the SPA should tolerate a newer API at all, which belongs with whoever owns deploy sequencing (Epic 8).

## Deferred from: implementation of 4-1-meeting-scheduling-management (2026-08-17)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-meeting-scheduling-management.md`
  summary: **The "non-blocking success toast" is a polite live region plus inline refusals** — the toast primitive is deferred again, now to Story 4.3.
  evidence: Task 5 asks for a toast on save. There is still no toast provider, queue or live-region host anywhere in `web/`, and Story 3.4's deferred item argued the primitive belongs with "the first story that has two surfaces needing one — Epic 4's diary and email writes are the obvious candidates". Story 4.1 owns exactly one such surface (the meeting save); 4.3's email send is the second. So this follows the shipped precedent rather than inventing a cross-cutting UI primitive inside a story about a table: `MeetingsSubTab` mounts one `<p role="status" data-testid="meetings-status" className="sr-only">` carrying at most one sentence, and each card renders its own `role="alert"` refusal inline at the control that caused it. NFR-3 asks for feedback that is non-blocking and never a native dialog, which this satisfies — and AC 2's `alert()` removal is asserted twice, by a vitest stub and by a Playwright `page.on("dialog")` listener that fails the smoke test if one ever appears. The primitive itself is now owed by 4.3, which will have both surfaces in one epic and no excuse.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-meeting-scheduling-management.md`
  summary: **The meetings list is a single page and `nextCursor` is published but unconsumed** — there is no "Show more" in the Meetings sub-tab.
  evidence: The server implements keyset pagination in full (`services/claims/meetings.py`: an opaque `(date, id)` cursor, every bound validated, `{items, nextCursor, total}`, and `tests/test_meetings.py::test_paging_visits_every_meeting_exactly_once` walks a book at `limit=1`). The SPA's `useMeetings` is a plain `useQuery` that reads the first page and ignores `nextCursor`, because the default page holds fifty and a handler's seeded diary holds two — a "Show more" control would be dead UI in every session this console currently has. The contract is already right, so the upgrade is `useInfiniteQuery` in one hook and a button, with no server change; it becomes real when Story 4.2's today's-meetings summary or a demo with a busy diary arrives. Named because "the cursor is there and nothing uses it" reads as an oversight rather than a decision.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-meeting-scheduling-management.md`
  summary: **A meeting cannot be edited — only created, completed and deleted** — so correcting a date means deleting a row and losing its audit thread.
  evidence: AC 4 names ✓ Done and Delete and nothing else, and the prototype has no edit either. The gap is worth recording because the table is *built* for one: `version` is on the row, `complete_meeting_cas` is a compare-and-swap over it, and `MeetingConflictProblemDocument` already carries the fresh entity — an `update_meeting` command would be a fourth ladder over the same machinery. What stops it being free is the audit shape: a partial update wants a field-level before/after diff (`update_claim_fields`' `_changes`), not the whole-row diff create and delete use, and deciding which fields are editable is a product question (re-dating a meeting is obviously fine; re-typing `three_point_contact_initial` to `litigation_prep` is arguably a different meeting). Left for whoever finds handlers deleting and re-creating.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-meeting-scheduling-management.md`
  summary: **`TimelineTag` still has no meetings member, and the timeline label map is still missing** — Story 3.5's item, unchanged rather than widened.
  evidence: Story 3.5's deferred note says "Epic 4's meetings and diary notes are the next two" tags to join the runtime-only set. They are not: AD-12 scopes `timeline_event` to claim-mutating commands and the prototype does not surface meetings on the claim timeline, so `services/claims/meetings.py` deliberately does not import `timeline.append` and a test asserts no event is written. The tag set is therefore unchanged and the raw-uppercase-chip problem is no worse than 3.5 left it. Recorded because the earlier note *predicted* this story would widen it, and a reader comparing the two would otherwise think the work was missed.

## Deferred from: code review of 4-1-meeting-scheduling-management (2026-08-17)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-meeting-scheduling-management.md`
  summary: **`feedbackFromError` has no 404 or 403 branch**, so a refusal the handler cannot retry out of is announced as "Could not save. Try again in a moment."
  evidence: `web/src/features/claim-detail/useInlineEdits.ts` branches on `isConflict` (409) and `isInvalidPatch` (422) and falls through to `FAILED_MESSAGE` for everything else. Story 4.1 gave the helper its first caller that can genuinely answer 404 — `POST /claims-diary/meetings` with a claim outside the caller's book (`/problems/meeting-claim-not-found`) — and the sentence the handler reads invites a retry that can never succeed. Not fixed here because the helper is shared by every inline edit and the injury popover: adding branches changes copy on surfaces this story does not own, and the right fix is probably a `not-found` member on `FieldFeedback` rather than another ternary. Cheap and self-contained whenever someone owns that file next.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-meeting-scheduling-management.md`
  summary: **`delete_meeting`'s audit `before` diff takes `claim_business_id` from the row read before the DELETE**, which is the exact unsoundness the surrounding docstring argues against.
  evidence: `delete_meeting_cas` returns its columns through `RETURNING` on the reasoning that "a value read *before* the statement is one another writer could have changed in between — so the log would describe a row that never existed in that state". `services/claims/meetings.py`'s delete ladder then composes the diff from those returned columns plus `claim_business_id` off the pre-read `existing`. Benign today: no command mutates `meeting.claim_id`, so the two can never disagree. Recorded because it is the exception the argument does not admit, and it becomes real the day an `update_meeting` command (already deferred separately) can re-link a meeting to another claim.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-meeting-scheduling-management.md`
  summary: **Correction: the earlier item in this file saying `nextCursor` is published but unconsumed no longer describes the code** — the review pass built the "Show more".
  evidence: That entry was written during implementation and was accurate then. Code review triaged the same observation as a defect rather than a deferral, because the sort is `(meeting_date, id)` *ascending* against a fifty-row page: a handler past fifty meetings loses the **newest** ones off the end, silently, with no count and no control. `useMeetings` is now a `useInfiniteQuery`, `MeetingsSubTab` renders `total` and a "Show more" while `hasNextPage`. Left in place rather than edited because entries here are append-only; read the two together.

## Deferred from: implementation of 4-2-claim-linked-diary-notes (2026-08-17)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-claim-linked-diary-notes.md`
  summary: **`DIARY_CHECK_IN_DAYS` is a Python constant, and a check-in *cadence* is arguably operator tuning (AD-8).**
  evidence: `worklist_actions` carries the cap, the padding floor and eleven urgencies — numbers an operator retunes to change how loud a list is. The trigger *logic* has always been Python, because it is a condition over typed state rather than a knob, and seven days is what "weekly" means. The counter-argument is real and is the same one Story 3.2's `SCHEDULE_WEEKS` item makes: "how often should a handler check in on a treatment claim" is a policy a carrier might well want to move without a deploy, and unlike `severityMin`/`severityMax` it is fixed by no column and no constraint. Promotion is one key in the parameter block plus a line in `WorklistActions`, and it is recorded rather than done because this story's task list scopes no rule-document change. The boundary is also worth stating: the window is **exclusive**, so a note written on Monday holds the row closed for six days and it fires again the following Monday. `tests/test_action_checklist.py` parametrises 0/1/6/7/30 days so the edge is a decision rather than a drift.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-claim-linked-diary-notes.md`
  summary: **A note cannot be edited or deleted, and `diary_note` therefore has no `version`.**
  evidence: Create-only in v1 by the design contract — there is no edit or delete control anywhere in the prototype's diary, and append-only rows are exempt from compare-and-swap by the write-concurrency convention. The gap is worth recording because it is sharper for notes than for meetings: a meeting is a plan somebody can delete and re-make, while a note is a *record*, and "I typed the wrong claim into the tag" or "that sentence names the wrong physician" are both realistic and currently unfixable. Adding either is not free — an editable append-only store needs a decision about whether the original text survives (an amendment, as the audit log would want) or is overwritten (a correction, which loses the record), and that is a compliance question rather than a CRUD one. Left for whoever finds handlers writing "ignore the note above".

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-claim-linked-diary-notes.md`
  summary: **The toast primitive is deferred once more — 4.3 now owes it, with no story left to defer to.**
  evidence: Story 3.4 put the primitive with "the first story that has two surfaces needing one"; 4.1 owned one (the meeting save) and 4.2 owns one (the note save), so this follows the shipped precedent again: `NotesSubTab` mounts one `<p role="status" data-testid="notes-status" className="sr-only">` carrying at most one sentence, with inline `role="alert"` refusals at the control and a `clearFeedback()` that resets both mutations and the local flag on every path. NFR-3 asks for feedback that is non-blocking and never a native dialog, which this satisfies — and the absence of `alert()` is asserted twice, by a vitest stub and by a Playwright `page.on("dialog")` listener that fails the smoke test if one appears. Story 4.3 has the email send, which is the second surface, and is the last story in the epic that could build it.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-claim-linked-diary-notes.md`
  summary: **`TimelineTag` still has no diary member, and the timeline label map is still missing** — Story 3.5's item, unchanged rather than widened, for the second story running.
  evidence: 3.5's note predicted "Epic 4's meetings and diary notes are the next two" tags to join the runtime-only set. Neither is: AD-12 scopes `timeline_event` to claim-mutating commands, a note changes no column of `claim`, and the prototype does not surface notes on the case timeline — so `services/claims/notes.py` deliberately does not import `timeline.append` and `test_writing_a_note_emits_no_timeline_event` asserts it. The 4.1 entry says the same about meetings. The tag set is therefore unchanged and the raw-uppercase-chip problem is no worse than 3.5 left it; recorded again only because the earlier note predicted otherwise and a reader comparing the two would think the work was missed.

## Deferred from: code review of 4-2-claim-linked-diary-notes (2026-08-17)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-claim-linked-diary-notes.md`
  summary: **`FieldFeedback` grew a `notFound` member, which *closes* the 4.1 code-review item that asked for one** — so that entry above is answered, not still open.
  evidence: The 4.1 review recorded that `feedbackFromError` "has no 404 or 403 branch", so a refusal the handler cannot retry out of was announced as "Could not save. Try again in a moment.", and suggested "the right fix is probably a `not-found` member on `FieldFeedback` rather than another ternary". Story 4.2 made that fix, because it added a 404 where the retry sentence is not merely unhelpful but harmful: `/problems/note-not-readable` is answered *after* the row is committed and audited, and a handler who re-sends duplicates a row in a table with no edit and no delete. `isNotFound` now maps to `{kind: "notFound", message: <the server's detail>}`; `InlineEditField` renders it like `failed` (no `aria-invalid`, no `attempted`), so the only surfaces whose copy changes are the two that can actually answer 404 — the scheduler's claim link and the note's claim tag. **403 is still unbranched**, and deliberately: every command's role gate refuses non-handlers, and no non-handler reaches a surface that renders inline feedback, so a `notPermitted` member would be copy nobody can see. Whoever builds a read-only supervisor case file owns it.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-claim-linked-diary-notes.md`
  summary: **Two reads are now issued on every request that almost nobody consumes** — `upcomingCount` recounts the whole book on every "Show more" page, and the action checklist reads the latest note for claims whose stage can never use it.
  evidence: `list_meetings` issues three statements per call (`select_meetings_page`, `count_meetings`, `count_meetings_matching`), and the third is a whole-book `COUNT(*)` over the caller's meetings. The SPA reads `upcomingCount` from `pages[0]` only, so every subsequent infinite-query page pays for a count nothing renders. Separately, `claim_actions` calls `select_latest_note_at` for every checklist request, though `_diary_check_in` discards the value for any claim not in `treatment` — roughly four claims in five on the seeded book. Neither is wrong and both are cheap at demo scale (a handler's book is tens of rows, and the queries are indexed), which is why this is recorded rather than fixed: the honest versions are "compute the count only when no cursor was supplied" and "let the caller pass the stage, or make the read lazy", and both trade a little clarity in a command ladder that is currently very easy to read. Worth taking together, by whoever next has a reason to touch `list_meetings` — Story 4.3's email log will have the same envelope question.
  resolution: |
    **One of the two halves is closed by Story 9.8, and the other is not.**

    The `list_meetings` half: `total` is now computed only when no cursor was
    supplied (`total = None if decoded is not None else await count_meetings(...)`),
    following `list_email_logs`, and `MeetingPage.total` / `MeetingListResponse.total`
    widened to `int | None`. That is the "compute the count only when no cursor was
    supplied" the entry names, and the Lists convention already wrote `total` as
    optional.

    **`upcomingCount` is deliberately *not* made conditional**, which is a decision
    this entry left open rather than one it prescribed. It is the greeting's "📅 N
    upcoming meetings", rendered directly above a list the reader is paging, so a
    value that vanished on page two would empty a sentence mid-scroll — where
    `total` is read from `pages[0]` and kept. Two counts on one envelope with two
    rules, and the difference is now argued in `MeetingPage`'s docstring rather than
    implicit.

    The second half — `claim_actions` reading `select_latest_note_at` for claims
    whose stage discards it — is untouched. It is one of the O(scope)/per-request
    read costs Story **9.10** collects, and it belongs there rather than in a paging
    story.

## Deferred from: follow-up code review of 4-1 and 4-2 (2026-08-17)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-meeting-scheduling-management.md`
  summary: **Delete is one unconfirmed click on an irreversible PHI row, and it is the documented way to correct a mistyped date.**
  evidence: `MeetingCard`'s Delete fires `useDeleteMeeting` immediately — no confirmation, no undo — and because `deferred-work` already records that there is no `update_meeting`, delete-and-recreate is the sanctioned correction path. So the destructive control is *routine*, not rare, and it sits a few pixels from the disabled ✉ seam in a 300px pane. Not fixed here because the spec forbids native `confirm()` and the house has no non-blocking confirmation primitive — the same gap that deferred the toast. Whoever builds that primitive (4.3 owes one) should give this control an inline "Delete? / Cancel" two-step rather than a dialog.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-meeting-scheduling-management.md`
  summary: **A past meeting nobody ticked renders identically to a completed one, and still offers "✓ Done".**
  evidence: `meeting_status` returns `done` for any past date regardless of `is_done`, so `labels.ts` gives it the ✓ glyph and the `opacity-60` tone while `MeetingCard` still shows the ✓ Done button (gated on `is_done`). From day two of every demo database the two seeded rows read as completed work. The handler cannot tell which greyed cards still need ticking. The fix is a third vocabulary member — `overdue`, or splitting the tone from the status — which is a change to a registered derivation and to the UI vocabulary it feeds, so it wants a product decision rather than a review patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-meeting-scheduling-management.md`
  summary: **A hung POST leaves the scheduler with no exit at all.**
  evidence: `MeetingSchedulerDialog` returns early from `onOpenChange` while busy and disables Cancel — both deliberate, from the first review pass, so a mid-flight dialog cannot be dismissed into an inconsistent state. But `api/client.ts` sets no `AbortSignal` and no timeout, so if the request never settles, ✕, Escape, backdrop and Cancel are all inert on a focus-trapped dialog indefinitely; the only exit is a reload, which loses the draft. Not fixed here because the honest fix is a timeout on the shared fetch client, which changes failure behaviour for every request in the app and belongs with whoever owns that file.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-claim-linked-diary-notes.md`
  summary: **The full note text is copied verbatim into `audit_event.after`, so purging a note will not purge its PHI.**
  evidence: `services/claims/notes.py::_diff` writes `note_text` into the JSONB diff — following the precedent `meetings.py` set for agenda text, and the model docstring has been corrected to say so rather than claiming otherwise. The consequence nobody had written down is retention: `deferred-work` hands Story 8.1 the handles it needs on `diary_note` (the FK, the business id in the diff, the DELETE grant), but the most sensitive free text in the console also sits in `audit_event`, and Story 8.2 is titled audit log *hardening* rather than purging. Deleting the note would leave the text behind. Story 8.1 should decide whether the audit diff for PHI-class free text stores the value or only the field name — the `audit_redactor` role the architecture spine already defines is the mechanism.

## Deferred from: implementation of 4-3-templated-stakeholder-emails (2026-08-18)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-templated-stakeholder-emails.md`
  summary: **The `email_log` audit diff deliberately omits the body, which diverges from `notes.py` and `meetings.py` — Story 8.1 should harmonise all three rather than discover the inconsistency.**
  evidence: The 4.1/4.2 follow-up review recorded that copying free text into `audit_event.after` means purging the row leaves the PHI behind in an append-only table whose disposition is redact-in-place, and handed the decision to Story 8.1. This story did not pre-empt that decision, but it also did not enlarge the problem: `services/claims/emails.py::_diff` writes `claim_id` (the `WC-nnnn` business id), `subject`, `recipients`, `priority`, `template_key` and `sent_at`, and **not** `body`. The reason is specific rather than squeamish — `email_log` is append-only with no delete path, so the after-diff is never needed to reconstruct a row somebody removed, and the body is the largest PHI blob the console stores. What the audit event records here is that a communication went out and to whom. A test asserts the body string appears nowhere in the serialized diff. **This does not make the audit event PHI-free, and the review was right to say so:** five of the six seeded subjects interpolate `{{worker_name}}`, so the injured worker's name and the claim's business id still land verbatim in `audit_event.after`. What the omission buys is proportion — the letter itself, the largest PHI blob the console stores, stays in the one table the purge cascade can delete — not exemption. The cost is that the three diary tables now disagree about how much of their free text the audit carries, which is exactly the kind of drift 8.1 will have to reconcile; the `audit_redactor` role the spine defines is the mechanism, and this entry is the third table it must cover.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-templated-stakeholder-emails.md`
  summary: **`MeetingParticipant` is now the shared stakeholder vocabulary for emails too, and its name has outlived its story.** The rename to `Stakeholder` is deferred.
  evidence: The story asked for "one shared 6-value set with 4.1's participant enum" so convert-to-email maps 1:1, and `data/models/enums.py` already had exactly those six members. `services/claims/emails.py` therefore imports and reuses it rather than declaring a second enum — two vocabularies is how convert-to-email starts needing a translation table, and the prototype's fragile label-substring match (`toList.some(t => t.toLowerCase().includes(k))`, which is why `Employer HR` matched `employer`) disappears entirely when the meeting's participants *are* the recipient members. But an email addressed to a `MeetingParticipant` reads wrong, and the honest fix is a rename: enum → `services/claims/meetings.py` → the router → the generated `schema.d.ts` type name → every test that spells it. That is mechanical and typed, so mypy would catch every miss — it was left out only because this story already ships two tables, five endpoints, a modal, a sub-tab and a new UI primitive, and a rename touching the generated client is not a change to bury in that diff. One vocabulary with an imperfect name beats two with good ones. Worth taking by whoever next opens both files.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-templated-stakeholder-emails.md`
  summary: **The list-envelope `total` question is answered for `email_log` only — `list_meetings` still recounts the whole book on every page.**
  evidence: The 4.2 code-review entry above records that `list_meetings` issues a whole-book `COUNT(*)` on every "Show more" page although the SPA reads `total` from `pages[0]` alone, and predicted "Story 4.3's email log will have the same envelope question". It did, and the answer was cheap here because nothing was shipped yet to change: `list_email_logs` computes the count only when no cursor was supplied and returns `null` otherwise, which the Lists convention already permits (`{items, nextCursor, total?}`). `list_meetings` was deliberately **not** touched — that is a change to shipped, tested behaviour with its own `upcomingCount` complication, and doing it as a side effect of an email story would put a meetings regression risk in a diff nobody would think to review for it. The two list commands now differ in this one respect, which is the argument for taking the meetings half soon rather than later.
  resolution: |
    **Closed by Story 9.8 — the "meetings half" this entry existed to
    schedule.** `list_meetings` now counts the book on the first page only, exactly
    as `list_email_logs` does, and the two list commands no longer differ in this
    respect.

    The `upcomingCount` complication the entry named as the reason for waiting is
    resolved by *not* moving it: it stays counted on every page, because it is
    rendered beside a list the reader is paging and a count that vanished mid-scroll
    would empty a sentence. That asymmetry is now argued in `MeetingPage`'s
    docstring, so the next reader finds the reasoning rather than an inconsistency.

    Taken here rather than by "whoever next has a reason to touch `list_meetings`"
    because this story is a paging story and the change is a paging change — the
    regression risk the entry worried about sits in a diff whose reviewer is looking
    for exactly this.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-templated-stakeholder-emails.md`
  summary: **The toast primitive exists at last, and the two surfaces that asked for it were not migrated to it.**
  evidence: Stories 3.4, 4.1 and 4.2 each recorded that the primitive belongs with "the first story owning two surfaces needing one", and 4.3 was the last story in the epic that could build it. It is built — `src/components/ui/toast.tsx`, one `<ToastHost>` in `App.tsx`, `role="status" aria-live="polite"`, dismissal scheduled by `push()` from the event handler that called it so the feature keeps the no-`useEffect` discipline `react-hooks/set-state-in-effect` enforces, and no new dependency. But `MeetingsSubTab` and `NotesSubTab` keep their `sr-only` `role="status"` regions exactly as they shipped: rewiring them would churn a dozen passing tests for no behaviour change, and the toast host announces on the same polite channel. So the console now has two feedback mechanisms doing the same job in the same pane. Migrating the two older surfaces is small and should follow; note that refusals are deliberately **not** part of it — an inline `role="alert"` at the control the handler must fix is the correct shape, and a refusal that vanishes after four seconds would be a regression.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-templated-stakeholder-emails.md`
  summary: **The meeting Delete control gained its inline two-step, which *closes* the 4.1 follow-up review item** — that entry is answered, not still open.
  evidence: The follow-up review recorded that "`MeetingCard`'s Delete fires `useDeleteMeeting` immediately — no confirmation, no undo — and because there is no `update_meeting`, delete-and-recreate is the sanctioned correction path", and assigned the fix to "whoever builds that primitive (4.3 owes one)… an inline 'Delete? / Cancel' two-step rather than a dialog". Story 4.3 built the primitive and took the control with it: `confirming` is card-local state, the first click swaps the button for Delete? / Cancel, and every other control on the card lowers it in its own handler. Deliberately **not** wired into `MeetingsSubTab`'s `clearFeedback()` — a confirmation that outlived its card, or followed the handler to the next row, would be worse than none. No `confirm()`, no dialog, no second primitive. The destructive control on an irreversible PHI row now takes two deliberate clicks.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-portfolio-kpi-cards.md`
  summary: **The Total Paid KPI card sums the static `paid_*` columns and therefore excludes $335,985 of `status = paid` bills and expenses that the Bills tab shows** — the carried-over "what does the paid figure actually sum" decision, now landed on a screen with a number attached.
  evidence: Real, quantified, and expected. `TotalPaidDerivation` adds `paid_indemnity + paid_medical + paid_expense`; its own docstring says that is *not* the right answer for an open claim, "where the columns are routinely zero while the schedule and the bills show real disbursements", and names `claim_financials.paid_to_date` as the computer for that. 38 of the 100 seeded claims (all 28 in treatment, 6 intake, 4 investigation) have zero paid columns while carrying 33,598,500 cents of paid bills and expenses — about 20% of the card's headline 167,049,700. So a supervisor reads "Total Paid $1,670,497", drills into any treatment claim, and the Bills tab shows a five-figure Paid to Date for a claim the card counted as zero. This is **not** an AD-10 violation (they are two derived values with one computer each) and **not** a deviation from the design contract: the prototype has no bills table and computes exactly the ported figure, and the Epic 5 context carries this forward as one of two product decisions that "land directly on these screens… what the 'total paid / total claim amount' figure actually sums (it moves the financial KPI cards if resolved)", explicitly wanting "an owner, not a dev-time correction". Recorded here with the magnitude so the owner is deciding against a number rather than a principle. Changing it is one call site (`services/worklist/summary.py`, `total_paid.of(claim)` → the `paid_to_date` path) plus both test oracles, and it moves Epic 7's financial decomposition with it.

## Deferred from: code review of 5-2-handler-performance-benchmarking (2026-08-18)

- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-handler-performance-benchmarking.md`
  summary: **`noDerivation.test.ts` cannot see a derivation written one prop-rename away, and its operator set excludes `===`** — a guard-design limit this story surfaced rather than caused.
  evidence: `HandlerBenchmarkTable.tsx` passes `pct={row.cycleSpeedPct}` into `CycleSpeedBar({ pct })` and the two callout names into `LeaderCallout({ leader, laggard })`. Inside those functions a `pct / 2` or a re-derived width matches no `DERIVED_FIELDS` name and fires no rule, so the guard stops at the component boundary — every feature in `ROOTS` has this shape, which is why it is pre-existing rather than new. Separately the `FORBIDDEN` pattern's operator set is `[-+*/%]` and `[<>]=?`, which does not include `===`, so `if (leader === laggard)` is unguarded and adding `leader|laggard` to the field list bought nothing. Closing it means either scanning through one level of prop aliasing or adding equality to the operator set; both are changes to shared plumbing that should not ride in a story diff.

## Deferred from: code review of 5-3-portfolio-analytics-charts (2026-08-18)

- source_spec: `spec-5-3-portfolio-analytics-charts.md`
  summary: Recharts 3 drags a full Redux runtime and ten d3 packages into the production graph, and resolves `react-is` to 17.0.2 inside a React 19.2 app.
  evidence: Installing `recharts@^3.10.1` for six charts added `@reduxjs/toolkit`, `redux`, `redux-thunk`, `react-redux`, `reselect`, `immer@11`, `es-toolkit`, `eventemitter3`, `decimal.js-light`, `tiny-invariant`, `victory-vendor` and ten `d3-*` packages, and flipped `@standard-schema/spec` and `react-is` from dev-only to runtime. The main chunk goes 584.74 kB (pre-existing >500 kB advisory, confirmed by building the baseline commit in a throwaway worktree) to 932.29 kB, gzip 173.64 to 274.91 kB. `react-is@17`'s element-type predicates (`isFragment`, `isValidElementType`) against React 19 elements are a known source of silent misbehaviour; recharts' peer range tolerates it, so nothing fails today. The honest closes are a code-split of the dashboard route and an explicit `react-is` resolution — both shared build plumbing that should not ride in a story diff.

- source_spec: `spec-5-3-portfolio-analytics-charts.md`
  summary: `portfolio_charts` folds six distributions and the SLA strip in Python over every claim row in scope on every dashboard mount, with `no-store` and no SQL-side `GROUP BY`.
  evidence: `services/worklist/charts.py::portfolio_charts` issues one 12-column read over the caller's whole scope and folds it in process; the endpoint is uncached by contract and is hit on every dashboard load. At the seeded 100 claims this is free and the "one scoped read, one pure fold" purity argument in the module docstring is worth more than the cycles. The shape is O(scope) per request, so the ceiling is real but unreached, and the docstring never mentions the cost. Recording it now beats discovering it at 100k claims; Epic 7 builds its cohort and segmentation aggregates on these same reads and is the natural place to weigh a `GROUP BY` push-down.

## Deferred from: implementation of 5-4-priority-claims-worklist-top-30 (2026-08-18)

Two of these are **verdicts on entries already in this file**, which Story 5.4 was asked to give: the epic context named the stage-group paging shape (2.1) and "may an action row approve a payment" (3.5) as parked pending this story's worklist. Both entries stay open with a recommendation attached rather than being closed here, because neither fix is in this story's scope.

- source_spec: `spec-5-4-priority-claims-worklist-top-30.md`
  summary: **Verdict on 2.1's "every Show more re-computes all four stage groups"** — this endpoint is the shape that deferral was waiting to see, and it recommends the `groups` filter parameter over a second response shape.
  evidence: `GET /dashboard/priority-claims` answers one list as `{items, nextCursor, total}` and a page of it costs one scoped read plus four bulk child reads, with no group the client discards. That is what a paged list should look like, and it cost nothing extra to build — so the queue's problem is not that its response shape is wrong in principle but that one endpoint serves two different requests through it. Of the two options that entry left open, the recommendation is the **`groups` filter parameter** (or a per-group endpoint) rather than bolting a second response shape onto `/claims/queue`: the initial load's "every group is always the truth" argument is sound and should survive, and a parameter narrows the *paging* request without touching it. Not implemented here — it is a contract change to another story's endpoint.
  resolution: |
    **Recommendation taken, by Story 9.8.** `GET /claims/queue` grew a
    `groups` parameter; no second response shape was bolted on and no per-group
    endpoint was created. The initial load's "every group is always the truth"
    argument survives untouched, because omitting the parameter returns all four
    groups exactly as before.

    The one refinement this entry did not specify: an unnamed group is `null` rather
    than an empty `StageGroupResponse`, so the narrowing is legible in the payload
    and "you did not ask" stays distinguishable from "there is nothing in this
    stage" (NFR-3). The two totals are counted over the book rather than summed from
    the payload, so a narrowed response still states how big the whole queue is.

- source_spec: `spec-5-4-priority-claims-worklist-top-30.md`
  summary: **`priority_claims` ranks the caller's whole scoped book in Python on every page request**, so a three-page walk scores the portfolio three times.
  evidence: The ordering is a total order over the population and is Python arithmetic over a JDM parameter block, so there is no page of it to read — `claim_queue` has the same shape and its docstring gives the reason (keyset pagination would need the sort key in SQL, which is what AD-2 forbids). At the seeded 100 claims a full walk costs three reads of 100 rows and three folds through five registered derivations, which is free; the shape is O(scope) per page and the endpoint is `no-store` by contract. Recorded beside 5.3's identical note rather than folded into it because this one multiplies by the page count. Epic 7 builds cohorts on the same reads and is the natural place to weigh a materialized ranking.

## Deferred from: code review of 5-4-priority-claims-worklist-top-30 (2026-08-19)

Two adversarial layers (blind hunter, edge-case hunter) against `7cca6ca..HEAD`. Ten findings were patched in the pass; the eight below were judged real but not this story's to fix.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-4-priority-claims-worklist-top-30.md`
  summary: The worklist cursor is an offset, so a claim leaving the population between two page requests slides the window and silently skips the row that moved across the boundary.
  evidence: Real and inherent to offset pagination, which is what `queue.py` shipped in Story 2.1 and what this story was specified to copy ("in `queue.py`'s shape"). The cursor pins all three rule-document versions and the `as_of` it aged against, so every input *it* controls is stable; what it cannot pin is the book. The honest fix is keyset pagination — resume after the last row's `(score, claim_id)` rather than at an offset — which is a cursor-contract change that should be made once, for both lists, rather than leaving the queue and the worklist paginating differently.
  resolution: |
    **Closed by Story 9.8, and closed the way this entry asked for it: once,
    for all three lists.** The worklist, the drill-through and the queue now resume
    after `priority.order_key` — `(-score, claim_id)` — rather than at an offset. A
    claim leaving the population between two requests no longer slides the window,
    so the survivors either side of it are walked exactly once.

    The entry's own framing named the obstacle ("the sort key is Python arithmetic
    and cannot be pushed into SQL"), and it turns out to be an obstacle only to a
    *SQL* keyset. The fold already produces a fully scored, fully sorted Python
    list; resuming is a `bisect_right` into it, over the same `order_key` that
    sorted it. The score is still computed exactly once, in Python, with no SQL
    `ORDER BY` over it and no materialized rank column — so AD-2 is satisfied
    literally rather than by argument.

    Cursors minted under the old contract carry `o` and no `k`, so they are refused
    with 400 `/problems/invalid-cursor` rather than reinterpreted: reading a stale
    offset as a key, or answering page one, would each resume a walk at the wrong
    row without saying so.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-4-priority-claims-worklist-top-30.md`
  summary: Actions on page two are generated against the cursor's pinned `as_of`, so a walk resumed later shows deadline-driven actions computed against a date up to `MAX_CURSOR_AGE` (7 days) old.
  evidence: Real, and the consequence of a deliberate choice: the cursor pins `as_of` because `days_open` feeds the score and a page walk that re-aged the book mid-walk would re-rank under the reader. Generating actions at today instead would split the date — a row whose `daysOpen` was computed on Monday carrying an action computed on Friday — which is a different inconsistency, not fewer. Practical exposure is seconds, since a walk is three clicks. The real fix is the one already recorded against Story 2.1: have the API report the `asOf` it resolved, and let a client decide whether a stale cursor is worth following.
  resolution: |
    **The fix this entry names as "the real one" has landed; the pin it
    describes is unchanged, and deliberately.**

    `GET /dashboard/priority-claims` publishes `asOf` — the day the rows were aged
    against, which on a cursor page is the day the cursor pinned. A client (or a
    reader) can now see that a walk resumed from a six-day-old cursor is showing
    actions computed against a six-day-old date, which is precisely the "let a client
    decide whether a stale cursor is worth following" the entry asks for.

    What has not changed is the pin itself, for the entry's own reason: `days_open`
    feeds the score, and re-aging the book mid-walk would re-rank it under the
    reader. Generating actions at today instead would split the date — a row whose
    `daysOpen` was computed on Monday carrying an action computed on Friday — which
    is a different inconsistency rather than fewer. `MAX_CURSOR_AGE` still bounds
    how stale a followed cursor may be.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-4-priority-claims-worklist-top-30.md`
  summary: `PriorityClaimsTable` never resets `expanded` when the base query refetches onto a different first cursor, so accumulated pages vanish and a page-two fetch fires with no user click.
  evidence: Real, and a faithful copy of `StageGroup.tsx`, which has the identical shape — `expanded` state, a cursor-keyed infinite query, and no reset when the anchor changes. Patching this table alone would leave the two paging surfaces behaving differently for no reason a reader could recover. Rare in practice because the first cursor embeds three rule versions, an offset and an `as_of`, so it only moves at UTC midnight or on a rules migration. Belongs with a single fix to the shared paging idiom.
  resolution: |
    **Closed for two of the three surfaces by Story 9.8; the third is named
    below rather than silently left.**

    `PriorityClaimsTable` and `DrillClaimsPage` now reset their expansion when the
    first cursor changes. The mechanism is a state adjustment during render — the
    React-documented way to reset state on a prop change — rather than an effect, so
    the re-render happens before anything commits and no request goes out from the
    discarded pass. `isExpanded` is the value read *this* pass, so the infinite
    query sees `enabled: false` immediately rather than one render later; that is
    what closes the "a page-two fetch fires with no user click" half.

    **`StageGroup.tsx` is not fixed, and that is a scoped decision rather than an
    oversight.** Its `expanded` is not local state: it lives in `useStageExpansion`
    above the queue pane and is read by the detail pane too, because a claim
    revealed by "Show more" is on screen and both panes have to agree about that.
    Resetting it from inside `StageGroup` means calling a parent's setter during
    render, which React warns about, so the honest fix is a change to the shared
    expansion hook rather than the six lines the two dashboard tables needed. The
    two surfaces that accumulate *dashboard* pages are fixed; the third keeps the
    shape, and the pattern to copy now exists.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-4-priority-claims-worklist-top-30.md`
  summary: A failed *refetch* of the base query replaces the whole rendered table — including rows already walked to via "Show more" — with the error alert, because the `isError` branch is tested before the data branch.
  evidence: Real, and the same branch order as all three sibling dashboard sections and `HandlerBenchmarkTable`; TanStack Query retains `data` across a failed refetch, so `isError && data !== undefined` is a reachable state on every one of them. It is sharper here only because this table accumulates pages, so more is lost. The fix — an inline "could not refresh" warning above retained rows, with the full-height alert reserved for the no-data case — is a dashboard-wide pattern change and should be made across the four sections at once.
  resolution: |
    **Closed for the two surfaces where the loss was a walk, by Story 9.8.**

    `PriorityClaimsTable` and `DrillClaimsPage` now test `isError` **after** the
    data branch: a failed refetch with rows on screen renders an inline
    "could not be refreshed" warning below the retained rows
    (`priority-claims-stale`, `drill-stale`), and the full-height alert is reserved
    for the case where there is nothing to show at all. That is exactly the shape
    the entry prescribes.

    The "dashboard-wide pattern change across the four sections at once" is **not**
    done. `HandlerBenchmarkTable` and the two chart sections keep the old branch
    order, and the argument for leaving them is the one this entry itself makes for
    why the defect is sharper here: those sections do not accumulate pages, so a
    failed refetch costs one payload rather than a walk. The pattern now exists in
    two places with its reasoning written down; applying it to the remaining
    sections is small and belongs with whoever next touches them.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-4-priority-claims-worklist-top-30.md`
  summary: `highRiskSeverityMin`, `medRiskSeverityMin` and `fraudFlagScoreMin` ship on every page and no component reads any of them.
  evidence: True — grepping `web/src` finds them only in comments. The justification given (a client holding a threshold would be a second copy of a rule it cannot see change) is sound but describes 5.1, where two card captions interpolate the numbers; nothing on this table does. The concrete cost is that `noDerivation.test.ts` now has to defend the row's `fraudScore` against a `fraudFlagScoreMin` sitting on the same object — a hazard the payload created for itself. Either a threshold footnote renders them, as `HandlerBenchmarkTable` does, or they come off the wire; Story 5.5's drill-through is the surface most likely to want the bands, so the decision is better made with it in view.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-4-priority-claims-worklist-top-30.md`
  summary: One rule value now has two wire names on one page — `PortfolioSummaryResponse.fraudScoreMin` and `PriorityClaimsResponse.fraudFlagScoreMin` are both `DerivationThresholds.fraud_flag_score_min`.
  evidence: Real. `noDerivation.test.ts` carries both spellings as separate guard alternatives, which is the concrete maintenance cost: a third payload has to guess which name it inherits. Not fixed here because the cheaper name is 5.1's and renaming a shipped field is a contract change to a story this one does not own. Worth settling before Epic 7 adds its own fraud surfaces.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-4-priority-claims-worklist-top-30.md`
  summary: The empty-book path still issues four `IN ()` child reads that cannot return a row.
  evidence: True, and locked in place by `test_the_aggregate_takes_exactly_five_scoped_reads`, which counts reads rather than describing when they are worth making. The cost is four no-op round trips for a persona whose scope qualifies nothing, which is the cheapest request this endpoint serves either way. Recorded rather than fixed because an early return would have to be taken alongside an amendment to that test, and the test's flat count is doing more good than the shortcut would.

## Deferred from: implementation of 5-5-dashboard-drill-through (2026-08-19)

- source_spec: `spec-5-5-dashboard-drill-through.md`
  summary: **`drill_through_claims` ranks the caller's whole scoped book on every page request**, so a two-page walk of the full portfolio scores it twice — 5.3's and 5.4's identical note, now on a third endpoint.
  evidence: The ordering is a total order over the filtered population and is Python arithmetic over a JDM parameter block, so there is no page of it to read; `claim_queue` and `priority_claims` have the same shape and the same recorded reason (keyset pagination would need the sort key in SQL, which AD-2 forbids). At the seeded 100 claims a full walk is two reads of 100 rows and two folds through five registered derivations, which is free. Recorded beside the other two rather than folded into them because this endpoint multiplies by *both* the page count and the number of drill-throughs a supervisor opens in a session, which is the highest of the three. Epic 7 builds cohorts and segmentation on these same reads and is the natural place to weigh a materialized ranking.

- source_spec: `spec-5-5-dashboard-drill-through.md`
  summary: **The drill-through's cursor is an offset**, so a claim leaving the filtered population between two page requests slides the window and silently skips the row that moved across the boundary — 5.4's recorded entry, inherited verbatim.
  evidence: Inherent to offset pagination, which is what `queue.py` shipped in Story 2.1 and what this cursor was specified to copy. This cursor pins both rule-document versions, the `as_of` it aged against *and* the filter set, so every input it controls is stable; what it cannot pin is the book. The honest fix is keyset pagination, made once for all three lists rather than leaving the queue, the worklist and the drill-through paginating differently. This entry exists so the third list is counted when that change is costed.
  resolution: |
    **Closed by Story 9.8, and this entry is why the change was costed for
    three lists rather than two.** The drill-through cursor now carries
    `priority.order_key`'s two halves for the last row served and resumes strictly
    after it, so a claim leaving the filtered population no longer slides the window.

    One thing this list has that the other two do not, and it is answered here as
    well: `filter[reserveVerdict]` is derived from a table a *read* rewrites, so its
    population can be re-partitioned with no document version having moved and no
    user having edited anything. The cursor now carries `verdict_digest`, a hash of
    the claim ids the facet left standing, minted only when that facet is set — so
    the walk detects the rewrite and refuses the page rather than skipping through
    it. See the Story 7.4 entry this pairs with.

- source_spec: `spec-5-5-dashboard-drill-through.md`
  summary: **`DrillClaimsPage` inherits `StageGroup`/`PriorityClaimsTable`'s two recorded paging rough edges**: `expanded` is never reset when the base query refetches onto a new first cursor, and a failed *refetch* replaces already-rendered rows with the error alert.
  evidence: Both are faithful copies of the shared idiom and both are already on this list against Story 5.4, which recorded that patching one surface alone would leave the paging surfaces behaving differently for no reason a reader could recover. There are now **three** of them (`StageGroup.tsx`, `PriorityClaimsTable.tsx`, `DrillClaimsPage.tsx`), which raises the value of a single fix to the idiom and is the reason for this entry. One thing is genuinely different here and worth naming: this list's first cursor moves whenever the *filter set* changes, which is a user gesture rather than a UTC midnight — but the filter set is in the query key, so a filter change is a different cache entry and the stale-anchor path is not reached by it. The remaining trigger is the same rare one.
  resolution: |
    **Closed for this surface by Story 9.8.** `DrillClaimsPage` resets its
    expansion when the first cursor changes, and its failed-refetch branch is
    non-destructive — the same two fixes made to `PriorityClaimsTable` in the same
    change, so the two dashboard paging surfaces have not been left behaving
    differently.

    The entry's count of three is now two-and-one: `StageGroup.tsx` keeps both
    shapes, because its `expanded` lives in `useStageExpansion` above the queue pane
    and is read by the detail pane as well — resetting it from inside the group
    means writing to a parent's state during render. The Story 5.4 entry above
    records that decision in full. The idiom the entry wanted fixed once is now
    written down twice with its reasoning; the third surface needs a change to the
    shared hook rather than a copy of these six lines.

    The observation this entry adds — that a filter change is a *different cache
    entry* and therefore does not reach the stale-anchor path — is still true, and
    `moveTo`'s existing reset is kept beside the new one rather than replaced by it:
    they close different doors.

- source_spec: `spec-5-5-dashboard-drill-through.md`
  summary: **The drill-through publishes no thresholds**, which settles half of 5.4's "three published thresholds are rendered by nothing" entry and leaves the other half open.
  evidence: 5.4's entry said the decision was "better made with Story 5.5's drill-through in view", on the grounds that it was the surface most likely to want the bands. It does not: every row arrives banded, the fraud flag arrives decided, and a caption quoting "Severity ≥ N" belongs to the card that was clicked rather than to the list it opened — so `DrillClaimsResponse` carries `rulesVersion` and `thresholdsVersion` and no cut-off at all. That is one data point, not a resolution: `PriorityClaimsResponse`'s three thresholds are still rendered by nothing, and the choice between "a footnote renders them" and "they come off the wire" is still open. The same is true of the two wire names for `fraud_flag_score_min` — this story added no third spelling, and the entry stands.

## Deferred from: code review of 5-5-dashboard-drill-through (2026-08-19)

*Every entry recorded under this heading is an accepted limit and has been moved to [Accepted limits](#accepted-limits) — nothing was deleted.*

Two findings from the adversarial pass that are real but are not this story's to fix. Everything else the reviewers raised was either patched into the diff or rejected; the four patched findings that mattered are narrated in the spec's Review Triage Log.

## Deferred from: code review of 6-1-local-model-serving-embedding-foundation (2026-08-19)

Seven findings that are real but belong to a later story or a decision this one had no standing to make. Everything else the two reviewers raised was patched into the diff or rejected; the patched findings are itemised in the spec's Review Triage Log.

Also closed by this story, and recorded here so the three open entries above are not read as outstanding: the AD-12 mark-stale obligation recorded from Stories 2.3, 2.4 and 3.1 is now discharged. `update_claim_fields`, `update_claim_severity`, `add_additional_injury` and `remove_additional_injury` each call `services/rag.mark_claim_stale` inside their own transaction. The fifth, `update_comp_rate_override`, was **decided rather than missed**: a comp-rate override is a financial decision about one claim and is not an input to the embedded summary, which carries no money at all. The decision is argued in that module's docstring and asserted by `test_the_comp_rate_command_is_deliberately_not_wired`, so the absence cannot later be mistaken for an oversight.

- source_spec: `spec-6-1-local-model-serving-embedding-foundation.md`
  summary: **Scoped ANN search can return fewer than `k` neighbours than the caller's book actually holds**, because pgvector post-filters HNSW candidates rather than filtering inside the index scan.
  evidence: Real and structural, not a bug in the query. `select_similar_claims` applies `employer_scope(ctx)` as a `WHERE` on top of an HNSW-ordered `LIMIT k`, and pgvector's index scan returns its candidate set before the employer predicate runs — so a narrowly scoped handler can exhaust the candidates before filling `k`. It is invisible today for the reason it is invisible in the tests: at 100 seeded rows the planner picks a sequential scan, which filters exactly. Both the repository test's `len(returned) == len(hers)` and the e2e spec's "the bound is the partition, never k" therefore pass while asserting something that stops being true at portfolio scale. The fix is a tuning decision rather than an edit — `SET LOCAL hnsw.ef_search`, pgvector's iterative scan mode, or over-fetching and re-filtering — and it should be made against a table with enough rows to measure, which this build does not yet have.

- source_spec: `spec-6-1-local-model-serving-embedding-foundation.md`
  summary: **The story asked for an initial embedding pass "callable at startup/seed time"; there is no non-e2e trigger.**
  evidence: Partly answered by design and partly outstanding. Migration 0041 pre-creates one pending row per claim and `refresh_stale_embeddings` is the single idempotent command that fills them, so nothing is missing functionally — but on a dev stack the first pass waits for a scheduler tick, and the only explicit trigger in the tree is `POST /admin/embedding-refresh`, mounted solely under `ENV=e2e`. A `python -m services.rag.refresh` entry point would close it in a few lines. Not built here because an entry point with no caller is the kind of surface that acquires flags nobody needs; add it the first time somebody actually wants to force a pass.

- source_spec: `spec-6-1-local-model-serving-embedding-foundation.md`
  summary: **AC 1's "CPU-small allowed in dev" holds for chat and not for embeddings**: the architecture's verified constrained-hardware pairing names `nomic-embed-text`, which emits 768 dimensions into a `vector(1024)` column.
  evidence: Real, and it is a decision rather than a defect — the story's own Data notes pin `vector(1024)` and say changing the embedding model's dimensionality is a migration rather than a config flip, so the implementation raises `EmbeddingDimensionMismatch` naming both lengths instead of padding. But that means an architecture-level statement about constrained hardware is now only half true, and it is recorded in prose (`services/rag/client.py`, `.env.example`) rather than anywhere the architecture would notice. Either the spine's model table should say the CPU fallback applies to chat only, or a second migration path (a `vector(768)` variant, or a dimension-parameterised column) needs designing. That is the architect's call, not a story's.

- source_spec: `spec-6-1-local-model-serving-embedding-foundation.md`
  summary: **The embedding client builds a fresh `httpx.AsyncClient` per call**, so nothing pools connections.
  evidence: Correct and cheap for what exists today — one refresh every fifteen minutes and one interactive `/similar` query — where a pooled client held across a process lifetime would be a resource to own for no measurable gain. It stops being right when Story 6.3's streaming chat lands and the same base URL is hit per token; that is the point to introduce a shared client on `app.state` with a lifespan, and to revisit `embedding_request_timeout_seconds` alongside it.

- source_spec: `spec-6-1-local-model-serving-embedding-foundation.md`
  summary: **`search_knowledge` is implemented, tested and unreachable from the browser** — the labour-law corpus has no HTTP surface.
  evidence: Intentional and worth stating so it is not later read as an omission. Story 6.4's labour-law quick action is its first consumer, and it will reach it through a registered agent tool rather than a REST route, so adding an endpoint now would create a surface with no client that Epic 6 would then have to justify keeping. The corpus is seeded and embedded so that when 6.4 arrives it retrieves something real rather than an empty table.

- source_spec: `spec-6-1-local-model-serving-embedding-foundation.md`
  summary: **The chat model is pulled and served but has no application caller**, so every dev's first `up` downloads several gigabytes for a model this build never speaks to.
  evidence: Required by AC 1, which asks the compose stack to serve chat *and* embeddings, and deliberate: the knob and the pull exist so that Stories 6.2 and 6.3 add a client rather than a container, and so the capacity question is answered before the code that needs it lands. The cost is real for anyone who only wants to run the tests, and `EMBEDDING_MODEL`/`CHAT_MODEL` are both settable in `deploy/.env` for that reason. Revisit if the first-boot cost becomes an obstacle before 6.3 gives the model a caller.

## Deferred from: code review of 6-3-copilot-chat-with-persistent-threads (2026-08-20)

Two performance shapes raised by the review that are design tradeoffs rather than defects. Everything else the two reviewers found was patched into the diff and is itemised in the spec's Review Triage Log.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-3-copilot-chat-with-persistent-threads.md`
  summary: **Opening the copilot panel runs the entire `claim_detail` assembly — stepper, injury diagram, documents, photos, benefit, reserve check — purely to render one greeting sentence.**
  evidence: Real and measurable, and a consequence of AD-2 rather than an oversight: the seeded case-summary greeting must be deterministic service output, so it reads the case file rather than letting the model invent a sentence, and `claim_detail` is the only assembled read that exists. The cost is one full detail build per panel open, on the handler's critical path, for perhaps three of its forty fields. The honest fix is a narrow projection — a `claim_summary` read that selects only what the greeting quotes — but introducing a second claim-read shape is a services-layer decision with more consumers than this story (Story 5.4's top-30 list and 6.4's quick actions would both want it), and it should be designed once against all of them rather than improvised here.

## Deferred from: 6-4-deterministic-quick-actions-qas (2026-08-20)

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-deterministic-quick-actions-qas.md`
  summary: **The copilot reuses `insight_staleness_disclosure_days` rather than adding the story's `embedding_staleness_threshold`.**
  evidence: A deliberate deviation from the story's Data notes, recorded rather than done. `config.py:333` already carries AD-12's configured threshold, and the disclosure sentence interpolates the number — "last indexed more than N days ago" — from exactly one place, `agents/tools/similar.py::_disclosure`. Two knobs would let the Insights card and the copilot chat tell the same handler two different numbers about the same neighbour in the same session, and AD-12 says "a configured threshold", singular. What is genuinely imperfect is the *name*: the knob now serves an insight card and a chat answer, and reads as though it served only the first. Renaming it to something kind-neutral touches three call sites, the env example and its tests for no behavioural gain; it belongs to whichever story next changes that block for a reason of its own.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-deterministic-quick-actions-qas.md`
  summary: **Every narrating quick action reads the whole `claim_detail` assembly at least once, and the reserve and fraud actions read it twice.**
  evidence: The same shape Story 6.3's review deferred for the greeting, now with a multiplier. Each node calls `claim_reader` for the fenced claim narrative, and `reserve_check`, `fraud_signals` and `rtw_reader` each reach `claim_detail` independently for their own block — so one button press assembles the stepper, the injury diagram, the documents block, the photos block and the benefit twice, to quote a verdict and an injury description. It is correct by AD-13 (one service call per tool, and a tool that shared a cached detail would be a tool holding state between calls) and it is invisible at seeded scale. The fix is the same narrow `claim_summary` projection 6.3 deferred, and it now has a third consumer, which strengthens the case for designing it once across all of them rather than improvising it inside `agents/`.

## Deferred from: code review of 6-4-deterministic-quick-actions-qas (2026-08-20)

One finding that is real but is a design refinement rather than a defect. Everything else the two
reviewers raised was patched into the diff and is itemised in the spec's Review Triage Log.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-deterministic-quick-actions-qas.md`
  summary: **`_BUILDERS`' `Callable[...]` erases the `prompt_key`/`requires_llm` pairing**, so
    `build_node("reserve", prompt_key=None, model=...)` type-checks and fails at runtime inside
    `prompts.load(None)`.
  evidence: Real, and the erasure is genuine — `Mapping[str, Callable[..., QasNode]]` accepts any
    argument list, so the one invariant that matters here (a `requires_llm: True` key has a prompt
    file and a `requires_llm: False` key does not) is enforced only by a test asserting the two maps
    agree, never by a type. It is unreachable today because `build_graph` is the sole caller and
    reads both halves off the same `QuickAction`. Left rather than fixed because the honest repair
    is a typed protocol or an overload pair over a heterogeneous builder table, which is a change to
    how the map is declared rather than a line — and the story that next adds a key (6.5 reuses
    `rtw`, 6.6 gates on the flags) is the one that will feel the gap and can design it against two
    consumers instead of one.
  resolved_by: `_bmad-output/implementation-artifacts/spec-6-6-honest-degradation.md` (2026-08-21).
    Fixed as the typed protocol this entry predicted, and by the story it named. `_BUILDERS` is now
    two maps — `_NARRATING_BUILDERS: Mapping[str, NarratingBuilder]` and
    `_DETERMINISTIC_BUILDERS: Mapping[str, DeterministicBuilder]` — and `build_node` selects between
    them on `requires_llm`. A narrating builder's signature takes a **non-optional** `prompt_key`, so
    the runtime failure inside `prompts.load(None)` is now a mypy error; the mismatched pairings the
    types cannot express (a true flag with no prompt file, a false flag with one) raise in
    `build_graph`, which is `lifespan` and therefore a process that will not start. The reason 6.6
    felt it rather than 6.5 is the one this entry gives: it is the story whose whole subject is that
    the flag means something, and `_build_data_alignment` holding a `BaseChatModel` it promised not
    to use was the flag being enforced by a comment.

## Deferred from: code review of 6-5-human-gated-writes-the-rtw-letter (2026-08-21)

Three findings that are real but are decisions rather than defects. The other nineteen the two
reviewers raised were patched into the diff and are itemised in the spec's Review Triage Log.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-5-human-gated-writes-the-rtw-letter.md`
  summary: **A resume whose caller has lost scope on the drafted claim wedges the thread instead of discarding and auditing the proposal**, which is what the spec's I/O matrix promises.
  evidence: Real, and the guarantee that matters still holds — nothing is written. `resolve_thread` joins under `employer_scope(ctx)` (`data/repositories/copilot.py:151-158`), so a claim reassigned out of the handler's book makes the resume a 404 at `api/routers/copilot.py:1027-1041`, *before* the graph runs. So the fail-safe is delivered by the scope layer rather than by the gate: no `record_copilot_approval` row is written, `pending_approval` stays set in the checkpoint, and that one conversation answers 409 to every further message with no reachable resume. The in-graph scope check at `approval.py:597-604` compares the drafted claim to `runtime.context.claim_business_id`, both derived from the same thread row, so it can never fire through the endpoint and is exercised only by a hand-driven test. Deferring rather than fixing because the architecture already reserved this decision: the spine's Deferred list carries "the precise fail-safe response code for a scope-lost resume" as intentionally unfixed, and the honest repair — an unscoped resume lookup that lets the graph discard and audit — trades directly against the enumeration-oracle argument that shaped the 403/404 split in this same story. Practical consequence is small: the stuck conversation is about a claim the handler can no longer see, and "+ New conversation" is unaffected.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-5-human-gated-writes-the-rtw-letter.md`
  summary: **`create_document` deliberately does not bump `claim.version`, so the letter save has no idempotency if a replay path is ever added.**
  evidence: Real but not live. The pin is unchanged after a successful save, and `run` at `api/routers/copilot.py:1047-1049` deliberately exempts a `command` body from the interrupt-pending 409. Today the thread advisory lock serialises resumes and the approval marker is spent, so no replay path exists — but the `update_claim_field` twin is protected by `update_claim_fields` bumping the version, and the letter is protected only by the absence of a caller. Any future story that retries an approved write, or that resumes a checkpoint out of band, would insert a second identical `document` row with no CAS to stop it. The choice is a uniqueness constraint on (claim, name, body hash), or an explicit written acceptance that the letter save is at-least-once. Not taken here because both are schema or contract decisions wider than the story that surfaced them.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-5-human-gated-writes-the-rtw-letter.md`
  summary: **`lineworker/web` has no formatter and no `format` script, so roughly a third of this story's web diff is unrelated reflow.**
  evidence: Real and repo-wide. `DiaryNav.tsx` shows ~60 of 90 changed lines as pure reflow — import lists, `CLOSED_COMPOSER`, `ComposerPrefill`, the JSX return — where the actual change is two fields and one callback; `CopilotPane.tsx`, `ClaimDetailPane.tsx`, `DocumentViewerDialog.tsx` and `api/copilot.ts` carry the same. The cause is that `package.json` has no prettier config and no `format` script while the server side has ruff in CI, so one editor's 80-column default was applied over a repo written at ~100 and nothing objected. The cost lands on review: on a diff whose whole subject is a write path a reviewer must be able to trace, the signal is diluted. The fix is to adopt a formatter with a committed config and a CI check, and to land the one-time reflow as its own commit — a repo-wide convention decision that should not be taken inside a copilot story, and which the server's ruff scope change (patched in this pass) is the precedent for.

## Deferred from: code review of 6-6-honest-degradation (2026-08-21)

Two findings that are real but are decisions rather than defects. The other twenty-one the two
reviewers raised were patched into the diff and are itemised in the spec's Review Triage Log.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-honest-degradation.md`
  summary: **`probe_model` builds a fresh `httpx.AsyncClient` per probe**, in a module whose docstring is largely about amortising upstream cost.
  evidence: Real, and the same shape as the deferral Story 6.1's review already recorded against the embedding client — which is why it is recorded here rather than fixed here. The cost is genuinely small: the cache coalesces concurrent callers and bounds the rate at one probe per `ai_health_probe_cache_seconds`, so a connection setup every ten seconds is not on any hot path, where the embedding client pays it per claim per refresh cycle. What makes it worth writing down is that the build now has two modules constructing a client per call for the same stated reason and one (`agents/chat_model.py`) pooling by construction with an argued docstring saying why the other two cannot. The honest repair is one decision about client lifetime taken across all three readers of `ollama_base_url`, not a third local fix — and `chat_model.py` already names the shape that decision should take.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-6-honest-degradation.md`
  summary: **The SPA's only polling query is now started by the Insights tab as well as the copilot**, so a supervisor who never opens the copilot still polls model availability every thirty seconds.
  evidence: Real and a direct consequence of a fix taken in this pass — `RefreshInsightsButton` consuming `useCopilotAvailability()` is what stops the 503 being the discovery mechanism, and TanStack Query starts the interval as soon as any component subscribes. The poll is bounded and cheap (one cached probe, shared across every subscriber on the page), so this is a design consequence rather than a leak. But `src/api/dashboard.ts:38` records "nothing in the app polls" as a deliberate stance, and this story broke it for one reason on one surface and then inherited it on a second surface for free. The choice is either to accept availability as an app-wide signal and say so where that stance is written down, or to give the Insights control a one-shot check instead of a subscription — and the first is the more honest reading of what the flag now is, which makes it a convention decision rather than a fix.

## Deferred from: implementation of 7-1-fraud-analytics-workspace (2026-08-21)

Decisions taken during implementation that a later story or a product owner should revisit. Recorded here rather than only in the Dev Agent Record because each is a question left open, not a choice made and closed.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-fraud-analytics-workspace.md`
  summary: **There is no HTTP-level test of a *scoped* analyst**, because the seed contains exactly one analyst and they are `scope_all` — so AD-7 on the three new routes is asserted at the service level with a hand-built `CallerContext` instead.
  evidence: Real, and the spec ruled it deliberately rather than this being an omission. `PERSONAS.analyst` is David Bline with `scope_all`, and seeding a second, narrowed analyst would move persona counts in Stories 1.3, 1.4 and 5.1 (`test_personas.py`, the login picker's option list, the top-bar oracle) and in `e2e/fixtures/login.ts` — four earlier stories' assertions, for one test. What *is* covered: `tests/test_fraud_analytics.py` builds `CallerContext(role=analyst, employer_ids=frozenset({…}))` from Jennifer Park's real employer set and asserts every claim behind the panel, the rates and the red-flag coverage is inside that book, including the empty-book predicate; and the HTTP layer still carries `test_query_parameters_cannot_widen_or_change_the_scope` over all three routes. What is not covered is the composition — that `api.deps.get_caller_context` builds a narrowed analyst context correctly — which is exercised for supervisors and handlers and inferred for analysts. The honest fix is one seeded scoped analyst, taken with the persona-count churn priced in, on a story that is already touching the seed.
  - *Re-raised by Story 7.3, which strengthened the service-level half rather than closing the gap.* `tests/test_segmentation.py` adds a Hypothesis property — no `Segmentation` value can produce a claim outside the unfiltered scoped set, over arbitrary filters rather than chosen ones — plus the narrowed and empty-book analyst against the values endpoint, which is the one payload that *enumerates* a book (employers, sectors, regions, ICD-10 codes) and therefore the one where a scope leak would be an enumeration rather than a count. The composition is still inferred. **Story 7.5 is now the next candidate**: it touches export and its audit trail, so it is the first story after this one with a reason to be in the seed's neighbourhood, and an export audit event naming a scoped analyst is the strongest argument yet for having one.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-fraud-analytics-workspace.md`
  summary: **One rule value still has two wire names**, and this story added no third: `PortfolioSummaryResponse.fraudScoreMin` and `PriorityClaimsResponse.fraudFlagScoreMin` are both `DerivationThresholds.fraud_flag_score_min`, and the two new fraud payloads publish it under the second spelling.
  evidence: 5.4's entry stands unchanged and is now one payload larger. `FraudPanelResponse` and `FraudRatesResponse` both carry `fraudFlagScoreMin`, chosen deliberately as the *existing* longer spelling rather than the shorter one, so `noDerivation.test.ts` still guards two alternatives and not three. It is worth noting what this story did to the surrounding vocabulary, because it makes the ambiguity slightly worse than 5.4 left it: there are now **four** published fraud numbers on one dashboard — `fraudScoreMin`, `fraudFlagScoreMin`, `fraudBandHighMin` and `siuFraudScoreMin` — of which the first two are one parameter under two names and the second and third are two parameters carrying the same integer. A reader who assumes any two of the four are related is wrong in a different way each time. The cheaper name is 5.1's and renaming a shipped field is a contract change to a story neither 5.4 nor 7.1 owns; the decision is now overdue rather than merely open.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-fraud-analytics-workspace.md`
  summary: **The three rate breakdowns and the panel each re-read the caller's whole scoped book**, so an analyst who changes one sort control pays a second full read of the portfolio — 5.3's, 5.4's and 5.5's identical note, now on two more endpoints.
  evidence: The ordering is a total order over an uncapped list computed in Python over a JDM parameter block, so there is no page of it to read and no way to push the flagged rule into SQL without splitting it across two tiers (AD-10). At the seeded 100 claims a re-sort is one read of 100 rows and one fold through three registered derivations, which is free. It is recorded beside the other three because this surface multiplies by the number of *sort clicks* in a session rather than by page count, which is the highest multiplier any of the five aggregates has — and because Epic 7's remaining stories build cohorts, segmentation and export on these same reads. The epic context already names this epic as the natural place to weigh a `GROUP BY` push-down or a materialized ranking; this entry is the fifth endpoint to be counted when that is costed.

## Deferred from: review of 7-1-fraud-analytics-workspace (2026-08-21)

One finding that is real and pre-existing. The other fifteen the two reviewers raised were patched
into the diff and are itemised in the spec's Review Triage Log; one was rejected.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-fraud-analytics-workspace.md`
  summary: **`rulesVersion` names a different document on different routes of the same dashboard** — `derivation_thresholds` (v6) on the two fraud payloads, `priority_weights` (v1) on the drill list, which publishes the thresholds separately as `thresholdsVersion`.
  evidence: Real, inherited rather than introduced here — `/dashboard/summary` and `/dashboard/charts` already set `rules_version` from the thresholds document, and Story 7.1's two new payloads followed the nearer precedent. So an analyst comparing "rules v6" on the fraud panel with "rules v1" on the claims list it drills into is reading two different documents under one field name, on one screen, with no way to tell. Not fixed here because renaming a published field is a contract change to three shipped stories, and the honest repair is one decision about what `rulesVersion` means across the whole dashboard — either every payload names the document it actually read, or the field is split everywhere as `/dashboard/priority-claims` already splits it. Worth taking with the *other* naming entry above (the four fraud numbers under three spellings), since both are the same question asked of different fields and answering them separately is how a third spelling arrives.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-trend-cohort-analytics.md`
  summary: **CLOSED by Story 7.3.** With a cohort split selected, an empty window published zero series, so the Trends period `<select>` and its "View claims" button both went dead — while the same empty window under `cohort=none` published zero-filled points and left both controls live.
  evidence: `trends_of` accumulated `cohort_keys` only from claims that survived the window cut, so no claims meant no series and `plan.periods` was empty; the availability of the keyboard's only drill path depended on an unrelated selector. `PortfolioTrends.buckets` now publishes the window's vocabulary independently of the fold — a fact about the *window*, which is what it always was — and `TrendsPage.planOf` reads it instead of walking a series' points. Taken here rather than in 7.2 for the reason recorded then: a ten-dimension AND makes an empty result routine rather than rare, so the control had to stay live. `tests/test_segmentation.py::test_an_impossible_combination_is_a_200_with_empty_aggregates` and the e2e zero-result spec assert it from both sides.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-trend-cohort-analytics.md`
  summary: `maxBuckets` bounds the size of the answer, not the cost of producing it — `portfolio_trends` reads the whole scoped book with no date predicate, so the dominant O(scope) per-claim fold is unchanged by any window width.
  evidence: `portfolio_trends` issues one unconditional `select_claim_columns_with_employer` over the caller's entire book; only the shaping loop scales with buckets × cohorts × metrics. This is the sixth endpoint to fold O(scope) in Python per request, and Story 7.2's Design Note 4 decided against a `GROUP BY` push-down deliberately rather than by inheritance, stating the trigger as a scope beyond roughly 10^4 claims or Story 7.3 multiplying grain × cohort × segmentation on one request. Recorded here so the sixth entry is counted when that is costed; the JDM description no longer claims the cap relieves it.


## Deferred from: implementation of 7-3-segmentation-universal-drill-down (2026-08-22)

*Every entry recorded under this heading is an accepted limit and has been moved to [Accepted limits](#accepted-limits) — nothing was deleted.*

## Deferred from: review of 7-3-segmentation-universal-drill-down (2026-08-22)

Four findings from the two adversarial passes that are real but are not this story's to fix. The
other sixteen were patched into the diff and are itemised in the spec's Review Triage Log; none was
rejected, and neither reviewer found an intent gap or a spec defect.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-3-segmentation-universal-drill-down.md`
  summary: **The three age edges are deliberately absent from `noDerivation.test.ts`'s `DERIVED_FIELDS`**, so the browser-side guard against re-deriving a rule value is switched off for `ageYoungerMin`/`ageOlderMin`/`ageOldestMin` across the whole tree.
  evidence: Real, and the omission's stated argument holds today: no payload in this console carries `employee.age`, so there is nothing for a client-side re-band to band — only `select_drill_rows` and the trends read touch the column and neither publishes it. But `ageBands.ts` does arithmetic on the three published edges (`older - 1`, `oldest - 1`) to turn half-open intervals into the closed ones a reader expects, and the guard that would catch a *second* consumer doing arithmetic on those fields is off. The day any payload publishes a worker's age — 7.4's financial decomposition segments by the same dimensions, and 7.5 exports whatever is on screen — a client-side re-band becomes legal and silent. The honest fix is to add the three names once a payload carries an age, which is a one-line change made at the moment it starts protecting something.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-3-segmentation-universal-drill-down.md`
  summary: **`lineworker/web/openapi.json` is gitignored**, so the CI step that is supposed to fail the build on a stale generated client only ever checks `src/api/schema.d.ts` — and this spec's own verification command inherited the same blind spot.
  evidence: Confirmed with `git check-ignore -v`: `lineworker/.gitignore:18` ignores `web/openapi.json`. The documented check is `npm run generate:api && git diff --exit-code -- src/api/schema.d.ts openapi.json`, and the second path is a no-op against an untracked file. In practice the guard still works, because `openapi.json` is the input `openapi-typescript` turns into `schema.d.ts` and a drifted spec shows up in the emitted types — but it works by accident of the pipeline rather than by the assertion the command appears to make, and anything the generator does not project into TypeScript (descriptions, examples, response documentation, `operationId` changes) drifts unobserved. The honest fix is to track the file or to drop it from the command; leaving it is the third option, and the one that keeps a check in CI that reads stronger than it is.

## Deferred from: implementation of 7-4-financial-decomposition (2026-08-22)

Four entries. Two are standing questions this story inherited and re-quantified rather than
answered, one is a count the epic asked to be kept deliberately rather than by default, and one is
the same AD-7 gap Stories 7.1, 7.2 and 7.3 each recorded and none has been able to close.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-4-financial-decomposition.md`
  summary: **The `total_paid` basis now carries a whole decomposition surface**, so the open product decision at `:501` moves four figures on a new screen as well as Epic 5's KPI cards.
  evidence: `services/worklist/decomposition.py` publishes `paidCents` as the registered `total_paid` derivation — the static `paid_*` columns — deliberately and for a stated reason: Epic 5's Total Paid card and 5.3's by-employer chart already fold that derivation, and a decomposition that summed a different notion of "paid" would put two portfolio totals one route apart. The consequence is that the finding recorded against Story 5.1 now has more surface behind it. `paidCents` is `167,049,700` over the seeded book while `33,598,500` of paid bills and expenses on the 38 open claims are outside it; every cost-driver cohort's `paidCents` inherits the same exclusion, and it is most visible on the litigation pair, whose `withDriver` cohort reports **exactly zero paid** because all three litigated claims are open. The card is built to survive that — the average projected cost is the headline and all three totals travel beside it — and `TotalsCard.tsx`'s caption states what the figure sums rather than letting the word "paid" carry it. What is *not* fixed is the basis, because fixing it is still one call site plus both oracles and it still moves Epic 5's cards; what has changed is that the owner is now deciding against four more figures. `projectedCents` inherits it too, being paid plus reserve.
  
- source_spec: `_bmad-output/implementation-artifacts/spec-7-4-financial-decomposition.md`
  summary: **The seventh O(scope) fold endpoint, and the first pair of them that reads three tables per request** — the `GROUP BY` push-down count, kept deliberately.
  evidence: Story 7.2 stated the revisit condition and Story 7.3's Design Note 4 narrowed it to scope size alone, retiring the "grain × cohort × segmentation" clause on the ground that a filter narrows a fold and multiplies nothing. This story adds two more endpoints to the count and one of them departs from the shape: `/dashboard/financials` is the seventh single-read fold, and `/dashboard/financials/reserve-adequacy` takes **three** scoped reads — the claims, then every one of their payment-schedule weeks and bills in bulk — and folds a per-claim verdict over all of them. At the seeded volume that is 100 claims, ~1,100 week rows and 520 bills per request and is free; at 10⁴ claims it is the first analyst surface whose *row* count grows faster than its claim count. The push-down answer is still no, and now for a second reason beside the two already recorded: the verdict is `services/financials`' and is reached through `reserve_check_from_rows`, so a SQL fold would be a second implementation of the rule AC 2 exists to have exactly one of. The honest trigger for this one is therefore not scope size but *schedule* size, and the honest fix if it is ever reached is a materialized per-claim verdict — which is a derived column `test_no_derived_columns.py` currently forbids and which would need its own invalidation story. Recorded so the count keeps being kept.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-4-financial-decomposition.md`
  summary: **A narrowed analyst still cannot be demonstrated at HTTP level** — the fourth story in this epic to record it, now over a surface that publishes money.
  evidence: Unchanged from 7.1, 7.2 and 7.3: the seed's one analyst is `David Bline`, who is `scope_all`, so every HTTP assertion about scope on this section is an assertion about a caller who can see everything. AD-7 is proved at the *service* level instead — `test_a_scoped_analysts_figures_are_folded_over_her_own_book` builds Jennifer Park's employer set into a hand-made analyst `CallerContext` and asserts both that the totals are her book's and that every employer the breakdown names is one of hers, and `test_an_analyst_with_no_employers_reads_an_empty_book_not_the_portfolio` covers the empty-book case that fails open if anybody writes `if not ctx.employer_ids: skip the filter`. The HTTP layer carries the smuggled-parameter check on both routes. What this story adds to the entry is the stakes: this is the first analyst surface whose leak would be *money* — a scope predicate dropped here publishes the portfolio's spend to a caller with three employers, which is a more consequential failure than a count. The fix is still one seeded scoped analyst, and it still moves persona counts in Stories 1.3, 1.4 and 5.1 plus `e2e/fixtures/login.ts`.

## Deferred from: review of 7-4-financial-decomposition (2026-08-22)

Two entries, both surfaced by the review of this story's own change and both left open deliberately
after the patch pass narrowed what could be narrowed.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-4-financial-decomposition.md`
  summary: **No cursor field can pin a verdict that a concurrent single-claim read rewrites** — the paging hazard specific to a facet derived from a written table.
  evidence: The cursor now carries `bands_version` and refuses a page bucketed by a superseded `reserve_bands` (`test_a_cursor_bucketed_by_a_superseded_reserve_bands_is_refused`), which closes the *document* half of the review's finding. The data half cannot be closed the same way: the verdict reads `payment_schedule_week.status`, and `reserve_check_for_claim` **writes** those statuses whenever anybody opens a claim's case file, so a handler viewing WC-0042 between an analyst's page one and page two can move it out of the `light` bucket with no document having changed and no version having moved. The claim is then silently skipped. This is the ordinary hazard of every facet over mutable columns — an edited stage moves a claim out of `filter[stage]=treatment` identically — and it is recorded rather than fixed because the honest fixes are large: a stable snapshot per cursor (which the architecture has no home for) or the materialized verdict column `test_no_derived_columns.py` forbids. Named here so that the next surface tempted to derive a facet from a written table finds the reasoning rather than rediscovering it.
  resolution: |
    **The half this entry says "cannot be closed the same way" is now
    detected rather than closed, by Story 9.8.**

    The drill-through cursor carries `verdict_digest` — a sixteen-character hash of
    the claim ids `filter[reserveVerdict]` left standing — minted only when that
    facet is set, beside the `bands_version` this entry describes. On every cursor
    page it is recomputed and compared, and a mismatch is refused with the same 400
    a superseded `reserve_bands` gets. So the walk *detects* the rewrite: a handler
    opening WC-0042 between an analyst's page one and page two no longer silently
    skips a claim, they get an honest "reload the list from the first page".

    Neither of the two large fixes the entry rules out was attempted: there is no
    per-cursor snapshot and no materialized verdict column
    (`test_no_derived_columns.py` still forbids one). Removing the write itself —
    `materialize_schedule`, reached from `GET /claims/{id}` — is **Story 9.7**, which
    is blocked on the Architect's scheduler decision; this story only makes a walk
    able to see it happen.

    Deliberately narrower than "pin every facet over a mutable column". A stage
    edited between two pages moves a claim out of `filter[stage]=treatment`
    identically, and that is *not* pinned: somebody performed that edit, the keyset
    resumption walks the survivors correctly, and refusing every page whose
    population moved would make a busy book unwalkable. The verdict is the one facet
    a **read** rewrites, which is the asymmetry that earns it a pin.

## Deferred from: implementation of 7-5-dataset-chart-export (2026-08-22)

Five entries. One is the same AD-7 gap Stories 7.1–7.4 each recorded, declined again *by the story
that was named as its candidate*; one is a cap that governs two things with different costs; one is
the O(scope) count the epic asked to be kept deliberately; one is a column the export deliberately
does not carry; and one is an audit row Story 8.1's purge cascade cannot reach from a claim.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-dataset-chart-export.md`
  summary: **A narrowed analyst still cannot be demonstrated at HTTP level** — and this is the story 7.4's entry named as the candidate to close it. It is declined again, with the stakes now higher than money.
  evidence: Unchanged in mechanism from 7.1–7.4: the seed's one analyst is `David Bline`, who is `scope_all`, so every HTTP assertion about scope on these six routes is an assertion about a caller who can see everything. AD-7 is proved at the *service* level instead — `test_a_narrowed_analyst_exports_only_their_own_book` builds Jennifer Park's employer set into a hand-made analyst `CallerContext` and asserts both that the exported claim ids are a strict subset of the portfolio's and that they are exactly her book's — and the HTTP layer carries a byte-identity smuggled-parameter check on all eight route/table pairs. What this story adds is the consequence of *not* closing it: an export writes an `audit_event` naming an actor, a target and a filter set, and that row is now the compliance record of PHI leaving the system (NFR-5). A scope error on this path does not merely show a caller more than their book — it produces a **file** that outlives the session and an audit row asserting the caller was entitled to it. So the gap is no longer only about what a test can see; it is about what the log would say afterwards. The fix is still one seeded scoped analyst, and it still moves persona counts in Stories 1.3, 1.4 and 5.1 plus `e2e/fixtures/login.ts` — which is why four stories have now declined it, and why it is worth an owner rather than a fifth dev-time judgement. This story's ACs never mention personas, which is the Block If the spec gave and the reason it was not taken here.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-dataset-chart-export.md`
  summary: **The eighth O(scope) fold** — six export routes re-run five existing folds over the caller's whole scope, and one of them re-runs the three-read adequacy path.
  evidence: The count Story 7.2 started, 7.3 narrowed to scope size alone and 7.4 pushed to seven. This story adds an eighth *entry point* without adding a new fold: every export calls the function its sibling route already calls, so the arithmetic per claim is unchanged and the read count is guarded in both directions (`test_the_export_takes_one_scoped_read_for_five_of_six`, `test_the_adequacy_export_takes_three_scoped_reads`). What is new is the *frequency* profile rather than the shape: an export is an explicit gesture rather than a page render, so these six are the first analyst endpoints a user invokes deliberately and rarely — which argues against a push-down rather than for one. `export_limits.maxRows` now bounds the answer explicitly where before it was bounded by luck, and that is the one thing this story adds to the standing question. The revisit trigger is unchanged: scope size for seven of the eight, and *schedule* size for the adequacy path, per 7.4's entry.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-dataset-chart-export.md`
  summary: **An `export.*` audit row is not reachable from the claims it exported**, so Story 8.1's purge cascade cannot find, redact or cascade to it by claim.
  evidence: `COPILOT_APPROVAL_ENTITY` argues at length that a *content-free* audit row must carry the **claim** as `entity`, because such a row has no diff to hold a `claim_id` in the way `injuries._diff` does — "the claim has to *be* the entity or the row is unreachable from the claim it concerns" — and it names Story 8.1's purge cascade as what finds rows that way. `record_export` departs from that precedent and sets `entity=<ExportTarget>`, `entity_id=<16-hex filter digest>`. The departure is argued in the function's docstring and is correct: an approval is about exactly one claim and an export is about a *set* — a hundred claims under a segmentation, a distribution over a whole book, or (for an emptied segment) none — so there is no single claim to name, and naming one would assert a relationship the event does not have and let a purge of that one claim erase an egress record covering ninety-nine others. What is deferred is the consequence, which is real: when Story 8.1 purges a claim, nothing on the export rows whose files contained it says so, so those rows are neither found nor redacted by a claim-keyed cascade. The row's `after` survives the cascade untouched — which is safe today only because it is content-free by construction (the filter set is query terms, checked by a scan for a seeded worker's name), so there is no PHI in it to redact. Closing it properly would mean putting a claim id list in `after`, which is exactly the content AC 3 and AD-11 forbid: a hundred claim ids *is* the file's index. The honest options for 8.1 are therefore (a) accept that export rows are purged by age and by actor rather than by claim, or (b) add a join table from export event to claim, which is a new table holding precisely the association the content-free rule exists to avoid persisting. That is a retention decision with an owner, which is why it is recorded here rather than settled by this story.

## Deferred from: review of 7-5-dataset-chart-export (2026-08-22)

Three entries. Two are questions the review opened that this story cannot close on its own
authority — one about what an export is allowed to carry, one about what its audit row can be
found by — and the third is a property of writing an audit row on a `GET` that the AC requires
and no mechanism in this build reconciles.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-dataset-chart-export.md`
  summary: **An export audit row is unreachable from the claims whose data its file carried** — the one code path that moved PHI outside the system is the one Story 8.1's purge cascade cannot find by claim.
  evidence: `services/audit/__init__.py` argues at length under `COPILOT_APPROVAL_ENTITY` that a content-free row must carry the *claim* as `entity`, because such a row "has no diff to carry a `claim_id` … so the claim has to *be* the entity or the row is unreachable from the claim it concerns", naming Story 8.1 explicitly. `record_export` cannot follow it and does not pretend to: an export spans a hundred claims, there is no single claim to name, and naming one would be a lie the retention floor would preserve for seven years. So `entity` is the target and `entity_id` is the filter digest, and the consequence is real — a subject-access or purge request scoped to one worker can find every edit, note, email and approval touching their claim, and cannot find the export that put their name in a spreadsheet on someone's laptop. The two honest options both belong to 8.1 rather than here: a join table naming the claims an export covered (which re-introduces the content the AC forbids the row to carry, and would itself need purging), or an accepted, documented limit on what the cascade reaches with export rows redacted on the same floor as everything else. Recorded so 8.1 finds the reasoning rather than the gap.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-dataset-chart-export.md`
  summary: **Six `GET` routes now perform a durable, non-idempotent write with no reconciliation in either direction**, so the row designated as the compliance record of PHI egress cannot answer whether the file was actually delivered.
  evidence: AC 2 requires the audit row and AC 3 requires the bytes, and nothing in HTTP lets both be true exactly once. The ordering was chosen deliberately and is argued at `_finish`: the row is committed after the table is materialised and before the first byte, so a transmission that dies half-way is *over*-recorded rather than unrecorded, which is the safe direction for an egress log. What is unaddressed is the other side of the same coin — a proxy retry, a browser prefetch, a reload, or a double click that races the disabled flag each write a second row describing an export that may never have reached anybody — and there is no request id, byte count or completion marker to tell two rows describing one delivery from two rows describing two. At the seeded volume and with a deliberate, rare user gesture this is noise rather than a defect; it matters the first time somebody counts export rows to answer "how many times did this leave", which is exactly the question the row exists for. The fix is a delivery-confirmation or an idempotency key on the request, and both are larger than this story.

## Deferred from: implementation of 8-1-phi-purge-cascade-retention (2026-08-22)

Seven entries. Three are decisions this story **closes** rather than defers, recorded here because
four earlier stories handed them to 8.1 by name and a reader following those entries needs to find
the answer rather than the question. Four are genuine residuals the cascade leaves.

### Closed by this story

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **The `audit_redactor_delete` retention floor stays baked into the RLS policy and is not driven from a `current_setting()` GUC** — Story 8.1's own task list proposed the GUC and it is refused.
  evidence: Task 1 suggested injecting the floor "from config at policy-evaluation time … e.g. via a `current_setting()` GUC set by the purge session". That would make the policy settable by the very session it guards, which is not a safety net: a bug — or an operator — setting the interval to zero would delete the whole audit log and RLS would agree, destroying the property Task 5 asks for in as many words ("the RLS policy is the safety net — a bug cannot delete younger rows"). Migration 0003 already bakes `Settings.audit_retention_years` into the policy at migration time, so the floor is changeable only by a reviewed migration, which is the correct authority for a number that decides how long a compliance record survives. The residual the GUC was meant to solve — config says five years, the policy still says seven, the job silently deletes nothing — is closed instead by `purge_expired_audit_events` counting the rows past its own cutoff and raising `RetentionFloorMismatch` when the delete comes up short. `20260822_0049_purge_grants.py`'s module docstring carries the argument and `test_a_floor_shorter_than_the_policys_raises_rather_than_under_deleting` pins it. **What is still open**: nothing in CI compares the two numbers at deploy time, so the divergence is detected on the first sweep after the change rather than at the change. Story 8.4's gate is the place for a check that reads the policy's `USING` clause and the setting together.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **`notes.py`, `meetings.py` and `emails.py` keep copying free text into `audit_event.after`** — the question Stories 4.1, 4.2 and 4.3 each deferred to this one, decided in favour of keeping it.
  evidence: The three stories left the question of whether a diary note's text, a meeting's location and an email's subject should stop being copied into the audit diff, and whether `emails.py`'s omission of `body` should be adopted by the other two or reverted. The decision is that **they keep it**. A diff recording which key changed but not to what is not an audit trail, and AD-4 fixes the schema around `before`/`after` precisely to hold the change; the PHI this leaves at rest is addressed by the mechanism the architecture already designed for it, which is redact-in-place, which this story delivers and which treats all three tables identically. `emails.py`'s omission of `body` stays as a proportionality choice — the largest blob the console stores, in an append-only table with no delete path, whose diff is therefore never needed to reconstruct a removed row — and is documented as such rather than harmonised away. **The residual, recorded rather than argued away**: PHI free text lives in `audit_event.after` until the subject is purged or the seven-year floor passes, and there is no interim redaction. A deployment that never purges anybody keeps every diary note's words for seven years.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **`export.*` audit rows are purged by age and by actor, never by claim** — the gap Story 7.5 recorded twice against this story, accepted as a documented limit.
  evidence: 7.5's implementation entry and its review entry both hand 8.1 the same choice: accept that an export row is unreachable from the claims whose data its file carried, or add a join table naming those claims. The cascade takes the first. `record_export` sets `entity=<ExportTarget>` and `entity_id=<16-hex filter digest>` because an export spans a set of claims and naming one would assert a relationship the event does not have — and a purge keyed on it would erase an egress record because one of a hundred claims it counted was deleted. The join table is refused because it is precisely the content AD-11 and 7.5's AC forbid persisting: a hundred claim ids *is* the file's index, and the table would itself need purging. What makes the acceptance safe rather than merely convenient is that an export row's `after` is content-free by construction — query terms, not claim values, which `test_dataset_export.py` proves by scanning the serialised payload for a seeded worker's name — so there is nothing in those rows to redact. They age out under `purge_expired_audit_events`. `services/audit/purge.py::_claim_audit_predicate` says so at the point where a reader would otherwise wonder why the predicate does not reach them. **What is still open**: a subject-access request scoped to one worker can find every edit, note, email and approval touching their claim and cannot find the export that put their name in a spreadsheet. That is a compliance posture with an owner, not a defect with a fix.

### Residuals this story leaves

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **The cascade spans two connections and two transactions, so a crash between their commits loses the `redact` events without losing the redaction.**
  evidence: AD-4 makes this structural rather than accidental: the redaction runs as `audit_redactor` (UPDATE on two columns, no INSERT) and the `redact` events are written by `lineworker_app` (INSERT, no UPDATE), and no transaction can span two roles on two connections. `_redact` commits the redaction *first*, which is the safe direction — the PHI is gone and the events are missing, where the other ordering would leave events asserting a redaction that had not happened and a re-run emitting them a second time. The residual is that a re-run does **not** re-emit the lost events, because the rows now carry the marker and `_unredacted()` skips them by design. Closing it needs either a two-phase commit across the two roles or a durable intent record written before the redaction — the second is a new table whose rows would themselves be audit-adjacent, and both are larger than this story. The window is one commit wide on a command that runs rarely and deliberately.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **`purge_user` redacts every diff its subject ever wrote, including claim-field edits on claims that are not being purged** — deliberate breadth, and it is destructive to case history.
  evidence: The predicate is `actor_id = :user_id`, so a handler's purge removes the `before`/`after` of every claim edit they made as well as the notes, meetings and emails the command deletes. The narrower alternative — redact only rows whose `entity` is one of the purged tables — was rejected because it would leave the same free text in the same JSONB column under a different `entity`, which is not a distinction PHI has. The cost is that a *claim* that was never purged loses the field-level history of every edit one departed handler made to it, and no other actor's rows are affected, so the file's history becomes unevenly readable. The skeleton survives throughout, so "who changed what field on which claim, and when" stays provable. Whether a user purge should be that broad is a compliance question with an owner; the code and `purge_user`'s docstring state the choice plainly so that narrowing it later is a one-line change to one predicate.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **A JSONB column assigned `None` through SQLAlchemy stores the JSON scalar `null`, not SQL NULL** — a trap that produced a real defect in this story and that every future predicate over `audit_event.before`/`after` inherits.
  evidence: `postgresql.JSONB` defaults to `none_as_null=False`, so `audit.record(..., before=None, after=None)` writes `'null'::jsonb` into both columns — and `IS NOT NULL` is true of them. The driver reads a JSON `null` back as Python `None` exactly as it reads SQL NULL, so the two are indistinguishable from every assertion written in Python, including the ones in `test_copilot_approval.py` and `test_dataset_export.py` that check a content-free row's diffs are `None`. The first version of the cascade therefore stamped the redaction marker over every `copilot_approval` and `export` row it found and emitted a `redact` event for each, asserting in the audit log a redaction that had not happened. `services/audit/purge.py::_has_content` closes it with `jsonb_typeof(column) <> 'null'` and argues the point at length, and `test_a_content_free_row_is_left_exactly_as_it_is_and_emits_no_redact_event` is the regression. **What is not closed**: the columns still hold JSON `null` rather than SQL NULL, so the storage is one representation and the code's mental model is another. Passing `sqlalchemy.null()` from `record` — or declaring `JSONB(none_as_null=True)` on both columns — would make them agree, and either is a migration-free change with a data backfill behind it (existing rows keep the JSON scalar). Worth taking with whichever story next touches `audit_event`'s schema; until then, any new predicate over those columns must use `_has_content` rather than `IS NOT NULL`.

## Deferred from: code review of 8-1-phi-purge-cascade-retention (2026-08-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **`GRANT audit_redactor TO CURRENT_USER` grants membership to whichever role ran the migration, which is not the role that later assumes it** — and it works today only because every owner in every profile is a superuser.
  evidence: Migration 0049's premise is "whoever runs the migration is by definition the role that will later run the purge script's owner connection". That is false in this repo's own e2e profile: `e2e/fixtures/reset.ts` runs `alembic upgrade head` with `ALEMBIC_DATABASE_URL` overridden to `lineworker_e2e_owner`, while `deploy/compose.e2e.yaml` gives the api service `lineworker` — a different role, and the one `purgeClaim` actually runs `SET ROLE` on. Both are superusers, so `SET ROLE` succeeds regardless of membership, which is exactly the condition the migration says it exists to stop relying on. Two consequences, both latent until a deployment stops using a superuser owner: the grant lands on the wrong role, and `GRANT <role> TO <role>` additionally requires the grantor to hold ADMIN OPTION, so a cluster where a DBA pre-creates `audit_redactor` and the migration runs as a non-superuser owner fails at `alembic upgrade` rather than at purge time. Closing it means naming the assuming role in configuration and granting to that, plus a startup membership check — a config-surface decision this story deliberately did not take.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **The daily audit sweep is two unbatched full-table scans of what will become the largest table in the schema, registered as a serial job.**
  evidence: There is no index on `audit_event.at` anywhere in `data/versions/`, and `purge_expired_audit_events` issues `count(*) WHERE at < cutoff` followed by an unbatched `DELETE WHERE at < cutoff`. `api/app.py` registers it without `background=True`, so it holds the scheduler tick and everything queued behind it — the payment batch and the embedding refresh. The checkpoint sweep beside it argues serial is acceptable because "a sweep is a bounded set of `DELETE`s"; no equivalent argument is made for the audit sweep, and after seven years it is not bounded. The first sweep after the floor is reached, or the first after a long outage, is one statement holding locks and generating WAL proportional to the whole backlog. The fix is an index on `at`, batched deletes, and `background=True` — a migration plus a job-shape change, which is larger than a review patch and which the Story 6.2 review already recorded the general form of.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **Checkpoint deletion is irreversible mid-cascade**, because the saver's pool is `autocommit=True` while the relational deletes around it are not.
  evidence: `_purge_stores` discards a claim's transcripts through the injected deleter, which reaches `AsyncPostgresSaver` on the psycopg pool `build_copilot` opens with `autocommit=True`. A failure part-way through therefore rolls back every relational delete while the transcripts already discarded stay gone — the `copilot_thread` rows come back, their checkpoints do not. The docstrings now say so rather than claiming re-runnability the arrangement cannot deliver, and the P3 resume path means the *rest* of the cascade can still be completed, but the transcript loss itself is unrecoverable. Making it atomic would mean either a non-autocommit saver connection enrolled in the same transaction (which the vendor's API does not offer) or a two-phase delete keyed on a tombstone — both larger than this story.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **`purge_user` deletes diary notes, meetings and email logs attached to claims that are not being purged**, stripping case history other handlers are actively working from.
  evidence: `USER_CHILDREN` keys on `app_user_id`, so purging a departed handler removes every note, meeting and email they ever wrote — including rows hanging off live claims. The docstring defends the *redaction* breadth at length and never argues the *deletion* breadth, and no test asserts a claim still renders correctly after one of its notes was removed this way. The e2e spec never exercises `--user` at all. The behaviour follows directly from "a purge request for a claim/user" and is probably what a subject-access deletion means, but "what a departing employee's purge does to other people's case files" is a compliance decision with an owner rather than a dev-time reading of an AC.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **The AD-11 ownership guard cannot see two easy evasions**: a delete against a table object rather than a model name, and a `DELETE FROM` composed by an f-string.
  evidence: `_is_phi_delete_call` matches `sa.delete(<Name>)` only when the bare argument name is in `PHI_MODELS`, so `sa.delete(_AUDIT)` — the exact spelling `services/audit/retention.py` itself uses via `AuditEvent.__table__` — is invisible to it; and `_RAW_DELETE` matches string literals, so `sa.text(f"DELETE FROM {table}")` passes. Neither shape appears in `test_the_phi_delete_guard_would_notice_a_new_delete`, so the guard's own mandatory positive control does not exercise them. Closing it properly needs alias tracking from `Model.__table__` assignments to their names and a decision about how much f-string SQL to reason about, which is a guard-design change rather than a patch.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **An in-flight copilot run can re-write checkpoints after the purge has discarded them**, leaving PHI behind with no `copilot_thread` row pointing at it.
  evidence: The cascade takes no thread lock. `agents/threads.py` has `RUN_LOCK_NAMESPACE` and a single-flight discipline precisely because a thread may have a run active or an interrupt pending, and a purge that deletes a thread's checkpoints while the saver is mid-run will simply see them written again by the run that owns them. The window is narrow and the management command is operator-initiated rather than concurrent by design, but the resulting state is the worst kind: orphaned transcript rows that no claim, thread or user row can reach, and therefore that no future purge finds. The fix is to take the existing run lock over each thread before discarding it.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **A claim edit committing between the cascade's commit and `_redact`'s SELECT leaves an unredacted diff behind.**
  evidence: `purge_claim` reads the audit rows to redact after it has committed the relational deletes, and takes no lock on the claim. A command that committed its own audit row in that window names a claim that no longer exists and is never redacted. The window is small and shrinking — once the `claim` row is gone every claim-scoped command 404s before it writes — but it is not zero, and the row it leaves is exactly the one nobody looks for again. `SELECT … FOR UPDATE` on the claim at the top of the cascade would close it.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **Nothing stops two `document` or `photo` rows sharing one `blob_key`**, so purging one claim could blind another claim's document.
  evidence: Neither column carries a unique constraint, and `_delete_blobs` removes every key the purged claim's rows named without asking whether a surviving row still names it. This is unreachable today — `blob_key` is NULL on all 563 `document` and all 293 `photo` rows and no code path writes one — so it is a trap for the story that adds upload rather than a live defect. The story that chooses the blob backend should either add the uniqueness constraint or make the cascade delete only keys with no surviving row.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **The api process now assumes `audit_redactor` on the owner credential once a day**, which widens what a compromise of that process reaches.
  evidence: `_redactor.py` declines to mint a fourth login role partly because it "would also be a credential whose entire purpose is to be able to overwrite the audit log, sitting in the same env file as everything else". The observation is right and the alternative it chose is strictly more privileged: `ALEMBIC_DATABASE_URL` is the schema owner, it is already on the api service in every profile, and this story is what makes the long-lived web process exercise it on a schedule rather than only at boot. Nothing here is wrong given the deferred IdP and scheduler decisions, but "the api holds owner credentials and now uses them daily" belongs in the deployment documentation Story 8.4 writes.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **The spec's own I/O matrix now disagrees with the shipped checkpoint sweep**, and the matrix is inside the read-only intent contract.
  evidence: The matrix's error-handling cell for the checkpoint retention job says "A failing thread deletion aborts the run". Review finding P9 established that this makes one permanently-undeletable thread stall every subsequent daily sweep, so the ninety-day floor would never be enforced again — which contradicts AC 5 in the same document. The contract is therefore internally inconsistent, and only one resolution keeps AC 5 true, so the sweep now skips the failing thread, deletes the rest, and logs a failure count. Recorded rather than silently absorbed: the matrix cell should be amended by whoever owns the intent contract.

## Deferred from: implementation of 8-2-database-audit-log-hardening (2026-08-22)

### Residuals this story leaves

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-database-audit-log-hardening.md`
  summary: **"Bounded rotation" is bounded in *days*, not in bytes** — the database log's footprint is seven files with no cap on how large any of them gets.
  evidence: The posture is `log_filename=postgresql-%a.log` + `log_truncate_on_rotation=on` + `log_rotation_age=1d` + `log_rotation_size=0`, which is PostgreSQL's own documented recipe for "keep a week and no more": seven files, one per weekday, each truncated when its day comes round. A byte cap was considered and rejected rather than overlooked — with a `%a` filename, `log_rotation_size` rotates *within* a day back onto the same file and silently destroys that morning's lines, which is a worse property for an audit-adjacent log than a size bound is worth. What makes the time bound sufficient today is the posture itself: with `pgaudit.log=ddl,role` and every DML class pinned off, a day's log is connection lines and schema changes rather than traffic, so the files stay small. What would break that is a deployment enabling connection logging, statement logging or a low `log_min_duration_statement` for debugging — all of which this story pins off but none of which it can prevent an operator overriding. Closing it properly means either a distinct filename pattern with a size cap (which stops being seven files and starts being unbounded), or disk monitoring, which is part of the observability stack Story 8.4 documents as deferred by decision.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-database-audit-log-hardening.md`
  summary: **`log_min_error_statement=panic` trades error-statement debuggability for the PHI surface**, and it is a real operational cost, not a free win.
  evidence: The default is `error`, which logs the *text of the statement* that raised — including its literal values. For this build that means a failed `INSERT` into `diary_note` would put a handler's note in the database log, and a constraint violation on `claim` would put the claim's fields there, both in the one store the Story 8.1 cascade cannot reach. Setting it to `panic` means effectively no error ever logs its statement. The cost lands on whoever debugs a production failure: the log says an error occurred and its SQLSTATE, and does not say which statement caused it, so diagnosis has to come from the application's own structured log — which by AD-11 carries an exception *class name* and no message. That is a deliberately thin trail. The alternative postures are `log_min_error_statement=panic` with statement text recovered from `pg_stat_statements` (normalised, so literal-free — a real option that needs an extension and a decision), or accepting `error` and treating the DB log as PHI-class for backup and purge purposes, which contradicts the whole point of this story. Recorded so the next person debugging a 500 knows why the log is quiet.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-database-audit-log-hardening.md`
  summary: **The blocked-value processor's firing is invisible until somebody reads a log line** — there is no counter, no alert and no test that would catch a leak in production.
  evidence: `logging_config._block_unallowlisted` replaces a non-allowlisted value with `<blocked:not-on-log-allowlist>` and returns. That is the right shape — Design Note 5 argues that dropping the key would make a leaking call render identically to a clean one — but it makes the backstop's *activation* a fact that exists only in the emitted line. `scripts/lint_log_phi.py` is the actual gate and covers everything it can see statically; what it cannot see is a keyword name computed at run time, or one arriving through the `**` unpack it exempts, and those are precisely the cases the processor exists for. So the two mechanisms that could tell an operator "your build is leaking" are a build-time lint that already passed and a string nobody greps for. `tests/test_log_phi_runtime.py::test_no_log_call_in_the_edit_path_uses_a_non_allowlisted_key` asserts the marker is absent for one flow, which is a floor rather than coverage. Emitting a counter, or raising in non-prod environments, both need a decision about what a logging pipeline is allowed to do when it finds a problem — and raising is explicitly refused here, because a log call must not break the request it is describing.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-database-audit-log-hardening.md`
  summary: **pgaudit's `role` class capture is written to a file nothing reads, ships or alerts on** — the capture is real and the *use* of it is not built.
  evidence: `pgaudit.log=ddl,role` means every `GRANT`, `REVOKE`, `CREATE ROLE` and `ALTER ROLE` lands in `postgresql-%a.log` inside PGDATA, which is exactly what AD-11 asks for and exactly what a compliance reader would want to see. But there is no log shipping, no aggregation, no dashboard and no alert — all deferred by decision, and Story 8.4 documents the deferral with its reopening trigger. The practical consequence is that a privilege escalation is *recorded* for at most seven days and is noticed only if somebody execs into the container and reads the file. That is a weaker property than "the database is audited" sounds, and the seven-day window above is what bounds it: an escalation eight days old has already been truncated by its own weekday's rotation. Closing it is the observability stack, not a change to this posture.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-database-audit-log-hardening.md`
  summary: **CI still cannot boot the prod profile, so TLS is proven by configuration and by two manual commands only.**
  evidence: There are no certificates in version control and there will not be, so `docker compose -f deploy/compose.yaml -f deploy/compose.gpu.yaml -f deploy/compose.prod.yaml up` cannot run in any automated job. What CI does run is `config`, which proves the overlay merges into a document that would start — the `${TLS_CERT_DIR:?}` guards fire, `ports: !override` replaces the dev mapping rather than appending to it, and the restated postgres `command:` survives the merge — and `server/tests/test_deploy_tls_posture.py`, which proves the files say what NFR-5 requires. Neither proves that nginx serves a valid chain, that Postgres actually accepts a TLS connection, or that `sslmode=verify-full` succeeds against the CA an operator supplied; a wrong `ssl_key_file` permission or a certificate whose CN does not match the deployment host fails at `up` and at first connect respectively, and nothing here would have caught either. The substitute is the two commands `deploy/.env.example` documents (`openssl s_client -connect <host>:443` and `SELECT ssl, version FROM pg_stat_ssl WHERE pid = pg_backend_pid()` from the api's own connection), which are part of the deployment checklist rather than optional. A CI job that generated a self-signed certificate into a scratch directory and booted the profile against it would close most of this, and belongs with Story 8.4's prod-profile completion.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-database-audit-log-hardening.md`
  summary: **The prod profile carries TLS but cannot be started at all** — `api/app.py` refuses to boot under `ENV=prod`, so the overlay deliberately does not set it.
  evidence: Story 1.3 made `ENV=prod` a deliberate boot refusal (`create_app` raises: "ENV=prod is refused while persona login is the authentication mechanism") because anybody who can reach the API can POST a persona id and receive a full session. `compose.prod.yaml` originally set `ENV: prod`, which meant the api crash-looped and `web` — which waits on `service_healthy` — never came up, so the whole profile was unbootable; `test_deploy_tls_posture.py` was asserting the presence of that line, i.e. the suite enforced the unbootable shape. The line is now removed, the file says at length why, and the test asserts its *absence* so nobody re-adds it as an obvious-looking omission. What this leaves open is a profile that is correct about transport encryption and silent about what environment it is: an api started from it runs dev-flagged (so `SESSION_COOKIE_SECURE` defaults off, and every other `ENV`-conditioned default is the dev one) behind a TLS ingress. **Story 8.4 (prod-profile completion) inherits both halves**: the IdP dependency that has to land before `ENV=prod` can ever be set, and the question of what the overlay should declare in the meantime — an `ENV` value between dev and prod, or explicit overrides of the handful of settings that key off it.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-database-audit-log-hardening.md`
  summary: **`log_min_error_statement=panic` also silences the statement text of a *failed* migration**, which is the one place an operator most needs it.
  evidence: A consequence of the entry above rather than a separate decision, but worth naming separately because the failure mode is different: `alembic upgrade head` against a database in an unexpected state raises with the driver's message and no statement, so a migration that fails in production leaves less in the database log than it did before this story. Alembic's own stdout still names the revision it was running, which is usually enough to locate the statement in the migration file — the residual is that the *bound values* (which for a data migration can be the interesting part) are unrecoverable by design. This is the intended trade; it is recorded so that nobody diagnoses it as a regression.

## Deferred from: code review of 8-2-database-audit-log-hardening (2026-08-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-database-audit-log-hardening.md`
  summary: **TLS to Postgres is *permitted*, not *enforced*** — `ssl=on` lets a client negotiate TLS and nothing refuses one that does not.
  evidence: The prod overlay sets `ssl=on` with a server certificate and key, and both API URLs carry `sslmode=verify-full`. What is missing is the server-side half: there is no `hostssl`-only `pg_hba.conf`, no `ssl_ca_file`, and no client-certificate requirement, so a connection that simply omits the parameter is accepted in plaintext. The enforcement therefore rests entirely on a string surviving in two environment variables — and `api/app.py:381` hands the copilot's psycopg pool `conninfo=settings.database_url` raw, so a URL that loses the parameter downgrades silently on the one connection that carries whole conversation transcripts. `.env.example` concedes the model ("an `f` means that connection fell back to plaintext") and then leaves detection to a manual command nobody is obliged to run. Closing it means shipping a `pg_hba.conf` whose only non-local line is `hostssl`, which is a decision about how the prod database authenticates rather than how it encrypts, and belongs with Story 8.4's prod-profile completion.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-database-audit-log-hardening.md`
  summary: **The prod profile now carries TLS and still cannot be started**, because `ENV=prod` is a boot refusal and the overlay therefore inherits `ENV: dev`.
  evidence: `api/app.py::create_app` raises unconditionally on `Env.prod` — Story 1.3's deliberate refusal while persona selection *is* the authentication mechanism. Review finding P1 established that setting `ENV: prod` in the overlay makes the api crash-loop and the whole stack with it, so the directive was removed and its absence is asserted. The consequence recorded here is the honest one: a profile named prod that renders `ENV: dev` is telling two stories, and every `settings.env`-conditional behaviour (cookie `Secure`, and anything a later story adds) resolves to the dev branch under it. This cannot be fixed by the compose file — it needs the Deferred IdP decision and the replacement of `_resolve_user`, after which `ENV: prod` goes back in and the assertion inverts again. Story 8.4 owns the prod compose and inherits both halves.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-database-audit-log-hardening.md`
  summary: **`log_truncate_on_rotation` does not apply on restart**, so the seven-day bound is weaker than the posture claims.
  evidence: The rotation recipe gives seven files, one per weekday, each truncated when its day comes round — but truncation happens on *rotation*, and a server restarted mid-week appends to the existing `postgresql-%a.log` rather than truncating it. A stack restarted often enough (an upgrade cycle, a crash loop, the e2e profile's own churn) accumulates entries older than the week the deferred-work entry above it claims. Closing it means either an explicit `pg_rotate_logfile()` on start, which is a startup hook this deployment shape does not have, or disk monitoring, which is the observability stack Story 8.4 documents as deferred.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-database-audit-log-hardening.md`
  summary: **`lineworker/postgres:pg18-pgaudit` is a locally built tag, so `docker compose pull` and any pull-only or air-gapped deployment now has no postgres image.**
  evidence: Both compose profiles previously named `pgvector/pgvector:pg18`, a registry-pullable tag; they now name a tag that exists only where `deploy/postgres` has been built. `docker compose up` builds it transparently because the service carries `build:`, so every path this repo documents keeps working — but a deployment that pulls rather than builds, or a host without the Dockerfile, gets an image-not-found at `up`. The fix is publishing the image to a registry, which is a decision about where this project's artifacts live and who signs them, and it has no owner yet.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-database-audit-log-hardening.md`
  summary: **The prod `web` healthcheck was replaced with an HTTPS loopback probe that no automated job can exercise.**
  evidence: The base check hits `http://127.0.0.1:80/`, which now answers 308, so wget would report the container unhealthy while it served correctly — the replacement was necessary. But it depends on BusyBox `wget` in `nginx:stable-alpine` speaking TLS (i.e. `/usr/bin/ssl_client` being present), and the prod profile is exactly the one CI renders and never boots. If the assumption is wrong, `web` never reports healthy and the stack is down while `docker compose config` looks perfect. Proving it needs a job that generates a self-signed certificate into a scratch directory and boots the profile against it, which would close most of the TLS residual as well and belongs with 8.4.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-database-audit-log-hardening.md`
  summary: **The blocked-value processor cannot see a foreign stdlib record's message body**, because `event` is a meta key.
  evidence: `_block_unallowlisted` blanks non-allowlisted *keyword* values and exempts `_META_KEYS`, which contains `event` — deliberately, since a blocked line must keep its subject or an operator cannot tell which call site leaked. For structlog calls that is exactly right, because the event name is a literal. For a record arriving from uvicorn, alembic or SQLAlchemy through `foreign_pre_chain`, the entire message *is* `event`, so a library that logged a statement or a parameter would pass the backstop untouched. Nothing in the tree does today, and the lint has no jurisdiction over library code, so the honest description is that the app-log ban covers this project's own calls and the pipeline's shape, not third-party log content. Closing it means scanning `_record.getMessage()` against a denylist, which is a different mechanism with a different false-positive profile.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-database-audit-log-hardening.md`
  summary: **`CREATE EXTENSION pgaudit` requires a superuser**, because pgaudit is not a trusted extension — so a least-privilege or managed deployment fails migration 0050 with an error whose docstring explains a different cause.
  evidence: The migration's docstring argues at length about `shared_preload_libraries`, which is the failure a developer meets locally. A cluster where the schema owner is not a superuser — a managed Postgres, or the least-privilege owner the Story 8.1 review already flagged `GRANT audit_redactor TO CURRENT_USER` as assuming away — fails instead with `permission denied to create extension "pgaudit"`, and the docstring sends the reader looking at their compose flags. Distinguishing the two in the migration means catching and re-raising with a message that names both causes; deciding what a non-superuser deployment should actually *do* is the larger question, and it is the same question 8.1's review left open about role membership.

## Deferred from: implementation of 8-3-encrypted-backups-restore-drill (2026-08-23)

### Residuals this story leaves

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **Nothing reads `status.json` except the container's own healthcheck**, so a failed nightly run is visible to `docker compose ps` and to nobody else.
  evidence: AC 3 is satisfied — a failed run, and a scheduler that quietly stopped, both turn the container `unhealthy` — but "surfaces non-silently" is bounded by who is looking. There is no monitoring integration, no alerting, no log shipping and no notification, all deferred by decision (the observability stack Story 8.4 documents), so the practical failure mode is a backup that has been failing for three weeks on a host nobody runs `ps` on. The status file was deliberately shaped as a machine-readable contract for exactly this reason — `result`, `stage`, `finished_at_epoch`, `artifacts[]` with names and byte counts, documented in `deploy/RESTORE-DRILL.md` — so whatever eventually watches it needs a reader and not a redesign. Closing it is one scrape target or one `docker events` consumer, and it needs the observability decision first.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **Procedure B (point-in-time recovery from base + WAL) is documented and has never been executed.** Only Procedure A is drilled.
  evidence: `deploy/RESTORE-DRILL.md` § 3 writes out the full PITR path — decrypt the base and every segment since it, `pg_verifybackup`, `restore_command` plus `recovery_target_time`, promote — and the pipeline really does produce the artifacts it needs (`base.tar.gz.age`, `base_manifest.age`, `wal/*.age`, all verified as real output of a real run). What has not happened is somebody replaying WAL onto a promoted base and checking the result, and the runbook says so in § 6 rather than implying otherwise. The gap matters because the two procedures buy different recovery points — 24 hours versus the unflushed tail of the current segment — so the number this deployment can actually promise is Procedure A's until B is rehearsed. Rehearsing it needs a WAL-enabled profile that stays up long enough to accumulate segments across two runs, which the e2e profile deliberately is not (`BACKUP_WAL=off`, and its schema is dropped before every spec file).

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **The SSH transport is never exercised anywhere** — dev, e2e, the pytest round trip and the drill all point `BACKUP_REMOTE` at a local directory.
  evidence: There is no remote host and no key in version control, and there will not be. What *is* exercised by CI is the `rsync` invocation itself, the per-run directory layout at the destination, and the remote prune, because the local form takes the same code path minus the transport. What is not exercised is `-e ssh -i <key>`, `BatchMode=yes`, `StrictHostKeyChecking=accept-new`, the remote prune's `ssh … 'cd … && ls | head'` (which additionally assumes GNU coreutils at the far end), and the read-only single-file key mount the prod overlay declares. All are asserted as configuration by `tests/test_backup_posture.py` and documented in `.env.example` and `deploy/RESTORE-DRILL.md § 6`, which states the limitation rather than implying the transport was proven. A first deployment must run `docker compose run --rm backup once` against the real destination once and look at the destination.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **The new hba records are `host`, not `hostssl`**, so this story did not close (and slightly widened the surface of) the standing "TLS to Postgres is permitted, not enforced" deferral.
  evidence: The Story 8.2 review already recorded that `ssl=on` permits TLS and nothing refuses a client that omits it, because there is no `hostssl`-only `pg_hba.conf`. This story is the first time this repository *ships* a `pg_hba.conf` — which is the file that deferral needs — and deliberately did not resolve it there: making the two network records `hostssl` would enforce TLS in prod and simultaneously break dev and e2e, which have no certificates at all, so it is a per-profile decision rather than a one-line edit. The widening is that there is now one more class of network connection (replication) reaching the database, and in prod it carries a byte-for-byte copy of the data directory over a link that is encrypted only because the client asked. Story 8.4's prod-profile completion owns the whole `pg_hba` question and now has the file to change.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **Re-running the purge after a restore is a manual step that nothing tracks, prompts or verifies** — bounded retention is the only mechanical mitigation.
  evidence: A backup taken before a Story 8.1 purge contains what the purge removed, and restoring it puts it back. Purge-aware backup filtering is refused by the story's own instruction and rightly so (it would be a second deletion path in a design whose whole premise is that there is one). What ships instead is `BACKUP_KEEP_RUNS`, which bounds how long purged data stays recoverable — a week at the default — plus `deploy/RESTORE-DRILL.md § 6`, which tells a restorer to re-run the purge for every subject purged since the restored run and gives the `audit_event` query that reconstructs the list. Two weaknesses remain: that query has to be run against the *pre-disaster* database, which in a real host loss is exactly the thing that is gone; and nothing anywhere fails, warns or records if the step is skipped. A purge ledger outside the database, or a purge-subject list shipped alongside each backup, would close it — and either is a design decision about where deletion requests are recorded, which belongs with the retention/IdP work rather than here.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **The backup container runs as root**, and it is the container holding a credential that can read every PHI store.
  evidence: Deliberate, argued in `deploy/backup/Dockerfile`: the status directory is a bind mount in the e2e profile and a named volume elsewhere, and a non-root uid matching neither the host user nor the volume's owner produces a job whose only failure mode is "permission denied" at 02:30 on somebody else's machine. The exposure is real but small — no published port, internal network only, one shell script on a timer — and it is the same posture the official Postgres image's own entrypoint starts from. Closing it means either a uid the compose files pin and the volumes are chowned to, or dropping privileges after the mounts are established, and both interact with the e2e bind mount that Playwright reads from the host.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **`lineworker/backup:pg18` is a second locally built tag with no registry behind it**, extending the deferral Story 8.2's review already recorded for `lineworker/postgres:pg18-pgaudit`.
  evidence: Same shape, same fix, one more instance: the service carries `build:`, so every path this repository documents keeps working, and a deployment that pulls rather than builds — or a host without the Dockerfile — gets an image-not-found at `up`. It is worth recording separately because the failure lands differently: a missing postgres image is a stack that does not start and is noticed immediately, while a missing backup image on a profile-gated service is a stack that starts perfectly and takes no backups. The prod overlay clears the profile gate precisely so that this is loud rather than quiet, but publishing both images to a registry is still the actual fix and still has no owner.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **Recovering from an invalidated replication slot deliberately creates a WAL gap**, and the gap is recorded as a count rather than as a range.
  evidence: `max_slot_wal_keep_size=2GB` means a slot whose consumer has been away too long is invalidated rather than allowed to pin WAL until the data volume fills — the right trade, since the alternative is a database that stops accepting writes. The cost is that the slot cannot then be resumed: `--create-slot --if-not-exists` sees a slot that exists, so the streaming connection keeps failing for ever. `entrypoint.sh` recovers by dropping and recreating the slot, but only at the ceiling of its exponential backoff (five minutes), so a transient blip resumes losslessly and only a real outage triggers a recreate. What is lost is every segment written between the invalidation and the recreate, and what `status.json` records is `wal_streaming: false` and a `wal_restarts` count — enough to know a gap exists, not enough to know which LSN range it covers. Recording the range means reading `pg_replication_slots.restart_lsn` before and after, which is a database read from a script that currently only ever writes files, and the number is only actionable if something is watching it, which is the observability deferral above.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **A missed nightly window is skipped, not caught up** — the scheduler fires only when `%H:%M` equals `BACKUP_AT`.
  evidence: `entrypoint.sh` wakes every sixty seconds and compares the clock to `BACKUP_AT`, recording the date *before* the run so a failure is not retried every minute for the rest of the night. Two consequences follow. A container that is down across its window (a host reboot, an image rebuild, a deploy at 02:30) does not back up that day and the next attempt is twenty-four hours later — surfaced by the healthcheck at `BACKUP_MAX_AGE_HOURS`, which is what the 26-hour default is sized for, but not repaired. And a run that fails is not retried at all until tomorrow, deliberately, because a tight retry against a database that is down is how a backup job becomes the outage. A catch-up rule ("if today's date is not recorded and it is past BACKUP_AT, run now") would close the first and is genuinely tempting; it was left out because it interacts badly with the second — the recorded-date-before-run guard is what stops a failing run from becoming a loop, and a catch-up rule has to distinguish "missed" from "failed" to avoid reintroducing it.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **The e2e gate can never assert that a backup decrypts** — by design, and the consequence is that "the identity works" is proved only in the pytest module.
  evidence: `deploy/compose.e2e.yaml` commits a real `age` recipient for which no identity exists anywhere, which is the correct arrangement (an identity in version control would make every backup encrypted to it readable by anybody with a checkout, and would teach the next story that committing one is a thing this repository does). So the e2e spec asserts what arrived off-host is `age` ciphertext carrying no `PGDMP` header, and stops there. The round trip — decrypt, `pg_restore`, compare row counts and one known claim — lives in `tests/test_backup_restore.py`, which mints a throwaway keypair per run. The residual is that the two halves are proved against two different databases by two different mechanisms, and neither on its own proves the shipped e2e pipeline produces a restorable artifact. Closing it means the e2e spec minting its own keypair at run time and passing the recipient into `compose run`, which is possible and was left as a larger change to the e2e fixture than this story needed.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **No automated test restores into a *different* cluster**, so the class of failure the executed drill found — a dump that restores perfectly while the cluster it lands in is missing the roles the grants name — is pinned by an artifact-content assertion rather than by a restore that would actually fail.
  evidence: `test_backup_restore.py`'s round trip creates a scratch *database* inside the same cluster as the source, where `lineworker_app` and `audit_redactor` already exist — so before `globals.sql.age` was added, that test passed against a backup whose restore produced an unopenable database, and the drill is what caught it. The replacement guard (`test_the_shipped_globals_artifact_carries_the_roles_the_dump_does_not`) decrypts the globals artifact and asserts both `CREATE ROLE` statements are in it, which pins the artifact but not the outcome: a future change to how grants are written could still leave a restore broken in a way only a foreign cluster would show. Closing it means starting a second, empty postgres container per test run and restoring into that — real, and a meaningfully slower and more docker-coupled suite than anything else in `server/tests` does today. The drill remains the thing that proves the outcome, which is the argument for running it on a schedule rather than once per story.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **The database password is in the argv of every `pg_dump`, `pg_basebackup` and the long-lived `pg_receivewal`**, so anything that can read `/proc` in the backup container — or run `docker top` on the host — can read it.
  evidence: Raised in review and real: `backup.sh` and `entrypoint.sh` both pass `--dbname="$DB_URL"`, and the URL carries the owner role's password. `--no-password` is already set, so the fix is mechanical in shape — split the URL and export `PGPASSWORD`, or write a `.pgpass` — but it means parsing a libpq URI in POSIX shell, in the one script where a parsing bug silently changes which database is backed up. The exposure it closes is narrow: the credential is readable only from inside a container that already holds an encrypted copy of every claim, and from a host session that can already read the volume. Left because the parsing is the risk and the story had no test that would catch getting it wrong.
  
- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **`wal_restarts` is cumulative for the life of the volume and never resets**, so `status.json` cannot distinguish a stream that is flapping now from one that flapped once months ago.
  evidence: `entrypoint.sh` persists the counter in the backup volume and increments it for ever; the deferred-work entry on WAL gaps treats that number as the signal a reader would use. A rate — restarts since the last successful run, or a timestamp of the last restart — is what a monitor actually wants, and adding one means deciding the window, which is the observability decision Story 8.4 documents as deferred. The live `wal_state` file is what the healthcheck reads, so the *current* state is not affected.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **The healthcheck learns that the WAL receiver died up to sixty seconds late**, because the scheduler loop is what writes `wal_state`.
  evidence: Verified by killing the receiver in a running container: `wal_state` stayed `streaming` for the remainder of the current loop iteration, and a backup run inside that window recorded `wal_streaming: true` for a stream that was already dead. The loop then detected it, incremented the restart counter and restarted the receiver — the recovery works, the reporting lags. Closing it means the healthcheck probing the process rather than reading a file, which makes the healthcheck a second place that knows how the receiver is started. A minute of lag on a condition whose consequence is measured in hours of RPO was judged the right trade; it is written down because "wal_streaming: true" is otherwise read as instantaneous.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **The WAL spool ceiling fires before the copy, so a database producing more than `BACKUP_MAX_SPOOL_SEGMENTS` of WAL between two runs loses segments that the night's copy would have shipped successfully.**
  evidence: The ceiling was deliberately moved ahead of the run during review, because after `ship_wal` it could never fire — a failed copy exits through the trap long before reaching it, and a failed copy is the whole scenario it exists for. The cost of that placement is the mirror case: 512 segments is 8 GB of WAL, so a busy instance generating more than that in twenty-four hours has its oldest segments dropped every night even though nothing is wrong with the destination, and `wal_dropped` reports it as a gap rather than as a capacity problem. Closing it properly means enforcing the bound *after* a successful copy and *before* a failed one — i.e. in two places with the same number, or in the trap — and deciding what a spool that is over the ceiling for a legitimate reason should do. This build's synthetic instance produces a handful of segments a day, so the case is unreachable here; a real deployment would size the knob from its own WAL rate, which is the number nobody has yet.

## Deferred from: code review of 8-4-complete-quality-operations-gate (2026-08-23)

- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-complete-quality-operations-gate.md`
  summary: **Branch protection cannot be applied, read or asserted from this repository, so "required check" remains a claim about a GitHub settings page.** `.github/required-checks.yml` and `tests/test_required_checks.py` make the *list* mechanical; nothing makes the *enforcement* mechanical.
  evidence: After this story `.github/` contains the workflow, one script (`scripts/check_model_ports.py`) and this story's `required-checks.yml` — and no ruleset file, no branch-protection config and no CODEOWNERS, because GitHub's branch protection lives in repository settings that no artifact in a working tree can reach. What the story could close, it closed: a job added to `ci.yaml` and not declared required now fails the build, and a declared check naming no job fails it too, so the manifest cannot silently fall behind the workflow. What is still open is one manual action by a repository admin — marking those five checks required on `main` — and the fact that nothing in CI would notice if somebody unmarked them tomorrow. Closing it means either GitHub's rulesets-as-code (a `.github/rulesets/*.json` synced by an action with admin scope, which is a token this project has no other use for) or an API probe in CI that reads the protection settings and compares them to the manifest — which needs the same token. `deploy/DEPLOYMENT.md` and the manifest header both state plainly that enforcement is a settings action, so the gap is documented rather than implied away.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-complete-quality-operations-gate.md`
  summary: **NFR-5's at-rest encryption requirement was not evidenced by the executed boot, and cannot be on a Docker Desktop host.**
  evidence: `deploy/DEPLOYMENT.md § 2` asks for `findmnt`/`lsblk`/`cryptsetup` output proving the Docker data root sits on a mapped LUKS device. None of those commands exists on macOS, and `docker info` reports `/var/lib/docker` — a path inside Docker Desktop's own VM rather than on the host — so even a Linux-shaped check would be interrogating the wrong filesystem. `fdesetup status` reported FileVault on, which encrypts the host disk holding the VM image and says nothing about the guest layout. The section now names a Linux host with a native engine as the supported production target and says a Docker Desktop boot is a rehearsal; the checklist in `§ Executed boot` carries this as an explicit unticked box rather than a silent omission. Closing it needs a Linux host, which is the same thing the GPU deferral below needs.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-complete-quality-operations-gate.md`
  summary: **The GPU overlay has still never been booted anywhere**, so `compose.gpu.yaml` is proven only to render.
  evidence: The boot host is arm64 darwin with no NVIDIA runtime, and `compose.gpu.yaml`'s own header records that a host without the Container Toolkit fails at container *create* — there is no CPU fallback path through it. The boot therefore ran `compose.yaml + compose.prod.yaml` and used the documented `qwen3:8b` CPU pairing, and `§ Executed boot` records the deviation. What is unproven is narrow but real: that `deploy.resources.reservations.devices` is accepted by a real engine, that `count: all` claims the GPU as intended, and that `qwen3:14b` loads in 24 GB as the spine's sizing says. `.github/scripts/check_model_ports.py` renders the GPU profile on every PR, so the AD-5 posture is covered; the hardware is not. Reopens when a GPU host exists for this project — the same trigger as the observability deferral's "when operations owns it".

- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-complete-quality-operations-gate.md`
  summary: **`ENV: prod` is still refused and the prod overlay still renders `ENV: dev`** — now with its two `env`-keyed defaults overridden explicitly rather than inherited from a value that is not true.
  evidence: `api/app.py:437` raises on `Env.prod` while persona login is the authentication mechanism, and `test_deploy_tls_posture.py` asserts the overlay does not set it, so the unbootable shape cannot be reintroduced by somebody filling in an obvious-looking omission. Story 8.2's review handed 8.4 both halves of this; the half that could be closed was closed — `config.py` was re-read rather than trusted, both properties that key off `settings.env` (`cookie_secure` and `scheduler_runs`) are now set explicitly in the overlay, and the posture test derives that list from `config.py` by pattern so a third one added later fails until prod restates it. The half that remains is the Deferred IdP decision itself: until `_resolve_user` is replaced, a profile named prod reports `ENV: dev` to `/api/healthz`, to `log.info("app.start", env=…)` and to anything a later story keys off it. `deploy/DEPLOYMENT.md § 8` lists it first with `api/app.py:437` as the pointer.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-complete-quality-operations-gate.md`
  summary: **The off-host SSH transport has still never been exercised anywhere**, and the executed boot did not change that.
  evidence: Story 8.3 recorded this and it survives verbatim: dev, e2e, the pytest round trip, the restore drill and now the prod boot all point `BACKUP_REMOTE` at a local directory, which exercises the same `rsync` invocation without the transport, the host-key pin, the key permissions or the remote prune's `ssh` command string. The boot did add one thing 8.3 could not: the backup job's replication connection now runs over the `hostssl`-enforced link with `sslmode=verify-full`, verified in `pg_stat_ssl` as `pg_receivewal … TLSv1.3`. The transport itself needs a second host, which this project does not have.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-complete-quality-operations-gate.md`
  summary: **`docs/Architecture-LINEWORKER.md § 8`'s CI bullet is the older, lighter statement of the gate** — "lint + typecheck + tests on every merge; migrations must apply clean" — with no mention of Playwright, story specs, `@smoke`, the merge-to-main full suite or the bijection lint.
  evidence: The spine's `Consistency Conventions — Testing & CI` row and AD-15 are the checklist of record and are what this story implemented; § 8 predates AD-15's addition and was not updated with it. Nothing is contradictory — § 8's list is a subset — but a reader who takes the rendered architecture as the CI contract will under-specify the gate, which is exactly the failure mode Story 8.4 exists to close for operations. Not fixed here because editing a planning artifact from an implementation story is how the two drift in the other direction; it belongs to an architecture update or a retrospective action item.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-complete-quality-operations-gate.md`
  summary: **`ci.yaml` has no `concurrency:` group**, so two pushes to the same PR run two full e2e jobs to completion.
  evidence: Noticed during the gate audit and deliberately not fixed: it costs runner minutes rather than correctness (each job builds its own compose stack on its own runner, so there is no shared state to race), and adding `cancel-in-progress` changes which runs are recorded against a commit, which is a decision about how the required checks read on a re-push rather than a gap in the gate. Worth doing; not this story's to decide unattended.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-complete-quality-operations-gate.md`
  summary: **The bijection lint checks the tracker against `e2e/stories/` and nothing checks either against the epics file**, so a story that exists in `epics.md` and was never added to `sprint-status.yaml` is invisible to the whole gate.
  evidence: AD-15 specifies the bijection between non-backlog sprint-status keys and spec files, and that is what shipped — tightened during implementation to full-key filename equality, so a renamed story now has to rename its spec in the same change, which is the property AD-15 gives as the reason for the lint. The residual is one level up: `sprint-status.yaml` is generated from `epics.md` by sprint planning and nothing re-checks the two afterwards, so a story added to an epic post-planning has no key, therefore no spec, therefore no finding. Closing it means a third input and a decision about which document is authoritative when they disagree — which is a sprint-planning question rather than a CI one.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-complete-quality-operations-gate.md`
  summary: **`ollama`'s 30-minute `start_period` is a wall rather than a budget**, so a model pull on a slow link flips the container unhealthy mid-download and it never recovers within that boot.
  evidence: Raised by review against this story's healthcheck change, but the `start_period` itself predates it (Story 6.1) and the change made the consequence *smaller*, not larger: nothing waits on `ollama` any more, so an unhealthy model server now costs the AI routes rather than the whole console, and § 4 of the deployment runbook was corrected in this pass to stop telling operators to `up -d --wait` — the one command that would still turn it into a boot failure. What remains is cosmetic-but-misleading: on a link slower than roughly 3 MB/s the 6.4 GB of models exceed the window, `docker compose ps` shows `unhealthy` for a container that is downloading normally, and only a restart resets the clock. Closing it means a healthcheck that distinguishes "pulling, making progress" from "stuck" — i.e. reading `ollama ps` or the pull's own progress — which is a second place that knows how the entrypoint starts models, and a bigger change than the symptom warrants.
  

---

## Deferred from: implementation of 9-8-paging-correctness-real-pagination (2026-08-25)

- source_spec: `_bmad-output/implementation-artifacts/spec-9-8-paging-correctness-real-pagination.md`
  summary: **Keyset resumption fixes a row *leaving* the population; a row whose *order key* changes mid-walk is still silently skipped or repeated**, on all three ranked lists (`services/worklist/priority_claims.py`, `drill_through.py`, `queue.py`).
  evidence: Real, and it is the residual of this story's own fix rather than a defect it introduced. The cursor records `priority.order_key` — `(-score, claim_id)` — for the last row served, and the next page is a `bisect_right` strictly after that key. That is exactly right when a claim *leaves* the list, which is the defect the offsets had. It is wrong when the key itself moves. Concrete case: a supervisor reads worklist page one; a handler raises claim X's `severity_score` between the two requests; X was ranked below the cursor's key and is now above it; page two resumes after the old key, X is never rendered, and the walk ends showing 37 rows beside a published `total` of 38 — a 200, with nothing on screen to say a row went missing. The mirror case repeats a row instead, when an edit moves a claim from above the key to below it. Both are narrower than what they replace (an offset lost a row on *any* departure, which happens without anybody editing anything), which is why the change is still the right one and this is a residual rather than a regression. Closing it means pinning a population-and-rank digest on every one of the three lists and refusing the page when it moves — and Story 9.8's own reserve-verdict pin is direct evidence of how easily such a digest is made over-broad: it shipped hashing the whole filtered, ranked list, so a stage edit or a reassignment tripped a 400 reading "The reserve verdicts behind that page have been recomputed", which was false in every one of those cases; it needed a second digest over the pre-facet population before the refusal meant what it said. A digest over the *ranking* is that same trap one size larger — every score edit anywhere in a caller's book invalidates every open cursor — so the choice between a walk that can skip a row and a walk a busy book can invalidate on every page is a product decision about which failure a supervisor should get, not a dev-time one.

---

## Accepted limits

Entries recording a **documented limit with no work attached**: a deliberate trade-off, a port of prototype behaviour, a cost that is real and correct at this size, a fact about the seeded dataset, or an argument for why an alternative was rejected. Nobody would open a ticket for one. They are here so that the sections above read as open work and this one does not — which is the whole of the exit criterion at the head of this file.

**Moved, not rewritten.** Every entry below is byte-identical to the one that stood under its original heading, which is named above each group. The only addition is an `accepted:` line stating what was accepted and why, so a reader can decide in one sentence whether the argument beneath it still holds. Nothing was deleted and no recorded reasoning was edited.

Partitioned by Story 9.8, the first Epic 9 story to land — the one piece of the 2026-08-24 restructure the triage table recorded as outstanding.

### From: Deferred from: code review of 1-6-domain-glossary (2026-08-10)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-domain-glossary.md`
  summary: `data/seed/extract_prototype_seed.py` documents and defaults to `../docs/Workers_Comp_Prototype.html`, which from `server/` resolves to `lineworker/docs/…` — a path that does not exist; the real one is `../../docs/…`.
  evidence: Verified by running it: the documented command fails with `FileNotFoundError`. Predates this story (Story 1.2 shipped it) and belongs to the portfolio seed, not the glossary. Story 1.6's own extractor had the same bug copied into it and fixed there; this one is left alone because re-running it rewrites `seed_data.json`, which is Story 1.2's committed artifact and outside this story's blast radius.
  accepted: The broken default path in the portfolio seed extractor is accepted because re-running the script would rewrite seed_data.json, Story 1.2's committed artifact, which sits outside the story that found it.

### From: Deferred from: code review of 2-1-prioritized-filterable-claim-queue (2026-08-11)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-prioritized-filterable-claim-queue.md`
  summary: The queue's DB-backed tests build the app on the migration *owner* connection, so the `SELECT`-only grant on `rule_document` is asserted by reading `information_schema` and never by serving a request as `lineworker_app`.
  evidence: Real, and pre-existing rather than introduced here — `tests/test_glossary.py` established the pattern in Story 1.6 and `conftest.py` exposes only `seeded_db_url` (the owner URL). The concrete hazard is that a missing or wrong grant on a new table would 500 every request in the deployed stack while the suite stayed green; only the e2e stack, which does run as the app role, would catch it. The fix is a shared app-role client fixture in `conftest.py`, which belongs with the fixture rather than with any one story.
  accepted: Asserting the app role's grants through information_schema rather than by serving a request is accepted because only the e2e stack runs as lineworker_app, and the shared app-role fixture that would close it belongs to conftest.py rather than to any one story.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-prioritized-filterable-claim-queue.md`
  summary: A missing, uncompilable or invalid rule document surfaces as an unhandled 500 rather than an RFC 9457 problem document, and it takes `/stats/topbar` down with it.
  evidence: Real: `rules.engine.load` raises when no document is effective on the date, and neither `/claims/queue` nor `/stats/topbar` catches it. Deferred because a 500 is a defensible answer to a broken deployment — the documents are seeded by migration, so their absence means the migration chain did not run — and because choosing the right shape (503 + `Retry-After`? a degraded queue?) is a service-availability policy decision that spans every future JDM consumer, not just this endpoint.
  accepted: A 500 for a missing or uncompilable rule document is accepted as a defensible answer to a broken deployment, since the documents are seeded by migration and choosing a softer shape is a service-availability policy spanning every future JDM consumer.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-prioritized-filterable-claim-queue.md`
  summary: The seed migration inserts the committed JDM documents without compiling them, so a syntactically valid but uncompilable document passes the migration and fails at request time instead.
  evidence: Real but narrow: `tests/test_rules_engine.py` evaluates both committed documents, and CI runs it, so a bad document cannot reach `main` — the exposure is a hand-edited document on a developer's branch. Compiling inside `upgrade()` would also make the migration chain depend on the ZEN runtime, which is a heavier coupling than the failure warrants.
  accepted: Seeding rule documents uncompiled is accepted because test_rules_engine.py evaluates both committed documents in CI, and compiling inside upgrade() would make the migration chain depend on the ZEN runtime.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-prioritized-filterable-claim-queue.md`
  summary: `utc_today` is defined in `rules/engine.py` and re-exported through `services/derivations`, so `services/worklist/queue.py` imports its clock from the derivations package while importing its parameters from `rules.parameters` — two doors to one value.
  evidence: Real, cosmetic today, and a genuine trap later: the moment a second clock source appears (a request-scoped "as of" for back-dated reporting, say), the two import paths make it easy to have half the derivations on one and half on the other. It belongs in a small `services/clock` or on the caller context, which is a cross-cutting placement decision.
  accepted: The two doors to utc_today are accepted as cosmetic today, because moving the clock into services/clock or onto the caller context is a cross-cutting placement decision that only pays off once a second clock source exists.

- source_spec: `_bmad-output/implementation-artifacts/2-1-prioritized-filterable-claim-queue.md`
  summary: A compiled `ZenEngine` decision object is held in a module-level `lru_cache` and shared across every concurrent request, and the `context` argument threaded through the evaluation cache key is exercised by no test.
  evidence: Both real, both low-consequence today. Nothing in the change establishes that the pyo3 decision object is safe to evaluate concurrently; the evaluation is also synchronous on the event loop. The practical exposure is small because the evaluated result is itself cached, so a given `(key, version, content, context)` is evaluated once per process — but that is a property of the cache, not of the object, and a future decision-table document that varies by claim would evaluate per row and make both facts matter at once. The context plumbing exists solely for that future document; until one exists, the cache-key design its docstring defends is unverified.
  accepted: Sharing one lru_cached ZenEngine decision across requests is accepted because the evaluated result is itself cached, so a given key is evaluated once per process until a document varies by claim.

### From: Deferred from: implementation of 2-2-case-header-stage-adaptive-overview (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-2-case-header-stage-adaptive-overview.md`
  summary: 101 seeded documents carry `Post-surgery` or `Closed` where a filing date belongs, and 62 timeline events carry `Closed`; all of them seed `filed_date` / `event_date` as NULL, so the timing note is not on the wire.
  evidence: Deliberate — a `DATE` column should not hold a non-date, and NFR-3 says render the absence rather than a placeholder that looks like data. Nothing is lost: the raw strings are still in `seed_data.json`'s `deferred` block, so Story 2.5 (whose Documents tab renders "name, type, filed date") can add a `filed_note` column and re-seed if a product owner wants "filed post-surgery" back. Flagged because 2.5 will be the first surface where the em dash is visible on a fifth of the rows.
  accepted: Seeding filed_date/event_date as NULL where the dataset holds 'Post-surgery' or 'Closed' is accepted because a DATE column must not hold a non-date and NFR-3 asks for the absence to render; the raw strings survive in seed_data.json's deferred block.

- source_spec: `_bmad-output/implementation-artifacts/2-2-case-header-stage-adaptive-overview.md`
  summary: The treatment overview's recent-6 timeline slice never fires against seeded data — the longest treatment timeline in the portfolio is exactly six events — so it is covered by unit tests over a synthetic twelve-event claim rather than by the e2e spec.
  evidence: A property of the dataset, not of the rule: a claim still in treatment has not accumulated the RTW and settlement entries that make settled timelines seven. The prototype's own `.slice(-6)` never fired either. `tests/test_claim_detail_assembly.py` exercises the rule over its whole domain and `tests/test_claim_detail.py` asserts the seeded claims are correctly reported as *not* truncated. Worth revisiting when Story 2.3 starts appending edit events, which is the first thing that will push a live treatment claim past six.
  accepted: Covering the recent-6 timeline slice by unit tests over a synthetic twelve-event claim is accepted because no seeded treatment timeline exceeds six events, which is a property of the dataset rather than of the rule.

### From: Deferred from: code review of 2-2-case-header-stage-adaptive-overview (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-2-case-header-stage-adaptive-overview.md`
  summary: The settled payout breakdown formats indemnity, medical, expense and the total independently at whole-dollar precision, so the three components need not add up to the total shown beside them.
  evidence: Real but dormant: `formatCents` drops cents (`maximumFractionDigits: 0`) and no seeded claim has a non-zero cents component, so every breakdown currently reconciles. It is the same argument `claim_money.py` makes about the percentage bar — round the parts independently and they stop summing to the whole — applied to money rather than shares. Becomes live with Epic 3's bill lines, which is also where a cents-precision formatter belongs; `lib/money.ts` already anticipates one.
  accepted: Independent whole-dollar formatting of the four payout figures is accepted while it is dormant, since no seeded claim carries a non-zero cents component and the cents-precision formatter belongs with Epic 3's bill lines.

### From: Deferred from: implementation of 2-3-audited-inline-field-editing (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-3-audited-inline-field-editing.md`
  summary: `icd_desc` is accepted by the command and validated like the rest, but no surface shows or edits it, so the whitelist has one member the UI never exercises.
  evidence: The story's task list names it explicitly (`icd` plus `icd_desc` if supplied) and the prototype stores both, but the prototype's injury card edits only the code. Left on the whitelist rather than dropped because an ICD-10 code and its description are one fact in two columns, and a command that could change the code while leaving the description describing the *old* diagnosis is the more dangerous shape. Covered by unit tests; the surface that edits it is whichever later story shows it (2.5's document viewer and Epic 6's copilot are both candidates).
  accepted: Keeping icd_desc on the edit whitelist with no UI is accepted because an ICD-10 code and its description are one fact in two columns, and a command that could change the code while stranding the description is the more dangerous shape; unit tests cover it.

### From: Deferred from: code review of 2-3-audited-inline-field-editing (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-3-audited-inline-field-editing.md`
  summary: `timeline_event` is asserted append-only in code, and documented as such at length, while the application DB role holds `UPDATE` and `DELETE` on it.
  evidence: Real. Migration 0003 grants `SELECT, INSERT` on `audit_event` — AD-4's "INSERT-only" enforced by the database. Migration 0010 (Story 2.2) grants `SELECT, INSERT, UPDATE, DELETE ON timeline_event, document TO lineworker_app`, presumably because `document` is a mutable entity and the two were granted together. Story 2.3 is where the append-only claim is made in prose (`services/claims/timeline.py`: "No `version`, no update path, no delete") and where the first runtime writer lands, so the gap is visible now — but the invariant is currently enforced by code convention rather than by the grant surface, which is the thing AD-4 says should not be true of an append-only store. Deferred because the fix is a new migration narrowing a grant that Story 2.2 issued, and splitting the two tables' grants is 2.2's decision to revisit alongside the `document` write path Story 2.5 will add.
  accepted: Leaving lineworker_app's UPDATE/DELETE on timeline_event is accepted for now because narrowing a grant Story 2.2 issued means a new migration that splits it from document's, which is 2.2's decision to revisit rather than 2.3's to make.

### From: Deferred from: implementation of 2-4-interactive-injury-diagram (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-4-interactive-injury-diagram.md`
  summary: `additional_injury.body_key` is a native `body_region` enum while `claim.body_key` is still `Text`, so one vocabulary has two storage forms.
  evidence: Deliberate and scoped: the story's task list puts the enum on the table it creates, and converting `claim.body_key` is a migration over a column Stories 1.2 and 2.3 own — with a ripple through the edit command's diff machinery, the case-header contract and the generated client. The vocabulary itself is single-sourced either way (`BodyRegion` in `data/models/enums.py`, labels in `services/claims/reference.py`, and `services/claims/edit.py` validates the claim's key against the same eleven members), so what differs is only where the refusal comes from — a 422 for the claim, a database error for the injury. The conversion is a small migration on 0013's pattern and should happen the next time `claim` is altered for another reason.
  accepted: One vocabulary in two storage forms is accepted because the members and labels are single-sourced either way, so only the refusal differs (422 versus a database error), and converting claim.body_key is a migration over a column two other stories own.

- source_spec: `_bmad-output/implementation-artifacts/2-4-interactive-injury-diagram.md`
  summary: `treatment_plan_step` is created, seeded and read, and nothing writes it at runtime — the plan a handler sees is the dataset's and cannot be amended.
  evidence: The story's task list is explicit that this table is reference content for Epic 2 ("effectively static reference rows this epic — no runtime writer until a later epic needs one"), which is why the table has no `version` column: there is no concurrent writer to arbitrate. Recorded because a treatment plan is the kind of thing a handler *would* amend, and the story that adds the command adds the column with it — the same order `Document` and `GlossaryTerm` establish.
  accepted: A treatment plan that cannot be amended is accepted for Epic 2 because the story scopes the table as static reference content, which is why it carries no version column — the story that adds the command adds the column with it.

### From: Deferred from: implementation of 2-6-incident-photos (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-6-incident-photos.md`
  summary: `photo` is a read-only table with no write command anywhere in the epics, so a handler cannot attach, caption, correct or remove a photograph — including one attached to the wrong claim.
  evidence: The story is explicit ("there is no photo upload, capture, annotation, or deletion anywhere in the epics — do not add write commands"), and migration 0020 enforces it with a `SELECT`-only grant rather than leaving it to component discipline. Recorded because the absence is more consequential for photos than for the other read-only tables this epic created: `treatment_plan_step` and `path_required_form` hold *reference* content that a handler has no standing to change, while incident photographs are evidence a plant safety officer produces continuously after the incident. The table deliberately carries no `version` column for the same reason `TreatmentPlanStep` does not — the story that adds the command adds the column with it, and it will need the grant widened in the same migration.
  accepted: The absence of any photo write command is accepted because the story forbids one in as many words and migration 0020 enforces it with a SELECT-only grant; the version column and the widened grant arrive with the story that adds ingest.

### From: Deferred from: implementation of 3-1-statutory-benefit-calculation (2026-08-14)

- source_spec: `_bmad-output/implementation-artifacts/3-1-statutory-benefit-calculation.md`
  summary: A jurisdiction with no rate schedule takes the **whole case file** down with a 500, which is intended — and it is the first worked answer to the shape question the 2.1 review left open about a missing rule document.
  evidence: The story is explicit that a missing state is a data error surfaced as problem+json rather than a default, and blanking one card while serving the rest would be the prototype's silent answer in a different shape. What is recorded here is the connection: item 2 of the 2.1 code-review deferrals asks what a broken rules deployment *should* answer (503 with `Retry-After`? a degraded payload?) and defers it as a service-availability policy decision. This story answers the same question for its own data with a 500 carrying `/problems/missing-state-rate` and a `detail` that names no claim data (AD-11), with the state code in the structured log. If the wider policy lands on something else, this is one of the two places that has to move.
  accepted: Taking the case file down with a problem+json 500 on a missing state schedule is accepted as intended: a missing state is a data error rather than a default, and blanking one card would be the prototype's silent answer in a different shape.

### From: Deferred from: implementation of 3-2-reserve-adequacy-check (2026-08-14)

- source_spec: `_bmad-output/implementation-artifacts/3-2-reserve-adequacy-check.md`
  summary: **The medical half of projected exposure is nil until Story 3.3 seeds `bill`**, so every verdict the console shows today is the indemnity projection judged against the reserve — and the portfolio bands 26 heavy to 7 light because of it.
  evidence: The story's own Dev Notes call this seam deliberate ("if 3.2 is executed before 3.3 it legally returns 0"), and 3.2 creates no tables by design. What is recorded here is the *consequence*, which the story does not state: with one of two exposure terms missing, the whole open portfolio is judged on a systematically low number, so the verdict distribution is skewed toward `heavy` and a genuinely under-reserved claim with large unpaid bills can currently read `adequate`. It is a demo-visible wrong answer rather than a missing feature, and it resolves the moment 3.3's seed lands — `services/financials/reserve.py::unpaid_medical_cents` is the one function body to change, and `tests/test_reserve_block.py::test_the_medical_term_is_zero_until_story_3_3_seeds_the_bills` fails when it does, which is the reminder rather than a surprise.

  **Resolved on 2026-08-14 by withholding the verdicts the missing term can flip**, so this item is now about the residual rather than the defect. The reasoning was that the unknown medical amount is non-negative, so the exposure computed without it is a *lower bound*: `light` is still sound (an unknown addition cannot pull an exposure back under a threshold it has already passed) while `adequate` and `heavy` are claims about an upper bound and are withheld as `indeterminate`. Kaya's book now reads 4 `light`, 15 `indeterminate`, 26 `closed_final`, and the phrase "reallocating surplus" is unreachable while a term is missing. What remains deferred is the obvious: **the console cannot give a complete reserve verdict on any open claim until Story 3.3 seeds `bill`**, and 15 of 19 open claims in that book show "Awaiting Bill Data" instead of a judgement. That is the honest state rather than a wrong answer, but it is a large share of the feature's value sitting behind the next story, and if 3.3 slips the right response is to seed `bill` rather than to loosen the withholding.
  accepted: Withholding the verdicts the missing medical term can flip — leaving 15 of 19 open claims reading 'Awaiting Bill Data' — is accepted as the honest state rather than a wrong answer, since the exposure computed without that term is a sound lower bound.

- source_spec: `_bmad-output/implementation-artifacts/3-2-reserve-adequacy-check.md`
  summary: **`ScheduleWeekStatus` lives in `data/models/enums.py` with no column holding one until Story 3.3 creates `payment_schedule_week`.**
  evidence: Deliberate and narrow. A native enum column needs its members in the data layer (`data/` must not import from `services/`), and the projection that *produces* the value landed a story before the table that stores it — so the vocabulary was put where 3.3 will need it rather than in `services/financials` to be renamed out on its way past. The cost is an enum in the data layer that nothing persists, which reads as a loose end until 3.3 lands; `BodyRegion`'s docstring makes the same argument for the same reason. `payment_scheduled` is the one member nothing produces yet at all — it is Story 3.4's approval state, named now so the four statuses a week can hold are declared together.
  accepted: An enum in data/models/enums.py that nothing yet persists is accepted because a native enum column needs its members in the data layer and the projection landed a story before the table, so the vocabulary was placed where 3.3 would need it.

### From: Deferred from: implementation of 3-3-bills-indemnity-payment-schedule (2026-08-14)

- source_spec: `_bmad-output/implementation-artifacts/3-3-bills-indemnity-payment-schedule.md`
  summary: **The readiness review's stated reason for creating `expense` in this story does not hold**, though creating it here is still right.
  evidence: The review moved `expense` into 3.3 because it "feeds the settled-stage payout breakdown rendered by Story 2.2", and this story's Dev Notes ask for that surface to be verified after seeding rather than assumed. Verified, and it was never empty: `SettledOverview.tsx` renders `overview.paidExpenseCents`, which is `claim.paid_expense`, and all 62 seeded settled claims carry a non-zero one from the Story 1.2 seed. What the table actually feeds is the Bills tab's own Expenses card (AC 3) and the effective expense figure on open claims. Recorded rather than silently corrected because a later story reading the review's sentence would go looking for a dependency that is not there; the docstrings on `Expense` and on the extractor now say so.
  accepted: Recording that expense does not in fact feed the settled payout breakdown is accepted as a note rather than a change, because creating the table in 3.3 is still right and the docstrings now say what it actually feeds.

### From: Deferred from: implementation of 3-5-auto-generated-action-checklist-approval (2026-08-17)

- source_spec: `_bmad-output/implementation-artifacts/3-5-auto-generated-action-checklist-approval.md`
  summary: **AC 2 says "JDM decision table" and `worklist_actions` ships as an `expressionNode`**, like the nine documents before it.
  evidence: Deliberate, and the spec's Design Notes reach the same conclusion independently. All nine committed JDM documents use `inputNode → expressionNode → outputNode` with constant expressions, and `rules/engine.py` evaluates a document **once per request** with an empty context, memoised on `(key, version, content, context)`. A `decisionTableNode` keyed on a per-rule or per-claim input would be evaluated eleven times per request, thrash that cache, and have no home in the frozen-parameter-block pattern `rules/parameters.py` is built around — the pattern that lets `tests/test_action_checklist.py` exercise all eleven rules with no engine started and no database opened. What AD-8 actually requires — parameters DB-versioned, effective-dated, out of Python, one rule element per tier — is satisfied exactly: the cap, the padding floor and eleven urgencies are thirteen keys in a versioned row, and `test_a_superseded_rule_document_changes_the_list_with_no_code_change` demonstrates a v2 with `cap: 1` changing the rendered list end to end. What is *not* satisfied is the node type, and the reason a real decision table might be wanted later is a per-rule row (rule → urgency → enabled) an operator could edit as a grid rather than as thirteen scalars. That is a change to `rules/engine.py`'s evaluation-and-caching contract, not to this document.
  accepted: Shipping the eleventh rule document as an expressionNode is accepted because AD-8's actual requirements are met by the versioned parameter block, and a decisionTableNode would be evaluated per rule per request against an engine cache built for the opposite shape.

- source_spec: `_bmad-output/implementation-artifacts/3-5-auto-generated-action-checklist-approval.md`
  summary: **The payment-confirmation action deep-links to the Bills tab rather than carrying its own ✓**, so Story 3.4's approval is reached in two clicks instead of one.
  evidence: Task 4 says "payment confirmation → 3.4's approval commands", and what ships satisfies the routing (the row's `bills` target lands on the tab where 3.4 put the button) without duplicating the control. Carrying it on the checklist row would mean the card holding a *week number or a line-item id and its version* — a second identity vocabulary on a payload whose other completions are claim- and document-shaped — and a second place the approvable-status rule is rendered, which is the disagreement `week_is_approvable` was published to prevent. The honest fix is either an `approvePayment` member on `ActionCommand` with the row carrying `paymentKind`/`targetId`/`version`, or a decision that the checklist links and never approves money. It wants Story 5.4's worklist to weigh in, because that surface will want the same thing.
  accepted: Routing the payment-confirmation action to the Bills tab rather than duplicating the approval control is accepted because carrying it would put a week or line-item identity on a claim-shaped payload and render the approvable-status rule in a second place.

### From: Deferred from: implementation of 4-1-meeting-scheduling-management (2026-08-17)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-meeting-scheduling-management.md`
  summary: **`meeting` is PHI-class and is not in any purge cascade, because there is no purge cascade.**
  evidence: The epic's technical decisions say the three new tables "must be reachable by the single purge cascade owned by `services/audit`" and that a story should "add them to that cascade's coverage rather than inventing per-table deletion". That cascade does not exist yet — Story 8.1 owns it — so nothing was added and nothing was invented. What this story did do is leave the cascade the handles it will need: `meeting.claim_id` is a real FK with the default `ON DELETE` (so an orphan is impossible rather than silently disappearing), every audit diff carries the claim's `WC-nnnn` business id rather than only the meeting's surrogate (`_diff`'s reason, which is `injuries.py`'s), and the app role holds `DELETE` on the table. Recorded so 8.1 has a list rather than a search.
  accepted: Leaving meeting out of a purge cascade is accepted for this story because the cascade itself was Story 8.1's to build, and 4.1 left it the FK, the business id in every audit diff and the DELETE grant it would need.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-meeting-scheduling-management.md`
  summary: **`SEAM_REASONS` still carries `diary`, `fraud` and `rtw_letter`; `meetings` is gone** — and the diary sentence was re-worded, which changed a server test and a web fixture.
  evidence: Enabling the meetings target was one deletion from `services/worklist/actions.py` and nothing in the SPA, which is the property `ActionTarget`'s docstring promises and is now asserted directly (`test_the_meetings_target_is_live_since_story_4_1`). The re-wording was forced: `diary` and `meetings` shared "Available with Diary & Meetings — Epic 4", and with the scheduler shipped that sentence describes work half of which is on screen. `diary` now reads "Available with diary notes — Story 4.2"; `tests/test_action_checklist.py`, `web/src/test/api-mock.ts` and `ActionsCard.test.tsx` were updated rather than worked around. Story 4.2 deletes the entry; Story 6.2 deletes `fraud`; `rtw_letter` waits for FR-H-11.
  accepted: The re-wording of the diary seam sentence is accepted as a recorded consequence rather than a defect, since enabling the meetings target was one deletion and the affected tests and fixtures were updated rather than worked around.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-meeting-scheduling-management.md`
  summary: **The 400 for a bad meetings cursor answers `/problems/invalid-cursor`, not the spec's `/problems/bad-cursor`.**
  evidence: A deliberate divergence, one word wide. `api/routers/claims.py`'s queue has answered `/problems/invalid-cursor` since Story 2.1 and its reusable `BAD_CURSOR_RESPONSE` dict (which the diary router imports) documents that type; two `type` values for one failure across two list endpoints would make the SPA's error handling depend on which list it was reading. The substance the matrix is about — a bad or expired cursor is a 400 and never a silent page-one fallback — is implemented and tested (`test_a_cursor_that_does_not_describe_this_list_is_refused` over six forged shapes, plus the endpoint test). If the wording is wanted, it is one string in two routers and a rename of the shared dict.
  accepted: The one-word divergence from the spec's problem type is accepted because the queue has answered /problems/invalid-cursor since Story 2.1 and two type values for one failure would make the SPA's error handling depend on which list it was reading.

### From: Deferred from: implementation of 4-2-claim-linked-diary-notes (2026-08-17)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-claim-linked-diary-notes.md`
  summary: **The diary check-in row gains no completion control — the note *is* the completion — which answers the question the 3.5 code review left open.**
  evidence: The 3.5 review recorded that "Story 4.2 enables the diary target, and whoever does it should decide then whether the row gains a completion control or stays a link". It stays a link. `ActionCommand` has four members, each naming a specific entity write, and `services/worklist/actions.py` states the rule in its own docstring: *"There is no 'completed actions' store, deliberately — a second place a completion is written down is the first place the two can disagree."* A fifth command whose only job is to record "I said I did it" is exactly that second place, and spec-3-5's Block If says HALT rather than add one. So `_diary_check_in` tightened to "in treatment **and** no note on this claim within `DIARY_CHECK_IN_DAYS`", which is what its own docstring promised would happen — the same entity-backed shape `osha_log` has. One control does both jobs, which is why `"diary"` had to be added to `ActionsCard.NAVIGABLE_FROM_OVERVIEW`: the render gate is `!enabled || NAVIGABLE_FROM_OVERVIEW.has(target)`, so an enabled row outside the set renders *no control at all*. Story 4.1 hit the same trap with `meetings`. This item is the answer, not a deferral; what remains open is only the cadence, below.
  accepted: Answering 3.5's open question with 'no completion control' is accepted as the decision itself, because a fifth command recording 'I said I did it' would be the second place a completion is written down.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-claim-linked-diary-notes.md`
  summary: **The check-in reads the *caller's* notes, because a note is its author's private record — not because a supervisor is looking at the case file (they cannot).**
  evidence: A note belongs to its author (`diary_note_scope` is author AND employer scope), so "has this been checked in on?" is answered from the reader's own diary. The alternative was to count *anybody's* note on the claim, which would publish the existence of a handler's private working record to whoever else read the claim — a diary is not a management report. **Correction (review, 2026-08-17):** the original wording of this entry, the two docstrings that carried it (`services/worklist/actions.py::_diary_check_in`, `data/repositories/claims.py::select_latest_note_at`) and the spec's own Design Note 4 all justified the decision with "a supervisor opening the same case file still sees the row". That consequence has no path to a screen: `GET /claims/{id}/actions` has no role gate, but the only surface reading it is the workspace and `web/src/App.tsx` gates that route to `allow={["handler"]}` — supervisors and analysts land on the dashboard and never reach a case file. The decision is unchanged and still right on the privacy argument alone; the reachability claim was false and has been removed from all four places. If a supervisor's checklist is ever meant to reflect their handlers' work, that is a product decision about what a diary is *and* a routing decision about what a supervisor can open — two owners, not a dev-time change to a scope predicate.
  accepted: Scoping the check-in to the reader's own diary is accepted on the privacy argument alone — counting anybody's note would publish a handler's private working record — and the false reachability claim has been removed from all four places that carried it.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-claim-linked-diary-notes.md`
  summary: **The meetings list grew a `day` filter and an `upcomingCount` field rather than a new endpoint — and the ordering changed globally for every reader.**
  evidence: The story forbids new meeting endpoints and a second writer on `meeting`, and `noDerivation.test.ts` scans `features/diary`, so the browser could neither filter to today nor count what is ahead. Both facts had to arrive already computed, and the smallest honest answer was to extend the one existing read: `GET /claims-diary/meetings` takes an optional `day` (the viewer's local calendar day, the same class of client input as `as_of`) and its envelope carries `upcomingCount`, always whole-book and independent of `day`. The Notes sub-tab therefore makes **one** request that serves both the summary and the greeting. Filtering a page of the unfiltered list in the browser would have been the fifty-row truncation bug the 4.1 review already caught once, in a new place — today's meetings sort *after* every past one. Two consequences are recorded because they are wider than this story: (1) the meetings ordering is now `(meeting_date, COALESCE(meeting_time,'00:00'), id)` for **every** consumer, so same-day meetings in the Meetings sub-tab read down the clock where they used to read by insertion order; (2) the cursor gained a third member, so cursors issued before this change are refused as `/problems/invalid-cursor` — already the tested behaviour, but a live client mid-scroll across a deploy will see one 400 and reload.
  accepted: Extending the one meetings read rather than adding an endpoint is accepted, with its two consequences recorded: same-day meetings now read down the clock for every consumer, and cursors issued before the third member are refused once.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-claim-linked-diary-notes.md`
  summary: **`useCompleteMeeting` now refetches on 200, which relaxes a policy Story 4.1 argued for.**
  evidence: 4.1's rule was that the 409 path refetches and the 200 path does not, because "the response body already refreshed the row and a follow-up GET would re-open the read-after-write window that returning the entity closes" (`useApprovePayment`'s rule). That premise held while a *row* was all the cache entry contained. Story 4.2 put `upcomingCount` on the envelope — a server-derived aggregate over the whole book that a single meeting's response cannot refresh, and that the browser is forbidden from decrementing (it is on `noDerivation`'s field list). Without the refetch, ticking a today meeting done leaves the greeting two elements above it saying "📅 3 upcoming meetings" over a summary showing two: the console disagreeing with itself, arrived at from the cache instead of from a second `>=`. The `refetch` parameter is kept rather than deleted, because the reason the two paths differ has not gone away and a future command that answers with its entity and moves no aggregate should be able to use the cheap path again. Recorded because it is a deliberate reversal of a documented decision, not an oversight.
  accepted: Refetching on the 200 path is accepted as a deliberate reversal, because upcomingCount is a server-derived aggregate a single meeting's response cannot refresh and the browser is forbidden from decrementing.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-claim-linked-diary-notes.md`
  summary: **The diary pane now opens on Notes rather than Meetings, which is a UX change this story was not explicitly asked to make.**
  evidence: `DiaryNav`'s 4.1 comment reads "Meetings is the sub-tab **this story** builds, so it is the one that opens" — provisional by its own wording. Notes is first in the strip, it is what the prototype's diary pane shows on arrival, and it carries the greeting that UX-DR9 names this story the primary bearer of; landing on the second tab would have put AC 1's whole surface behind a click. The cost is real and is why this is recorded: sub-tab selection is local UI state and deliberately not in the URL (only `?claim=` is), so a **reload returns to Notes wherever the handler was**, and `4-1-meeting-scheduling-management.spec.ts` needed one added click after its reload to keep asserting what it asserts. If sub-tab selection should survive a reload, that is a second query parameter and a decision about what is shareable — the same class of decision `?claim=` already answers, and not one this story should take on the side.
  accepted: Opening on Notes is accepted because it is the prototype's arrival state and carries the greeting UX-DR9 names, at the cost that a reload returns there — putting sub-tab selection in the URL is a separate decision about what is shareable.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-claim-linked-diary-notes.md`
  summary: **`diary_note` is PHI-class and is not in any purge cascade, because there is still no purge cascade.**
  evidence: The 4.1 entry says the same about `meeting`; this is the second of the three tables Epic 4 creates. Story 8.1 owns the cascade, so nothing was added and nothing was invented. What this story left it are the handles it will need: `diary_note.claim_id` is a real FK with the default `ON DELETE` (so an orphan is impossible rather than silently disappearing), every audit diff carries the claim's `WC-nnnn` business id rather than only the note's surrogate, and the app role holds `DELETE` on the table — asserted by `tests/test_diary_notes.py::test_the_app_role_can_delete_so_story_8_1_can_purge`, so a later narrowing of the grant fails a test rather than surprising 8.1. `note_text` is the most sensitive free text the console stores and nothing logs it.
  accepted: Leaving diary_note out of a purge cascade is accepted for the same reason as meeting: Story 8.1 owns the cascade, and this story left it the FK, the business id in every diff and a DELETE grant pinned by a test.

### From: Deferred from: code review of 4-2-claim-linked-diary-notes (2026-08-17)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-claim-linked-diary-notes.md`
  summary: **The day-scoped meetings key rolls over on the next render after local midnight, not at midnight** — and no timer is built to close the gap.
  evidence: `queryKeys.meetings.day(day)` takes the viewer's local calendar day from `lib/clock.ts::todayIso`, and `NotesSubTab` resolves `new Date()` during render. So a Notes sub-tab left open across midnight keeps yesterday's cache key, yesterday's "Today is …" line and yesterday's summary until something re-renders it — which, with `refetchOnWindowFocus` off and a 15s `staleTime` that only marks the *old* key stale, may be hours. Corrected in the comment (it previously claimed the key "rolls over at local midnight", describing a timer nothing schedules) rather than fixed, because the fix is a decision this story should not take on the side: a `setTimeout` to the next local midnight needs an effect in a feature that deliberately has none, or a clock context above the pane, and either wants an owner. The blast radius is small and self-correcting — any click in the pane re-renders it — which is why this is recorded rather than escalated.
  accepted: The cache key surviving local midnight until something re-renders is accepted because the blast radius is small and self-correcting — any click in the pane fixes it — while a midnight timer needs an effect or a clock context this feature deliberately has none of.

### From: Deferred from: implementation of 4-3-templated-stakeholder-emails (2026-08-18)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-templated-stakeholder-emails.md`
  summary: **`select_email_templates`/`select_email_template` take a `CallerContext` they immediately discard**, because reference data has no owner and the repository's structural test requires the parameter.
  evidence: `tests/test_scoped_repository.py` requires every public function in `data/repositories/claims.py` to take exactly one non-defaulted `CallerContext`, which is the rule that makes an unscoped read impossible to write by accident. `email_template` rows are reference data with no owner and nothing to scope, so the two readers take the parameter and `del ctx` immediately with a docstring saying plainly what it is not. The house's answer elsewhere for ownerless reference data is a carve-out module that takes no context at all (`glossary.py`, `statutory_forms.py`); a fourth such module for two functions consumed by exactly one scoped command looked worse than the discard. Recorded because the discard is the kind of thing a reader will either fix or copy, and it should be neither without a decision: either reference-data reads move out of the scoped repository as a class, or the structural test grows an explicit allowlist.
  accepted: The discarded CallerContext on two ownerless reference reads is accepted because a fourth carve-out module for two functions looked worse than the discard, and the structural test that forces the parameter is what makes an unscoped read impossible to write by accident.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-templated-stakeholder-emails.md`
  summary: **The "one transaction" audit test cannot actually distinguish one transaction from two** — a pre-existing technique limitation now copied into a third module.
  evidence: `tests/test_emails.py::test_the_row_and_its_audit_event_are_one_transaction` monkeypatches `db.commit` to raise and then asserts neither the row nor its event is on disk. `tests/test_diary_notes.py:846` established the pattern and `test_meetings.py` follows it. The gap is that an implementation doing `insert → commit() → audit.record() → commit()` would raise on the *first* patched call, leave nothing on disk, and pass identically — so what the test really rules out is a commit through a different session, not a second transaction. Not caused by this story, and not fixed here because the honest version needs a session-level probe (a `SAVEPOINT` counter, or an event listener on `begin`/`commit`) that belongs in `conftest.py` as shared plumbing rather than in one story's test module. Whoever adds it should retrofit all three.
  accepted: The technique limit in the three one-transaction tests is accepted because the honest version needs a session-level SAVEPOINT or begin/commit probe that belongs in conftest.py as shared plumbing rather than in one story's test module.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-templated-stakeholder-emails.md`
  summary: **`meetings.py::normalise_participants` has the same "TypeError escapes as a 500" shape the 4.3 review fixed in `normalise_recipients`** — Story 4.1 code, left alone rather than widened into an email story's diff.
  evidence: `normalise_recipients` constructed `MeetingParticipant(value)` without checking the shape first, so an unhashable member or a non-iterable `raw` raised `TypeError` rather than `ValueError` and escaped the command's refusal path as a 500 instead of the intended 422. The 4.3 review caught it and it is now guarded, matching what `normalise_priority` already did one field over. `normalise_participants` is the function 4.3's was copied from and still has the original shape. Not reachable through the API — Pydantic types the request body as `list[MeetingParticipant]` and refuses a bad shape first — so this is AD-16's "a caller that is not this SPA" case, which is exactly the case both functions' docstrings say they re-validate for. One guard, in a file this story had no other reason to open.
  accepted: Leaving the twin guard unwritten in meetings.py is accepted because Pydantic types the request body and refuses a bad shape first, so the path is unreachable except by AD-16's 'a caller that is not this SPA'.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-portfolio-kpi-cards.md`
  summary: **`USED_THRESHOLDS` in migration 0028 is a hand-transcribed allowlist with nothing tying it to the parameters `compute_benefit` actually reads**, so it fails silently rather than loudly when it rots.
  evidence: Story 5.1 gave 0028 an overlay so a later migration adding a *required* `DerivationThresholds` field cannot kill a fresh `upgrade head` on a parameter this migration never reads (the failure it hit: 653 fixture errors, invisible on existing databases). The guard that keeps the overlay honest is `USED_THRESHOLDS = ("riskHighMin", "riskMedMin", "ptdSeverityThreshold")`, correct today — traced through `compute_benefit` → `derivations.indemnity_type` + `derivations.risk` — but derived by hand through two layers of registry indirection, and checked for *presence* only. Add a parameter to `RiskDerivation` in a later story, forget this tuple, and 0028 completes the missing key from the on-disk document and materializes every seeded payment schedule under a threshold the database does not hold: no exception, no failing test, and the wrong number is financial seed data. The honest close is a mechanical guard — vary one field of `DerivationThresholds` at a time and assert `compute_benefit`'s output moves only for fields named in the tuple — which belongs beside the payment-projection tests as shared plumbing rather than inside a migration, and wants doing before the next required parameter lands.
  accepted: The hand-traced threshold allowlist is accepted for now because the mechanical guard that would keep it honest — varying one DerivationThresholds field at a time — belongs beside the payment-projection tests as shared plumbing rather than inside a migration.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-portfolio-kpi-cards.md`
  summary: **`GET /dashboard/summary` has no role gate and a test makes handler access a contract** — harmless for a count of your own book, and a precedent Story 5.2 must decide on its own terms rather than inherit.
  evidence: Consistent with `/stats/topbar`, so not a regression, and not a leak: the same `employer_scope` predicate applies and `App.tsx` gates the route to supervisor and analyst so no handler reaches the page in the SPA. But `test_the_cards_cover_exactly_the_personas_book` deliberately parametrizes `("Sarah Williams", "handler")` and asserts 200, which turns an accident into a tested contract, and AC 1 is written "Given a supervisor or analyst". The router's "no role branch of any kind… a property this endpoint has by having nothing else in it" is a safe sentence about ten counts over the caller's own claims and an unsafe one copied forward: Story 5.2 serves *peers' performance rows* — rank, cycle time, RTW rate, complexity grade — for every handler with claims in scope, which is a different kind of answer about other people. Worth deciding while the precedent is one endpoint rather than four.
  accepted: The ungated summary endpoint is accepted because the same employer_scope predicate applies and the SPA routes handlers elsewhere; what it must not do is become the inherited precedent for Story 5.2's peer-performance rows.

### From: Deferred from: code review of 5-2-handler-performance-benchmarking (2026-08-18)

- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-handler-performance-benchmarking.md`
  summary: **`DerivationThresholds.__post_init__` permits `riskMedMin == riskHighMin`, which would make the `med` risk band unreachable** — the same equality hole Story 5.2's new parameters were amended to close, in the block that predates them.
  evidence: `rules/parameters.py:274` refuses only `risk_med_min > risk_high_min`. Equal cut-points are not an inversion, so they pass validation and collapse a band silently — no exception, no failing test, and every claim graded High or Low. Story 5.2's review caught the identical shape in the new `HandlerPerformance` block (complexity and deviation cut-points) and the spec now requires `>=` there. The pre-existing twin was left alone because tightening a shipped rule block's validation can turn a currently-loading document into a hard startup failure, which wants its own change and its own check of every seeded version rather than a side effect of a dashboard story.
  accepted: The equality hole in the pre-existing risk block is accepted for now because tightening a shipped rule block's validation could turn a currently-loading document into a hard startup failure, which needs its own change and a check of every seeded version.

### From: Deferred from: implementation of 5-4-priority-claims-worklist-top-30 (2026-08-18)

- source_spec: `spec-5-4-priority-claims-worklist-top-30.md`
  summary: **Verdict on 3.5's "may an action row approve a payment"** — this surface votes for *links, never approves*, and wants no `approvePayment` member on `ActionCommand` on its account.
  evidence: The worklist is read-only by role capability (AD-7): it raises no audit event, offers no control and its action column is a **label** carrying no `command`, no `target` and no document id at all. Adding an approval to `ActionCommand` would put the approvable-status rule in a third place and would give this table a control a supervisor has no capability to use. So nothing here wants that member; if it is ever added it should be for the checklist's own reasons — a handler saving a click on her own case file — and not because a supervisor's table asked for it. The entry stays open on the checklist's terms.
  accepted: The worklist's answer — no approvePayment member on its account — is accepted as a vote rather than a resolution, because it is read-only by role capability and the question stays open on the checklist's own terms.

- source_spec: `spec-5-4-priority-claims-worklist-top-30.md`
  summary: **The worklist's action column is reader-relative, because `latest_note_at` is scoped to the caller's own diary** — one claim's "next best action" can differ between this table and the handler's own checklist.
  evidence: `select_latest_note_at_for_claims` inherits the single-claim read's scope, and that scope is argued: counting anybody's note would publish a handler's private working record to whoever else read the claim. A supervisor has no notes on these claims, so every row resolves `None` and the diary check-in rule fires wherever the stage gate allows. The blast radius is bounded by construction — `urgencyDiaryCheckIn` is `low` and `generate_actions` ranks on urgency first, so the row only reaches position zero when nothing medium or high fired — and it is pinned by `test_the_action_column_is_generated_with_no_note_of_the_readers_own` so it cannot change silently. Not corrected, because the correction is the leak the existing docstring refuses. The honest close is a decision about whether "has this claim been checked in on" is a claim-level fact or a diary-level one, which is a product question about what a diary is.
  accepted: A next-best-action that can differ between the supervisor's table and the handler's checklist is accepted because the correction would be the privacy leak the note scope refuses, and the row's low urgency bounds where it can surface.

### From: Deferred from: code review of 5-4-priority-claims-worklist-top-30 (2026-08-19)

- source_spec: `_bmad-output/implementation-artifacts/spec-5-4-priority-claims-worklist-top-30.md`
  summary: Two e2e assertions compare a single page of Jennifer Park's worklist against the oracle's cap-30 list, and agree only because her population (8) happens to be under the page size (10).
  evidence: Real and currently correct. A seed change pushing her book past ten qualifying claims would fail the scoping test and the smuggled-parameter test for a reason that has nothing to do with scope or smuggling, so the failure would read as a scope regression. Fixing it properly means the oracle growing a page notion, which means restating the page-size rule in the e2e fixture — the thing that file works hardest to avoid. Cheapest honest fix is probably for the oracle to consume the `cap` and page size the payload already publishes.
  accepted: The oracle comparing a single page against a cap-30 list is accepted as currently correct, because giving the e2e oracle a page notion means restating the page-size rule in the fixture that works hardest to avoid restating rules.

### From: Deferred from: implementation of 5-5-dashboard-drill-through (2026-08-19)

- source_spec: `spec-5-5-dashboard-drill-through.md`
  summary: **The four SLA tiles are the one dashboard surface Story 5.5 left non-navigable**, and closing that gap needs a cohort predicate exported from `sla.py` plus a thirteenth drill-through facet.
  evidence: Two things block a `filter[slaBreach]` today, and both are structural rather than preferences. `test_nothing_outside_the_worklist_aggregation_reads_the_sla_source_columns` confines `sla_pick_days`, `sla_approve_days` and `sla_settle_days` to `services/worklist/sla.py`, so the predicate cannot be evaluated inside `drill_through.py` — it would have to be a second scoped read on a route whose entire discipline is `test_the_aggregate_takes_exactly_one_scoped_read`. And the tile UI does not distinguish *which* clock a cohort belongs to: a reader clicking the amber "Settle" tile means "the claims that missed the settle target", which is a vocabulary the four-tile surface cannot express as a single value. The honest fix is one function — `sla.cohort_of(sample, targets) -> SlaMetricKey | None` or similar, exported from the module that already owns those columns — consumed by a thirteenth facet whose values are the four `SlaMetricKey` members. It is a small change made once; it was not half-built here because a facet that read the columns from the wrong module would have to be un-picked before the guard could be trusted again. `PortfolioCharts.tsx` carries the same argument at the call site so the omission reads as a decision.
  accepted: Leaving the SLA tiles non-navigable is accepted because a facet would need the SLA columns read outside the module a guard confines them to, and the four-tile UI cannot express which clock a cohort belongs to as a single value.

### From: Deferred from: code review of 5-5-dashboard-drill-through (2026-08-19)

- source_spec: `spec-5-5-dashboard-drill-through.md`
  summary: **The drill list's claim rows are the one new click target that is not a link**, so the story's own read-only route cannot be middle-clicked, copied, or opened in a new tab from the list that exists to reach it.
  evidence: Real, and the asymmetry is visible in the diff: the KPI cards, chart legends, handler cells, claim ids and "View all" are all `<Link>`s, while the rows reuse `features/queue/ClaimCard.tsx` — a `<button>` whose `onSelect` calls `navigate`. Not fixed here because `ClaimCard` is shared with the handler queue, where `onSelect` sets a query parameter rather than navigating, so making it a link means either forking the component or giving it a polymorphic root, and both are changes to Epic 2's most-rendered surface for the benefit of one caller. The review's narrower half — that it drew no focus ring while every neighbour did — **was** patched, because that one is a defect on both callers. The honest fix is a `ClaimCard` that takes a `to` or an `onSelect` and renders the matching element, made when a second caller needs it rather than speculatively.
  accepted: The one non-link click target is accepted because ClaimCard is shared with the handler queue, where onSelect sets a query parameter, so a link means forking Epic 2's most-rendered component for one caller.

- source_spec: `spec-5-5-dashboard-drill-through.md`
  summary: **`AppliedFilter.display` is `null` for two different reasons and the chip cannot tell them apart**: an id outside the caller's scope (the AD-7 case, where withholding the name is the point) and an id inside it that simply has no claims today.
  evidence: Real. `_applied` resolves the two id displays with a scan over the rows already read, so "no row carries this id" is the only signal it has, and an employer with zero claims in scope degrades its chip exactly as a smuggled one does. The privacy case is the load-bearing one and it behaves correctly; the innocent case is cosmetic and rare (every seeded employer has claims). It is recorded rather than fixed because the fix is not local: distinguishing them means a second read — a scoped lookup asking "is this id in my book?" — on an endpoint whose one-scoped-read discipline is pinned by a test, and the chip would then be publishing existence for ids with no rows, which is the enumeration answer the 404 convention refuses elsewhere. Worth revisiting only if Epic 7's segmentation needs to name a facet value that matches nothing.
  accepted: Chips degrading identically for a smuggled id and an innocent empty one is accepted because the privacy case is load-bearing and behaves correctly, while distinguishing them needs a second read on a one-read route and would publish existence for ids with no rows.

### From: Deferred from: code review of 6-1-local-model-serving-embedding-foundation (2026-08-19)

- source_spec: `spec-6-1-local-model-serving-embedding-foundation.md`
  summary: **The Ollama image is `ollama/ollama:latest`**, while the architecture's stack table calls for a digest pinned at deploy.
  evidence: Real, and deliberately left. A digest committed into a dev compose file goes stale invisibly — nobody notices until a rebuild pulls a different image than the one the digest names — so the architecture puts the pin at deployment time rather than in the repository. But `compose.gpu.yaml` is the only prod-shaped artefact in the tree and pins nothing, and no deploy procedure or digest-refresh step is documented anywhere. What is missing is the procedure, not the line: whoever owns the first non-dev deployment (the same person the Deferred IdP decision is waiting on) should land both together.
  accepted: Leaving the image unpinned in the dev compose file is accepted because a committed digest goes stale invisibly and the architecture places the pin at deployment time; what is owed is the deploy procedure, which the first non-dev deployment owns.

### From: Deferred from: code review of 6-3-copilot-chat-with-persistent-threads (2026-08-20)

- source_spec: `_bmad-output/implementation-artifacts/spec-6-3-copilot-chat-with-persistent-threads.md`
  summary: **Every run and every history fetch issues three separate scoped queries plus a saver read before any work starts**, where one join would answer all three.
  evidence: Real, and invisible at present scale — the thread service resolves the thread row, the claim's visibility and the current sequence independently, each with its own `employer_scope(ctx)` predicate, because each is a separate repository function with its own guard obligations under `test_scoped_repository.py`. Collapsing them means either a composite repository read whose scope predicate is harder to eyeball or pushing the composition into the service, which is where the guards cannot see it. The four-guard suite is deliberately shaped to make the scoped read the easy one to write, and trading that for three saved round trips is not a bargain worth striking before there is a measurement showing it matters.
  accepted: Three separate scoped reads per run are accepted because each is a repository function with its own guard obligation, and collapsing them would move the scope predicate where the four-guard suite cannot see it for three saved round trips.

### From: Deferred from: 6-4-deterministic-quick-actions-qas (2026-08-20)

- source_spec: `_bmad-output/implementation-artifacts/spec-6-4-deterministic-quick-actions-qas.md`
  summary: **The RTW draft letter names no return date, because no reader the copilot has projects one.**
  evidence: Real, and the omission is the honest half rather than a gap. `Claim.rtw_rec` and `Claim.actual_rtw` are columns on the ORM model and appear on **no** `ClaimDetail` field, so `agents/tools/rtw.py` can project the contraindications, the return-to-work prognosis and the stage variant's `return_status` and nothing else. The dates are not absent from the *build*, and the distinction matters because both quick actions answer about the same claim: `services/worklist/actions.py::_overdue_rtw` reads `rtw_rec` and `actual_rtw` off the ORM row directly and can put "Review Return-to-Work policy & offer letter" on the `nextactions` checklist as overdue. What is missing is a `ClaimDetail` projection the copilot's tools could read, which is why the gap is a deferred *services-layer* item rather than a missing column. Putting a target date in the letter would mean either a services-layer change this story has no standing to make or a date the model originated, and AD-2 forbids the second in the same sentence it forbids a money amount. The node's prompt therefore instructs "the start date will be confirmed by the handler". Story 6.5 owns the editable RTW modal and the approval gate that saves the letter back to the claim, which is where a handler would type one — so the services-layer projection (a `ClaimDetail.injury.rtw_recommended` / `actual_rtw` pair, or an RTW block of its own) should be designed there, against a surface that has a writer for it.
  accepted: Omitting a return date is accepted as the honest half: no ClaimDetail field projects rtw_rec or actual_rtw, and a date the model originated is what AD-2 forbids — the projection belongs with Story 6.5's editable modal.

### From: Deferred from: implementation of 7-1-fraud-analytics-workspace (2026-08-21)

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-fraud-analytics-workspace.md`
  summary: **`fraud_red_flags` takes two scoped reads where every other aggregate on this dashboard takes one**, and the second exists only to supply a denominator.
  evidence: Deliberate and pinned by `test_the_red_flag_aggregate_takes_exactly_two_scoped_reads` rather than left to grow. The published coverage is "claims carrying a fraud narrative, out of claims in scope", and the second half cannot come from the insight join: a claim with no `ai_insight` row produces no row to count. The alternatives are worse — an outer join from `claim` returns one row per claim in the portfolio to answer a question about a handful of them, and dropping the denominator would leave the card unable to say what fraction of the book it is describing, which is the one thing AD-10 requires of a cached-content surface. Recorded because the *count* is the discipline every sibling aggregate is held to, and a departure from it should be visible rather than discovered.
  accepted: The second scoped read is accepted because the coverage denominator cannot come from the insight join — a claim with no ai_insight row produces no row to count — and the departure is pinned by a test rather than left to grow.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-fraud-analytics-workspace.md`
  summary: **The rate tables' sort state is component state and is not in the URL**, so a shared or reloaded link to the Fraud section always shows the three default orders.
  evidence: A choice rather than an oversight, and the reasoning is that Story 7.3 owns what belongs in this workspace's address bar. Three sort parameters would triple a shareable link's surface area for a preference that carries no data and that the payload already echoes (`RateBreakdownResponse.sort`), and 7.3's segmentation control — which *is* URL state by its own acceptance criteria — will have to decide how the workspace's query string is composed. Making that decision twice, once here for sorts and once there for filters, is how two schemes end up in one address bar. The cost today is that "send me this table sorted by most flagged" is not a link; the fix is one line per parameter once the workspace's URL contract exists.
  accepted: Keeping the three sorts out of the address bar is accepted because Story 7.3 owns the workspace's URL contract, and composing that scheme twice is how two schemes end up in one query string.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-fraud-analytics-workspace.md`
  summary: **`test_no_module_outside_the_registry_hardcodes_the_band` gained 55 and deliberately did not gain 60**, so the SIU referral threshold is the one fraud cut-off no grep guards.
  evidence: The spec sanctions the omission and this records what it costs. Six files outside the guard's allowlist carry a bare 60 and none is a rule value: a 60-second HTTP timeout in `config.py` and `api/routers/claims.py`, "6 000 is 60%" in a basis-points note in `rules/parameters.py`, a `[10:60]` slice in `services/worklist/queue.py`, and — in `agents/tools/fraud.py` and `services/worklist/drill_through.py` — two *prose* mentions of `score >= 60` written precisely to explain why the comparison is not in the code. Adding 60 would fail the test on six comments, and the only ways out are rewording six explanations or allowlisting five directories, both of which trade a real guard for a green tick. What is left uncovered is narrow: `siu_fraud_score_min` is read by exactly one derivation and published by three payloads, all of which are asserted against the loaded document. The honest fix, if it is ever worth one, is a guard that reads code with comments stripped — which the web side already has in `noDerivation.test.ts::code()` and the server side does not.
  accepted: Leaving the SIU threshold outside the grep guard is accepted because adding 60 would fail the test on six comments and prose explanations, trading a real guard for a green tick, while the parameter is read by one derivation and asserted against the loaded document.

### From: Deferred from: implementation of 7-3-segmentation-universal-drill-down (2026-08-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-7-3-segmentation-universal-drill-down.md`
  summary: **`useDrillClaims` did not gain `placeholderData: keepPreviousData`**, so the flagged-claims list embedded in the Fraud section blanks to a skeleton while a filter change is in flight, where the four aggregate surfaces beside it keep their previous answer on screen.
  evidence: A deliberate omission. AC 8 asks that "previously rendered surfaces stay visible and only the affected surface reports busy", and the four analyst reads (`useFraudPanel`, `useFraudRates`, `useFraudRedFlags`, `useTrends`) plus `useSegmentationValues` all carry the option for exactly that reason. `useDrillClaims` is Story 5.5's hook and is shared with `DrillClaimsPage`, whose `isLoading` predicate (`isPending && data === undefined`) and whose "Show more" accumulation are both written against a query that *does* drop to `undefined` on a key change — so adding the option would change the behaviour of a shipped screen for the benefit of an embedded list, which is the kind of change that belongs to the story that owns that screen. The cost is one skeleton on a surface that is genuinely affected by the filter, which is the weaker half of the criterion rather than a violation of it.
  accepted: The one skeleton on a filter change is accepted because useDrillClaims is Story 5.5's hook and its DrillClaimsPage predicates are written against a query that does drop to undefined on a key change.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-3-segmentation-universal-drill-down.md`
  summary: **A dimension offers one value at a time and there is no OR within one**, so "Aerospace or Automotive" is two questions rather than one.
  evidence: What the story asked for — the AC says composed filters AND together and says nothing about multi-value — and the shape enforces it: `Segmentation`'s fields are single optionals, so there is nowhere to put two sectors and `test_one_value_per_dimension_and_no_or_within_one` asserts the type refuses it rather than that a function happens not to implement it. Recorded because the *epic* context says "multi-value per dimension, dimensions AND-composed (values within a dimension OR-composed)" and this story's own spec does not, so the two planning documents disagree and the narrower reading was taken. Closing it is not a local change: it multiplies the wire form (`filter[sector]=A&filter[sector]=B`), the cursor payload `drill_through` compares by bytes, the chip row's one-chip-per-facet shape, and every oracle in three suites.
  accepted: Single-valued dimensions are accepted as what the story asked for and what the type enforces, recorded only because the epic context describes multi-value and the narrower reading was taken; widening it moves the wire form, the cursor bytes, the chip row and three oracles.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-3-segmentation-universal-drill-down.md`
  summary: **`fraud_red_flags` replaced its `COUNT` with a full projection read**, so the red-flag view's second scoped read is now one row per claim in scope rather than one integer.
  evidence: Forced rather than chosen, and still two reads (`test_the_red_flag_aggregate_takes_exactly_two_scoped_reads` pins it). The published coverage denominator is now the *segmented* book, and a segmented count cannot be taken in SQL: two of the ten dimensions are registered derivations, so counting them in the database would put half the filter in `data/` and half in `services/` — the split `claims.py` refuses in writing twice. The row set is the one the panel and the rates already read on the same screen, so at the seeded volume it costs nothing; it is recorded because "the second read is a cheap aggregate" was the argument 7.1 made for taking it at all, and that argument no longer holds.
  accepted: Reading rows instead of a count for the denominator is accepted as forced rather than chosen: two of the ten dimensions are registered derivations, so a segmented count in SQL would split the filter across data/ and services/.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-3-segmentation-universal-drill-down.md`
  summary: **`claimsInScope` changed meaning on two shipped payloads** — `FraudPanelResponse` and `PortfolioTrendsResponse` now report the *segmented* book rather than the caller's whole one.
  evidence: Deliberate and argued at both classes: with a filter applied every figure on those payloads describes the intersection, so a denominator quoting the unfiltered portfolio would put "11 of 100" under charts folded from twenty. The unfiltered count did not disappear — it is `claimsInScope` on `GET /dashboard/segmentation/values`, published beside `claimsMatching` so the workspace can state both. Recorded because it is a semantic change to a field two earlier stories shipped, and a client that had stored a response from before this story would read the two as the same number.
  accepted: Redefining claimsInScope as the segmented book is accepted because every other figure on those payloads describes the intersection, and the unfiltered count is still published beside claimsMatching on the segmentation values route.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-3-segmentation-universal-drill-down.md`
  summary: **`test_no_module_outside_the_registry_hardcodes_the_band` gained 45 and is still a grep for four known integers**, so an age edge retuned to a number the regex does not carry loses its guard.
  evidence: The spec's rule was followed — 45 joined the set because every bare 45 in the tree was already inside the allowlist — and this records what the guard is, which the 7.1 entry above says of 60 from the other direction. The check greps for `65|55|45|35` with comments included; it is not "no rule value is inlined" and cannot become that without reading the parameter block it is meant to be independent of. The honest fix, if it is ever worth one, is the same one 7.1 named: a guard that reads code with comments stripped, which the web side already has in `noDerivation.test.ts::code()`.
  accepted: A guard that greps 65|55|45|35 with comments included is accepted as what it is, because becoming 'no rule value is inlined' would require reading the parameter block it is meant to be independent of.

### From: Deferred from: review of 7-3-segmentation-universal-drill-down (2026-08-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-7-3-segmentation-universal-drill-down.md`
  summary: **`ageBands.ts` decouples the band vocabulary from the edges but not from the arity** — it maps exactly four members onto exactly three edges in one fixed order.
  evidence: Real, and it is the one seam left in an otherwise complete argument. The whole reason the `AgeBand` members are ordinal words rather than numbers is so a v8 of `derivation_thresholds` can move an edge without falsifying a member's name. That works. What does not work is a v8 that *adds* a band: the helper would label four of five and return `undefined` for the fifth, and `chipLabel` would fall through to the raw wire value, so one picker option and one chip would read `oldest` while its neighbours read "45–54". Not fixed here because the fix is either a vocabulary-length assertion (which turns a silent mislabel into a hard failure at a moment nobody is testing bands) or an edge list published as an array (which is a wire change to a payload three stories now consume). Worth taking with whichever story next touches the age document.
  accepted: Mapping four members onto three edges in a fixed order is accepted because the alternatives — a length assertion that hard-fails when nobody is testing bands, or an edge array on the wire — are both larger than the seam, which only opens if a document version adds a band.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-3-segmentation-universal-drill-down.md`
  summary: **The workspace nav carries the ten dimensions between sections but not the six section controls**, so Trends → Fraud → Trends silently resets `grain`, `anchor` and `cohort` to their defaults.
  evidence: Real and argued in `WorkspaceNav.tsx`'s docstring, and it is not a regression — before this story those controls were component state, which unmounted on the same navigation. So the behaviour is unchanged; what changed is that it is now surprising, because the filter beside them *does* survive the click. AC 3 is satisfied for the case it names (a shared or reloaded URL restores the state) and is silent on in-app navigation, which is why this is not a defect against the story. The tension is that the address bar now holds two classes of state with two lifetimes and nothing on screen says which is which. The fix is one line per parameter in the same helper that already carries the dimensions; the question it needs answered first is whether a section's controls *should* follow the analyst between sections or whether each section owns its own view — a product question, not a dev-time one.
  accepted: Section controls resetting on in-app navigation is accepted because it is unchanged behaviour, and whether a section's controls should follow the analyst between sections is a product question rather than a dev-time one.

### From: Deferred from: implementation of 7-4-financial-decomposition (2026-08-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-7-4-financial-decomposition.md`
  summary: **The portfolio reserve verdict reads stored schedule rows and never refreshes them**, so a claim whose week boundary has passed can read one band behind its own case file.
  evidence: Deliberate and argued at `services/financials/reserve.py::reserve_checks_for_claims`: `reserve_check_for_claim` materializes before it reads because two *claim-level* surfaces must not disagree, and a materialization is a write that a read-only analyst route may not make — `priority_claims.py::_action_labels` set the precedent for bulk-reading stored rows for the same reason. Three of the five `payment_schedule_week` statuses move with the calendar, so between the moment a week elapses and the next time anybody opens that claim, the portfolio's `remaining_indemnity_cents` for it is higher than the case file's would be, and a claim sitting near a band edge can be counted `light` here and `adequate` there. `ReserveAdequacyCard.tsx` footnotes it rather than implying a freshness the payload does not have, and `test_the_bucket_a_claim_lands_in_is_its_own_case_files_verdict` pins the *shared computation* by refreshing every claim first and then comparing — which is what AD-2 actually requires and is deliberately not evidence about the staleness window. Closing it needs either a background materializer on a schedule (a job runner this build does not have) or a read-path refresh (a write on a `GET`), and both are larger than this story.
  accepted: A portfolio verdict up to one week boundary behind the case file's is accepted because materializing is a write a read-only analyst route may not make, and the card footnotes the staleness rather than implying a freshness the payload lacks.

### From: Deferred from: review of 7-4-financial-decomposition (2026-08-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-7-4-financial-decomposition.md`
  summary: **`filter[reserveVerdict]` still costs two bulk reads on a route with no role gate**, now proportional to the filtered list rather than to the caller's whole book.
  evidence: `GET /dashboard/claims` is reachable by any authenticated session — it is the drill list every persona uses — and the 25th facet is the first one whose predicate needs rows the claim read does not carry. The review found the verdict load running over the *whole scoped caseload* before any other facet narrowed it, so `&filter[reserveVerdict]=light` alone forced every schedule week and every bill in scope into Python and `&filter[employerId]=…` beside it reduced nothing. That half is fixed: `_without_reserve_verdict` applies the other twenty-four facets first and `test_the_verdict_reads_cover_the_narrowed_list_and_not_the_whole_book` pins the population at 62 rather than 100. What remains is that the *unnarrowed* request — the verdict facet and nothing else, which is exactly what the adequacy card's own drill issues — is still a whole-book read of two child tables by design, and no role gate stands in front of it. At the seeded volume that is ~1,100 week rows; the entry exists because the growth profile belongs with the push-down count above, and because "the cheapest request on this route is the one the UI generates" is worth an owner deciding about rather than inheriting.
  accepted: The unnarrowed verdict facet's two bulk reads on an ungated route are accepted at seeded volume, the review having already fixed the half that read the whole book before the other facets narrowed it.

### From: Deferred from: implementation of 7-5-dataset-chart-export (2026-08-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-dataset-chart-export.md`
  summary: **`export_limits.maxRows` caps CSV and XLSX at the same number**, though the two have materially different memory profiles.
  evidence: Deliberate and argued in `rules/documents/export_limits.v1.jdm.json`: the parameter's unit is *rows of the exported table*, and one number governing all eight targets is what keeps eight caps from drifting apart. But the two renderers are not alike. `render_csv` is a generator draining a 64 KB buffer, so its peak is the materialised table plus one chunk; `render_xlsx` builds the whole workbook in memory (`{"in_memory": True}`, which is the AD-11 posture and not negotiable) and holds a shared-string table besides, so a fifty-thousand-row spreadsheet is resident twice — once as the table and once as the zip — before a byte is sent. At the seeded hundred claims neither is measurable and the cap is far above anything this dataset can reach, which is why it is recorded rather than split. The honest fix, if a real book ever approaches the cap, is a second parameter in the same document (`maxXlsxRows`) rather than a code constant, because it is the same *kind* of number with a different answer. Splitting it now would be adding a parameter nobody can tune against evidence.
  accepted: One cap for both renderers is accepted because the parameter's unit is rows of the exported table and one number keeps eight caps from drifting; a maxXlsxRows belongs in the same document if a real book ever approaches it.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-dataset-chart-export.md`
  summary: **The claim export omits `reserve_verdict`**, so the one facet whose value the drill list can filter on is the one column a downloaded claim list does not carry.
  evidence: Deliberate, argued at `_CLAIM_COLUMNS`, and worth recording because it is the only asymmetry between the export's columns and the drill projection's fields. `DrillFlags.reserve_verdict` is `None` unless `filter[reserveVerdict]` is set — the verdict map is loaded conditionally precisely so every other drill URL stays at one scoped read — so a column for it would be blank on almost every export and populated on a few, which is a column meaning "we did not look" that reads as "there is no verdict". A blank a consumer cannot tell from an answer is worse than an absent column. The consequence is that an analyst who exports a claim list and then wants each row's reserve verdict has to take the second export (`/dashboard/financials/reserve-adequacy/export`), which gives the *distribution* rather than the per-claim value — so the per-claim verdict is reachable on screen and not in a file. Closing it means either loading the verdict map unconditionally on the export path (three reads for every claim export, where the paged sibling takes one) or publishing a tri-state column, and both are decisions about what an export costs rather than about how it renders.
  accepted: Omitting the verdict column is accepted because it is loaded only when the facet is set, so the column would be blank on almost every export — a blank meaning 'we did not look' that reads as 'there is no verdict'.

### From: Deferred from: review of 7-5-dataset-chart-export (2026-08-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-7-5-dataset-chart-export.md`
  summary: **The claim export publishes twenty-two per-claim fields no API payload has ever published**, and the argument for it is about the SQL projection rather than about egress.
  evidence: Real, and the spec ruled it deliberately at Design Note 6 — "nothing new leaves the database: the read is the read the list already takes" — which is true of `select_drill_rows` and is not the boundary NFR-5 is about. `DrillRow` publishes fourteen fields and its own docstring records that it *withholds* employer id, handler name and state; `_CLAIM_COLUMNS` publishes thirty-six, adding `age`, `gender`, `disability`, `icd`, `doi`, `froi_date`, `state`, `reserve_cents`, `fraud_score`, `severity_score`, `handler_name`, `employer_id`, `osha_recordable`, `sector`, `region`, `status`, `return_status`, `recovery` and the four derived bands. The counter-argument the story rests on is genuine and unchanged: an export whose purpose is offline segmentation analysis and which omits the segmentation dimensions is an export nobody can use, and every field is inside the caller's scope and already reachable to that persona in aggregate. What has no owner is the *minimum-necessary* question — whether an analyst entitled to a book is entitled to every column of it in a file that leaves the building — and this is the first surface on which it is more than theoretical, because it is the first payload to carry a worker's age and gender per claim. The audit row records how many rows left and under which filter; it records nothing about which columns, so the question is not even answerable after the fact today. The honest fix is a per-target column policy with an owner, which is a product decision rather than a dev-time one.
  accepted: Exporting the segmentation dimensions is accepted because an offline-analysis export that omits them is unusable and every field is inside the caller's scope; the minimum-necessary question it opens is a product decision with an owner.

### From: Deferred from: implementation of 8-1-phi-purge-cascade-retention (2026-08-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-8-1-phi-purge-cascade-retention.md`
  summary: **Checkpoint retention is keyed on `copilot_thread.created_at`**, so the ninety-day knob bounds a conversation's *age* rather than its idleness.
  evidence: The four checkpoint tables carry no timestamp column this codebase may read — they are the saver's under AD-3's vendored-migration exception, and `tests/test_layering.py` fails the suite for any module that names one in SQL — so the only clock available to `purge_expired_checkpoints` is this project's own `copilot_thread.created_at`, minted by the command that starts the conversation. The consequence is real: a thread created ninety-one days ago is swept even if a handler used it yesterday, and the handler loses a conversation they were still in. `test_the_checkpoint_sweep_discards_conversations_older_than_the_cutoff` pins the behaviour so it cannot change by accident. The fix is a `last_used_at` column on `copilot_thread`, written by the run endpoint — which is a write on the hot path of every chat turn and a mutable column on a table whose docstring argues at length that it is append-only and therefore needs no `version`. That is a decision about what a thread row *is*, and it belongs to whoever next changes the copilot's persistence rather than to the retention job.
  accepted: Sweeping conversations by age rather than idleness is accepted because the checkpoint tables carry no timestamp this codebase may read, and a last_used_at column means a write on the hot path of every chat turn on a table argued to be append-only.

### From: Deferred from: code review of 8-2-database-audit-log-hardening (2026-08-22)

- source_spec: `_bmad-output/implementation-artifacts/spec-8-2-database-audit-log-hardening.md`
  summary: **`information_schema.role_table_grants` under-reports**, so the grant-drift test can miss a grant made by another grantor.
  evidence: `test_audit_grants.py` asserts set equality on privileges read from that view, and spends a paragraph arguing the `==` is the whole design. But the view returns only rows where the current role is the grantor, the grantee, or a member of the grantee — so a future migration granting `UPDATE ON audit_event` from a grantor the owner connection is not a member of is invisible to it and the equality still passes. `pg_class.relacl` (or `information_schema` joined against `pg_roles`) is the drift-proof source and would also make the column-level assertions readable in one place. The rewrite is small; it was left because it changes what every assertion in the file reads from, and doing that under review rather than under spec is how a verification test quietly stops verifying.
  accepted: Reading grants from a view that only returns rows the current role granted or holds is accepted for now, because switching every assertion in the file to pg_class.relacl under review rather than under spec is how a verification test quietly stops verifying.

### From: Deferred from: implementation of 8-3-encrypted-backups-restore-drill (2026-08-23)

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **The off-host destination is a single target with no scheduled integrity verification** beyond rsync's own transfer check.
  evidence: `manifest.json` carries a sha256 per artifact, computed over the *ciphertext* so a copy can be verified by somebody who cannot read it, and `deploy/.env.example` and the runbook both document the `sha256sum -c` invocation. Nothing runs it on a schedule and nothing runs it at all until a restore. So a destination that silently corrupted a file, or a retention policy at the far end that deleted one, is discovered at restore time — which is precisely the class of failure this story exists to eliminate. There is also exactly one destination: `BACKUP_REMOTE` is a single string, so "off-host" is one host, and losing it plus the primary loses everything. Closing either half means a verification job and a second destination, both of which are operational decisions with no owner yet.
  accepted: A single BACKUP_REMOTE with verification deferred to restore time is accepted because a second destination and a verification job are operational decisions with no owner yet; the per-artifact ciphertext sha256 and its documented invocation are already shipped.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-3-encrypted-backups-restore-drill.md`
  summary: **The spec's Design Note 1 was wrong on a fact, and closing the gap needed a `pg_hba.conf` record** — a change to the postgres service the design note said would not be necessary.
  evidence: Design Note 1 chose `pg_receivewal` over `archive_command` partly because it "needs no change to the postgres service beyond the retention bound". That is one record short of true in this deployment. The official Postgres image's entrypoint appends exactly `host all all all scram-sha-256`, and PostgreSQL does not match a **physical replication** connection against a database column of `all` — verified by reproducing the failure (`FATAL: no pg_hba.conf entry for replication connection from host "172.17.0.1", user "lineworker"`), so both `pg_basebackup` and `pg_receivewal` were unusable from the backup container. The fix is `deploy/postgres/pg_hba.conf` (the four `initdb` defaults restated, plus `host replication all all scram-sha-256`) mounted read-only and named by `-c hba_file=` in all three profiles — the same `-c` mechanism Story 8.2's Design Note 1 argues for, chosen because an `initdb` hook would never fire on the pre-existing `pgdata` volume and the drift would be invisible. **No role, no grant, no schema change**: the owner role already carried REPLICATION. The residual is that this repository now owns its own `pg_hba.conf` wholesale, so any future change to the image's default authentication posture has to be mirrored into that file by hand; `test_backup_posture.py::test_the_committed_hba_file_permits_replication_and_nothing_looser` asserts both directions of it.
  accepted: Owning a committed pg_hba.conf is accepted as the cost of making pg_receivewal work at all, with the residual that image default changes must be mirrored by hand — asserted in both directions by test_backup_posture.py.

### From: Deferred from: code review of 8-4-complete-quality-operations-gate (2026-08-23)

- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-complete-quality-operations-gate.md`
  summary: **The NFR-7 "SSE + assistant-ui stream protocol" element is closed by a vocabulary contract test, not by a third integration test**, and the two halves are still exercised against fixtures each side authors.
  evidence: The audit found both halves real — `tests/test_copilot_threads.py` drives the actual endpoint and asserts `text/event-stream`, the frame sequence, exactly one `done` and exactly one `error`; `web/src/api/copilot.test.ts` drives the client parser through truncation, cancellation and a final frame with no trailing newline — and the join missing. `tests/test_stream_protocol_contract.py` now reads the server's frame names out of `copilot.py`'s AST and compares them to `SERVER_EVENTS` in `web/src/api/copilot.ts`, which closes the drift the client file's own comment predicts. What it does not do is run one process against the other: a renamed frame is caught, a *semantic* change to what a correctly-named frame carries is not, and neither is a client that parses a well-formed stream incorrectly. A genuine cross-process test would need the web client's parser driven against a live server response, which is a harness this repository does not have and which belongs with Story 6.5's scope rather than with the gate that audits it.
  accepted: Closing the stream-protocol element with an AST-to-constant frame-name comparison is accepted because a genuine cross-process test needs a harness driving the web parser against a live server response, which belongs with Story 6.5 rather than the gate auditing it.

- source_spec: `_bmad-output/implementation-artifacts/spec-8-4-complete-quality-operations-gate.md`
  summary: **`test_deploy_ops_posture.py`'s env-keyed-settings check derives its subject from `config.py` by pattern, so a third `env`-keyed default written in a shape the pattern does not match would escape it silently.**
  evidence: The check greps `config.py` for the `self.env is …` derivation shape, finds exactly the two properties that exist (`cookie_secure`, `scheduler_runs`), and asserts the prod overlay sets both explicitly — which is strictly better than the hardcoded list it replaced, and it fails loudly if the pattern matches nothing at all. What it cannot see is a *differently shaped* third one: `self.env in (Env.prod, Env.e2e)`, or a non-boolean default selected by a dict lookup on `self.env`, or one resolved in a validator rather than a property. The consequence is the one this story closed for the two that exist — a setting silently resolving to its dev branch behind a TLS ingress. Closing it properly means asserting over `Settings`' resolved attributes at runtime rather than over the source text, which needs the settings module imported into a posture test that deliberately imports nothing.
  accepted: A posture check that greps config.py for one derivation shape is accepted as strictly better than the hardcoded list it replaced, since asserting over resolved Settings attributes would need the settings module imported into a test that deliberately imports nothing.
