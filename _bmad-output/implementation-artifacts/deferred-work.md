# Deferred Work

Items raised in review that were consciously deferred. Each carries the reason it was not actionable at the time.

## Deferred from: code review of 1-3-role-persona-login (2026-08-10)

- **No session reaper; login never revokes existing sessions.** Every `POST /auth/login` inserts a row unconditionally and expired rows are deleted only "on contact" in `resolve_session`, so a persona can hold an arbitrary number of simultaneously-valid tokens and the `session` table is append-mostly. Deferred: how many concurrent sessions a persona may hold (multi-device vs single-session) is a policy decision beyond this story, and the cleanup job needs a scheduler that does not exist yet.
  - *Re-raised by the 2026-08-10 code review of Story 1.4*, with the sharper case: `login` does not revoke **the session presented in its own request cookie**, so a login that skips `POST /auth/logout` (a second tab, or the guard's redirect after an unrelated failure) leaves the previous token live and replayable for the full TTL even though the browser has discarded it. Still deferred for the same reason — revoking the presented cookie is a one-line change, but it *is* the single-session policy decision, made silently. The "↩ Switch" button, the only persona-switch path in the UI, does revoke.
- **`/personas` has no `filter[…]`/`sort`/cursor parameters.** The Lists convention specifies these for all list endpoints; the persona picker returns a fixed ten rows in the `{items, nextCursor, total}` envelope without them, and the role partition happens in the SPA. Deferred: the convention's pagination/filter machinery should be built once, with Epic 2's first genuinely pageable list endpoint, rather than improvised for a ten-row public picker.
- **An idle SPA never notices server-side session expiry.** With `staleTime: 30_000`, `refetchOnWindowFocus: false` and a guard that checks only on mount, a user left on `/dashboard` past `expires_at` keeps seeing the authenticated shell until their next navigation. Deferred: the right fix (periodic revalidation, or a refresh-on-focus policy) is coupled to the session-lifetime decision above.
- **`ApiModel` has no `extra` policy.** With `populate_by_name=True` and Pydantic's default, request models accept both `personaId` and `persona_id` and silently ignore unknown fields, so the camelCase boundary is a convention rather than an enforced contract. Deferred: strictness here is a project-wide decision affecting every future endpoint, not a defect of this story.

## Deferred from: code review of 1-6-domain-glossary (2026-08-10)

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-domain-glossary.md`
  summary: The list envelope on `GET /api/glossary` asserts a pagination that does not exist — `nextCursor` is structurally always null, the repository read has no `LIMIT`, and `total` is `len(items)` computed from the very list it is returned beside, so a truncation could never be detected.
  evidence: Real, but pre-existing convention rather than a defect of this story: `/personas` shipped the identical shape in Story 1.3, and item 2 of this file already records that the convention's pagination/filter machinery should be built once, with Epic 2's first genuinely pageable list endpoint. The concrete hazard is deferred with it — if a later story adds a `LIMIT` to `list_terms` for any reason, `total` silently changes meaning from "rows that exist" to "rows on this page", and `test_every_persona_gets_every_term_in_order` would fail on `items` first and mask the semantic break. When that machinery lands, `total` should come from a `COUNT(*)` or be dropped (the convention writes it as optional).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-6-domain-glossary.md`
  summary: `data/seed/extract_prototype_seed.py` documents and defaults to `../docs/Workers_Comp_Prototype.html`, which from `server/` resolves to `lineworker/docs/…` — a path that does not exist; the real one is `../../docs/…`.
  evidence: Verified by running it: the documented command fails with `FileNotFoundError`. Predates this story (Story 1.2 shipped it) and belongs to the portfolio seed, not the glossary. Story 1.6's own extractor had the same bug copied into it and fixed there; this one is left alone because re-running it rewrites `seed_data.json`, which is Story 1.2's committed artifact and outside this story's blast radius.

## Deferred from: code review of 2-1-prioritized-filterable-claim-queue (2026-08-11)

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-prioritized-filterable-claim-queue.md`
  summary: The queue's DB-backed tests build the app on the migration *owner* connection, so the `SELECT`-only grant on `rule_document` is asserted by reading `information_schema` and never by serving a request as `lineworker_app`.
  evidence: Real, and pre-existing rather than introduced here — `tests/test_glossary.py` established the pattern in Story 1.6 and `conftest.py` exposes only `seeded_db_url` (the owner URL). The concrete hazard is that a missing or wrong grant on a new table would 500 every request in the deployed stack while the suite stayed green; only the e2e stack, which does run as the app role, would catch it. The fix is a shared app-role client fixture in `conftest.py`, which belongs with the fixture rather than with any one story.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-prioritized-filterable-claim-queue.md`
  summary: A missing, uncompilable or invalid rule document surfaces as an unhandled 500 rather than an RFC 9457 problem document, and it takes `/stats/topbar` down with it.
  evidence: Real: `rules.engine.load` raises when no document is effective on the date, and neither `/claims/queue` nor `/stats/topbar` catches it. Deferred because a 500 is a defensible answer to a broken deployment — the documents are seeded by migration, so their absence means the migration chain did not run — and because choosing the right shape (503 + `Retry-After`? a degraded queue?) is a service-availability policy decision that spans every future JDM consumer, not just this endpoint.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-prioritized-filterable-claim-queue.md`
  summary: The seed migration inserts the committed JDM documents without compiling them, so a syntactically valid but uncompilable document passes the migration and fails at request time instead.
  evidence: Real but narrow: `tests/test_rules_engine.py` evaluates both committed documents, and CI runs it, so a bad document cannot reach `main` — the exposure is a hand-edited document on a developer's branch. Compiling inside `upgrade()` would also make the migration chain depend on the ZEN runtime, which is a heavier coupling than the failure warrants.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-prioritized-filterable-claim-queue.md`
  summary: Every queue request reads, derives and scores the caller's entire scoped portfolio before slicing — including page-2 requests, which redo all of it — because the sort key is Python arithmetic and cannot be pushed into SQL.
  evidence: Real and by design at this size (100 claims, and a `scope_all` persona is still the whole table). It is recorded because the cost is proportional to the portfolio and the queue is on the handler's critical path: the first portfolio that makes this matter needs either a materialised score column refreshed on write (which AD-10 forbids as a user-writable column but would permit as a derived cache) or a scoring pass pushed into SQL from the same JDM parameters. Neither is a decision this story should make.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-prioritized-filterable-claim-queue.md`
  summary: `utc_today` is defined in `rules/engine.py` and re-exported through `services/derivations`, so `services/worklist/queue.py` imports its clock from the derivations package while importing its parameters from `rules.parameters` — two doors to one value.
  evidence: Real, cosmetic today, and a genuine trap later: the moment a second clock source appears (a request-scoped "as of" for back-dated reporting, say), the two import paths make it easy to have half the derivations on one and half on the other. It belongs in a small `services/clock` or on the caller context, which is a cross-cutting placement decision.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-prioritized-filterable-claim-queue.md`
  summary: Both queue oracles resolve "today" independently of the server, so a test run that straddles midnight UTC disagrees with the stack by a day on every `days_open`, score, order and marker expectation.
  evidence: Real and acknowledged in `e2e/fixtures/seed.ts`'s docstring but not in `tests/seed_fixture.py`'s. The exposure is a few seconds per day, and the alternative — pinning a date the running stack does not share — trades a rare flake for a permanent fiction. Recorded because the honest fix is for the API to report the `asOf` it resolved and for both oracles to consume it, which is a small wire change better made when a second date-sensitive endpoint needs it.

## Deferred from: code review of 2-1-prioritized-filterable-claim-queue (2026-08-11)

Second review pass — three layers (adversarial, edge-case, acceptance audit) against `e889089..b4a5494`. Three items below restate deferrals already recorded from the first pass and are noted as such rather than duplicated in substance.

- source_spec: `_bmad-output/implementation-artifacts/2-1-prioritized-filterable-claim-queue.md`
  summary: `pageLimit` lives in the priority-weights JDM document, so changing the queue's default page size requires an Alembic migration seeding a new document version — and, because the cursor records the weights version, doing so invalidates every outstanding cursor.
  evidence: Real and a consequence of an argument made deliberately (a rule element lives in one tier, and how many claims a page holds is worklist tuning). The cost was not priced when the placement was chosen: page size is a transport concern that operators tune far more often than they retune weights, and coupling the two means a routine paging change re-ranks nothing but invalidates every open queue. Revisit when a second consumer needs a page size — the honest split is probably `pageLimit` in config and the marker parameters in the document.

- source_spec: `_bmad-output/implementation-artifacts/2-1-prioritized-filterable-claim-queue.md`
  summary: Every "Show more" request re-computes and re-serialises the first page of all four stage groups, and the client keeps one and discards three.
  evidence: Real. `useStageGroupPages`'s query function ends `return data!.groups[stage]`, and the endpoint's "every group is always the truth" rule — well argued for the initial load, since a response with three blank groups is indistinguishable from a caseload that has nothing in them — was never revisited for the paging path, where the same endpoint serves a fundamentally different request. At `pageLimit: 50` an incremental page can cost up to 200 fully-derived cards to deliver 50. Deferred rather than patched because the fix is a second response shape, and choosing between "a `groups` filter parameter" and "a separate per-group endpoint" is a contract decision that should be made once, with Story 5.4's top-30 list in view.

- source_spec: `_bmad-output/implementation-artifacts/2-1-prioritized-filterable-claim-queue.md`
  summary: A compiled `ZenEngine` decision object is held in a module-level `lru_cache` and shared across every concurrent request, and the `context` argument threaded through the evaluation cache key is exercised by no test.
  evidence: Both real, both low-consequence today. Nothing in the change establishes that the pyo3 decision object is safe to evaluate concurrently; the evaluation is also synchronous on the event loop. The practical exposure is small because the evaluated result is itself cached, so a given `(key, version, content, context)` is evaluated once per process — but that is a property of the cache, not of the object, and a future decision-table document that varies by claim would evaluate per row and make both facts matter at once. The context plumbing exists solely for that future document; until one exists, the cache-key design its docstring defends is unverified.

- source_spec: `_bmad-output/implementation-artifacts/2-1-prioritized-filterable-claim-queue.md`
  summary: `uv.lock` was excluded from the review diff as a generated artifact, so the transitive dependency set that `zen-engine==0.53.0` introduced has not been read by anyone.
  evidence: True by construction — both review passes excluded it. `zen-engine` is a Rust/pyo3 extension and the first non-pure-Python runtime dependency in the server, which makes its transitive set worth one deliberate look rather than a permanent exclusion. Belongs with a supply-chain review of the whole lock file, not with this story.

## Deferred from: implementation of 2-2-case-header-stage-adaptive-overview (2026-08-12)

Decisions taken during implementation that a later story or a product owner should revisit. Recorded here rather than only in the Dev Agent Record because each is a question left open, not a choice made and closed.

- source_spec: `_bmad-output/implementation-artifacts/2-2-case-header-stage-adaptive-overview.md`
  summary: The prototype labels `paidIndemnity + paidMedical + paidExpense` "Total incurred", but in insurance practice *incurred* is paid **plus reserve** — so the label names a different quantity from the one it computes, on the investigation card, the settled banner and the settled payout breakdown.
  evidence: Real, and ported deliberately: the prototype is this story's design contract and the figure it shows was what stakeholders reviewed. The derivation is named for what it actually adds up (`total_paid`) so the code does not repeat the error, and the label is the prototype's. Changing the *number* is a product decision — it moves every "total incurred" figure in the console and will move Epic 5's KPI cards and Epic 7's decomposition with it — so it wants an owner rather than a dev-time correction.

- source_spec: `_bmad-output/implementation-artifacts/2-2-case-header-stage-adaptive-overview.md`
  summary: The prototype tints the fraud score on the investigation card at 55 and 35; this story renders it uncoloured, because those two cut-offs appear in no rule document.
  evidence: Real. Colouring it would have put an unversioned threshold in the browser — precisely what AD-8 moved the risk bands out of `config.py` to prevent, and what `noDerivation.test.ts` fails a build over. The honest fix is a `fraud_band` derivation with its cut-offs in `derivation_thresholds`, which is a new registered derivation and outside this story's task list. Epic 7's fraud analytics workspace (7.1) is the natural place: it needs the same bands for its own charts.

- source_spec: `_bmad-output/implementation-artifacts/2-2-case-header-stage-adaptive-overview.md`
  summary: 101 seeded documents carry `Post-surgery` or `Closed` where a filing date belongs, and 62 timeline events carry `Closed`; all of them seed `filed_date` / `event_date` as NULL, so the timing note is not on the wire.
  evidence: Deliberate — a `DATE` column should not hold a non-date, and NFR-3 says render the absence rather than a placeholder that looks like data. Nothing is lost: the raw strings are still in `seed_data.json`'s `deferred` block, so Story 2.5 (whose Documents tab renders "name, type, filed date") can add a `filed_note` column and re-seed if a product owner wants "filed post-surgery" back. Flagged because 2.5 will be the first surface where the em dash is visible on a fifth of the rows.

- source_spec: `_bmad-output/implementation-artifacts/2-2-case-header-stage-adaptive-overview.md`
  summary: The treatment overview's recent-6 timeline slice never fires against seeded data — the longest treatment timeline in the portfolio is exactly six events — so it is covered by unit tests over a synthetic twelve-event claim rather than by the e2e spec.
  evidence: A property of the dataset, not of the rule: a claim still in treatment has not accumulated the RTW and settlement entries that make settled timelines seven. The prototype's own `.slice(-6)` never fired either. `tests/test_claim_detail_assembly.py` exercises the rule over its whole domain and `tests/test_claim_detail.py` asserts the seeded claims are correctly reported as *not* truncated. Worth revisiting when Story 2.3 starts appending edit events, which is the first thing that will push a live treatment claim past six.

## Deferred from: code review of 2-2-case-header-stage-adaptive-overview (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-2-case-header-stage-adaptive-overview.md`
  summary: Migration 0011's `downgrade()` runs `DELETE FROM document` / `DELETE FROM timeline_event` unqualified, so it destroys rows it never inserted.
  evidence: Real, and the pattern is pre-existing — 0004 and 0007 clear their tables the same way. It is sharper here because `timeline_event` is not only a seeded table: 0010 grants the app role INSERT precisely so Story 2.3's edits, Epic 3's approvals, Epic 4's meetings and Epic 6's letters can append to it, and `upgrade()` restores only the seed. A `downgrade 0010` months from now would silently drop every runtime-appended event. Deferred rather than patched because the right fix is a decision about the whole family of seed migrations — a marker column distinguishing seeded from appended rows, or a refusal when the row count exceeds the seeded total — not a one-off `WHERE` clause in this revision.

- source_spec: `_bmad-output/implementation-artifacts/2-2-case-header-stage-adaptive-overview.md`
  summary: The settled payout breakdown formats indemnity, medical, expense and the total independently at whole-dollar precision, so the three components need not add up to the total shown beside them.
  evidence: Real but dormant: `formatCents` drops cents (`maximumFractionDigits: 0`) and no seeded claim has a non-zero cents component, so every breakdown currently reconciles. It is the same argument `claim_money.py` makes about the percentage bar — round the parts independently and they stop summing to the whole — applied to money rather than shares. Becomes live with Epic 3's bill lines, which is also where a cents-precision formatter belongs; `lib/money.ts` already anticipates one.

- source_spec: `_bmad-output/implementation-artifacts/2-2-case-header-stage-adaptive-overview.md`
  summary: `Record<Enum, …>` lookups for the phase and coordination accents have no runtime fallback, so a value the deployed bundle does not know throws and blanks the case-file pane.
  evidence: TypeScript makes these exhaustive at compile time, so the only way to reach it is deploy skew — a server sending a new enum member to a browser holding cached older JavaScript. That is a real deployment mode and not one this project has decided a policy for (cache-busting, a version handshake, or defensive fallbacks at every enum site). Recorded so the decision is made once for the whole SPA rather than as a defensive `??` in this one card.

## Deferred from: implementation of 2-3-audited-inline-field-editing (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-3-audited-inline-field-editing.md`
  summary: AD-12 requires that an AD-4 command mutating an *embedded source field* call `services/rag`'s mark-stale command in the same transaction; `update_claim_fields` writes `injury_type`, `cause` and `body_part` — three fields any similar-case embedding will be built from — and calls nothing.
  evidence: `services/rag` does not exist yet (Epic 6 creates it, and Story 6.1 defines what a claim embedding is built from), so there is no command to call and no staleness to track. Recorded here rather than left to be noticed later because the obligation attaches to *this* command the moment 6.1 lands: the seam is one call inside the existing transaction, and the story that adds embeddings has to walk every AD-4 command written before it. The other direction — embedding source fields chosen so that no 2.3-editable column is one — is also a legitimate resolution, and is 6.1's to make.

- source_spec: `_bmad-output/implementation-artifacts/2-3-audited-inline-field-editing.md`
  summary: The edit command is not stage-scoped: a handler can PATCH the six clinical fields on a settled claim as easily as on an investigation one, even though the only UI that offers the editors is the investigation overview.
  evidence: Deliberate, and the alternative was worse. Story 2.4 edits the body part from the injury-diagram tab, which is available at every stage, so a stage gate here would have to be relaxed one story later. Nothing in the epics' acceptance criteria restricts the command by stage, and "a settled claim's ICD-10 was recorded wrong" is a correction a handler legitimately makes. If a lifecycle rule is wanted — say, no clinical edits after settlement without a reopen — it is a real business rule with a real owner, and it belongs in the rules tier beside the other lifecycle decisions (3.5's approvals), not as an `if` in the command.

- source_spec: `_bmad-output/implementation-artifacts/2-3-audited-inline-field-editing.md`
  summary: `icd_desc` is accepted by the command and validated like the rest, but no surface shows or edits it, so the whitelist has one member the UI never exercises.
  evidence: The story's task list names it explicitly (`icd` plus `icd_desc` if supplied) and the prototype stores both, but the prototype's injury card edits only the code. Left on the whitelist rather than dropped because an ICD-10 code and its description are one fact in two columns, and a command that could change the code while leaving the description describing the *old* diagnosis is the more dangerous shape. Covered by unit tests; the surface that edits it is whichever later story shows it (2.5's document viewer and Epic 6's copilot are both candidates).

- source_spec: `_bmad-output/implementation-artifacts/2-3-audited-inline-field-editing.md`
  summary: Editing the body part rewrites the stored `body_part` label from the diagram's vocabulary, so an edited claim reads "Right Hand" where an untouched one reads "Wrist(s) & Hand(s)" — two vocabularies in one column.
  evidence: The prototype's own behaviour (`updateBodyPart` sets `c.bodyPart = opt.label`), ported deliberately. The dataset's `body_part` describes the injury in the carrier's wording; `body_key` names a region on the SVG; nothing in the data maps between them, and inventing a mapping would be this codebase answering a clinical question the dataset did not. The honest resolutions are a product decision — either the diagram labels become the vocabulary, or `body_part` stops being writable and the two live side by side — and Story 2.4, which puts both on screen at once, is where it will be visible enough to decide.

## Deferred from: code review of 2-3-audited-inline-field-editing (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-3-audited-inline-field-editing.md`
  summary: `timeline_event` is asserted append-only in code, and documented as such at length, while the application DB role holds `UPDATE` and `DELETE` on it.
  evidence: Real. Migration 0003 grants `SELECT, INSERT` on `audit_event` — AD-4's "INSERT-only" enforced by the database. Migration 0010 (Story 2.2) grants `SELECT, INSERT, UPDATE, DELETE ON timeline_event, document TO lineworker_app`, presumably because `document` is a mutable entity and the two were granted together. Story 2.3 is where the append-only claim is made in prose (`services/claims/timeline.py`: "No `version`, no update path, no delete") and where the first runtime writer lands, so the gap is visible now — but the invariant is currently enforced by code convention rather than by the grant surface, which is the thing AD-4 says should not be true of an append-only store. Deferred because the fix is a new migration narrowing a grant that Story 2.2 issued, and splitting the two tables' grants is 2.2's decision to revisit alongside the `document` write path Story 2.5 will add.

- source_spec: `_bmad-output/implementation-artifacts/2-3-audited-inline-field-editing.md`
  summary: The `edit` timeline tag renders as the raw lowercase token (`edit`) because there is no UI label map for timeline tags.
  evidence: Pre-existing from Story 2.2 — `Cards.tsx` renders `{entry.tag}` directly, and the seeded tags (`intake`, `froi`, `rtw`, …) read acceptably as uppercase chips. Story 2.3 adds the first tag the seed does not contain, which is what makes the absence of a label map visible. The stored snake_case value is convention-correct; what is missing is the UI-owned display map every other enum in the case file already has (`STAGE_LABEL`, `DISABILITY_LABEL`, `COMM_STATUS_LABEL`). Deferred because Epic 3, 4 and 6 each add their own tags, and the map wants writing once against the full set rather than growing a member per story.

## Deferred from: implementation of 2-4-interactive-injury-diagram (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-4-interactive-injury-diagram.md`
  summary: A secondary injury's severity score does not reach the risk band, the queue's priority score, the High Risk tile or any other portfolio figure — a claim whose primary injury scores 20 and whose secondary scores 95 is still banded `low` everywhere outside the diagram.
  evidence: Correct for this story and probably wrong for the product. `risk` is defined as a band of `claim.severity_score` (Story 1.4) and every consumer of it — the gauge, the queue dot, the top-bar tile, Epic 5's KPIs — reads that one column; nothing in 2.4's acceptance criteria asks for the aggregate to change, and inventing one (max? sum? weighted?) would be this codebase answering a clinical question the dataset did not. The prototype has the same behaviour and the same gap. The honest resolutions are a product decision — either secondary injuries are clinical detail that does not move the claim's risk, or `severity_score` becomes a *derived* figure over the injury set, which is a new registered derivation with a rules-tier parameter and a re-think of what the editable number on the severity card means. Epic 7's fraud/severity analytics is the first place the difference will show up in a chart.

- source_spec: `_bmad-output/implementation-artifacts/2-4-interactive-injury-diagram.md`
  summary: A secondary injury can be captured and removed but not corrected: there is no command to change its body part, injury type or severity score once it exists.
  evidence: The story's AC 2 asks for add and remove and nothing else, and the prototype has no edit path either (`removeInjury` filters the array; nothing mutates an entry). The row carries `version` precisely so an update command can compare-and-swap on it when one is written. Recorded because "delete it and add it again" loses the audit continuity that the `additional_injury` id represents — the log will read as two unrelated events rather than one correction — and because a handler correcting a typo is a likelier action than the pair of writes it currently takes.

- source_spec: `_bmad-output/implementation-artifacts/2-4-interactive-injury-diagram.md`
  summary: `additional_injury.body_key` is a native `body_region` enum while `claim.body_key` is still `Text`, so one vocabulary has two storage forms.
  evidence: Deliberate and scoped: the story's task list puts the enum on the table it creates, and converting `claim.body_key` is a migration over a column Stories 1.2 and 2.3 own — with a ripple through the edit command's diff machinery, the case-header contract and the generated client. The vocabulary itself is single-sourced either way (`BodyRegion` in `data/models/enums.py`, labels in `services/claims/reference.py`, and `services/claims/edit.py` validates the claim's key against the same eleven members), so what differs is only where the refusal comes from — a 422 for the claim, a database error for the injury. The conversion is a small migration on 0013's pattern and should happen the next time `claim` is altered for another reason.

- source_spec: `_bmad-output/implementation-artifacts/2-4-interactive-injury-diagram.md`
  summary: Story 2.3's open question about the two body-part vocabularies is now *visible* — the diagram's labels and the carrier's `body_part` wording are on screen together — and is still unresolved.
  evidence: Restated rather than duplicated: 2.3 recorded it and named this story as the place it would become visible. It has. The Injury Diagram's select reads "Right Hand" while the case header reads "Wrist(s) & Hand(s)" until somebody edits the region, at which point the server rewrites the label from the diagram's list and the two agree — so a claim's wording changes as a side effect of an unrelated correction. Still a product decision (either the diagram's labels become the vocabulary, or `body_part` stops being writable and the two live side by side), and still not one a dev-time choice should close.

- source_spec: `_bmad-output/implementation-artifacts/2-4-interactive-injury-diagram.md`
  summary: The AD-12 mark-stale obligation recorded for `update_claim_fields` now attaches to three more commands: `add_additional_injury`, `remove_additional_injury` and `update_claim_severity` all write fields a similar-case embedding would be built from.
  evidence: Extends the item recorded from Story 2.3's implementation rather than replacing it. `services/rag` still does not exist (Epic 6 creates it), so there is still no command to call — but the surface that Story 6.1 has to walk has grown from one command to four, and a secondary injury is exactly the kind of clinical detail a similar-case search would want to have indexed. The seam is one call inside each existing transaction.

- source_spec: `_bmad-output/implementation-artifacts/2-4-interactive-injury-diagram.md`
  summary: `treatment_plan_step` is created, seeded and read, and nothing writes it at runtime — the plan a handler sees is the dataset's and cannot be amended.
  evidence: The story's task list is explicit that this table is reference content for Epic 2 ("effectively static reference rows this epic — no runtime writer until a later epic needs one"), which is why the table has no `version` column: there is no concurrent writer to arbitrate. Recorded because a treatment plan is the kind of thing a handler *would* amend, and the story that adds the command adds the column with it — the same order `Document` and `GlossaryTerm` establish.

## Deferred from: implementation of 2-5-documents-employee-id-statutory-forms (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-5-documents-employee-id-statutory-forms.md`
  summary: **The schema carries no fatality indicator, so Path C is recognised by a severity/outcome proxy that no real claim can currently meet.** A fatal claim in production would be classified Path B and its handler would never see the death-benefit filings.
  evidence: The most consequential gap this story leaves, and it is a data-model gap rather than a rules one. Story 1.2's schema has no `fatality` flag, no `deceased` outcome and no death-benefit status; `ReturnStatus` offers `under_treatment`, `returned_and_under_therapy` and `returned_and_fully_recovered`, none of which means the worker died. The story permitted a "JDM-parameterized severity/outcome rule", and `claim_path` uses the strongest statement the schema can make — maximum severity, permanent disability, never returned — with `pathFatalitySeverityMin` at the top of `severity_score`'s domain. That cut-off is deliberately high because the seeded book's seven `permanent` claims are *all* amputations scoring 75-98 and several of them **returned to work fully recovered**: any lower threshold would file a living worker as a death-benefit claim and show their handler the AFF-1, C-64 and C-65. The resolution is a column (a `fatality` boolean, or a fourth `ReturnStatus` member) plus a migration and a one-line change to the derivation's first branch — not a re-tuning of the threshold, which is why `test_documents_tab.py::test_no_seeded_claim_is_classified_a_fatality` says so in its failure message.

- source_spec: `_bmad-output/implementation-artifacts/2-5-documents-employee-id-statutory-forms.md`
  summary: The nine seeded `download_url` values are NY WCB and FNSB placeholders and are presented to handlers as if they were the jurisdiction's forms.
  evidence: NFR-4 and an explicit Deferred architecture decision ("State statutory coverage… and sourcing of real statutory form PDFs"), so this is a restatement rather than a discovery — recorded here because the story's instruction was to surface the caveat in code comments and *not* in the UI, which means the only thing a handler sees is a Download button that behaves exactly like a real one. Every claim in the portfolio is filed under one of fifteen plants across a dozen states; the forms are New York's. Before go-live the table needs a jurisdiction dimension (`path_required_form` currently keys on path alone) and validated per-state form references. The caveat lives in `data/models/core.py`, migration 0017 and 0018, and the extractor.

- source_spec: `_bmad-output/implementation-artifacts/2-5-documents-employee-id-statutory-forms.md`
  summary: No document has bytes: `blob_key` is null on all 563 rows, nothing ingests a file, and the viewer renders a generated sheet rather than the filing itself.
  evidence: Correct for this story — the prototype has no files behind its document rows, and the MinIO-versus-volume decision is explicitly Deferred — but worth recording as what it is: a document viewer that has never displayed a document. What this story lands is the *boundary* (`services/blobstore`, four operations, conformance-tested against a volume implementation) and its wiring through `document_content`, so attaching a real PDF is a change to the store and to an ingest path that does not exist yet, not to the route, the payload shape or the dialog. The story that adds upload is also the story that chooses the backend and configures it.

- source_spec: `_bmad-output/implementation-artifacts/2-5-documents-employee-id-statutory-forms.md`
  summary: The AD-12 mark-stale obligation and the `timeline_event` grant item both now touch `document`, whose write path this story was expected to add and did not.
  evidence: The 2.3 code-review item deferring the `timeline_event` grant narrowing says the split "is 2.2's decision to revisit alongside the `document` write path Story 2.5 will add". Story 2.5 adds no write path — it reads the table, and the story's own scope note is explicit that upload and review→confirm write-backs are out of scope (Epic 3's action worklist owns the latter). So `lineworker_app` still holds `UPDATE`/`DELETE` on `timeline_event` because it was granted together with `document`, and the pairing that justified deferring the fix has not materialised. The narrowing migration is now independent of any document story and can be written on its own.

## Deferred from: implementation of 2-6-incident-photos (2026-08-12)

- source_spec: `_bmad-output/implementation-artifacts/2-6-incident-photos.md`
  summary: **No photo has bytes, and nothing in Epics 1–8 ingests one** — the console's evidence grid can only ever show the 293 seeded caption/source rows, and the "viewer" has never displayed a photograph.
  evidence: Correct for this story and narrower than 2.5's equivalent gap, which is why it is recorded separately. The prototype's thumbnails are the 📷 emoji, so `blob_key` is null on all 293 rows and the placeholder is the designed state rather than a degradation. But where a *document* viewer at least assembles a real sheet from stored columns, a photo viewer with no bytes has nothing to show — it renders a caption, a source line and a claim id. What this story lands is the resolution path: `has_blob` and `blob_url` are separate fields on the contract precisely so the two states a real deployment produces (a presigning store, and a mounted volume whose `url()` answers null by design) are already distinguishable, and `photos_block` takes an optional `BlobStore` so wiring one in is a change to one line of `claim_detail`. The story that adds ingest is also the story that chooses the backend — the same story 2.5 is waiting on.

- source_spec: `_bmad-output/implementation-artifacts/2-6-incident-photos.md`
  summary: `photo` is a read-only table with no write command anywhere in the epics, so a handler cannot attach, caption, correct or remove a photograph — including one attached to the wrong claim.
  evidence: The story is explicit ("there is no photo upload, capture, annotation, or deletion anywhere in the epics — do not add write commands"), and migration 0020 enforces it with a `SELECT`-only grant rather than leaving it to component discipline. Recorded because the absence is more consequential for photos than for the other read-only tables this epic created: `treatment_plan_step` and `path_required_form` hold *reference* content that a handler has no standing to change, while incident photographs are evidence a plant safety officer produces continuously after the incident. The table deliberately carries no `version` column for the same reason `TreatmentPlanStep` does not — the story that adds the command adds the column with it, and it will need the grant widened in the same migration.

- source_spec: `_bmad-output/implementation-artifacts/2-6-incident-photos.md`
  summary: `photo.source` is a provenance *sentence* ("Plant safety, 03/22") holding a month/day fragment with no year, so photographs cannot be sorted, filtered or aged by when they were taken.
  evidence: Deliberate, and the alternative was worse: the fragment carries no year, its day is not always the incident's, and 232 distinct source strings across the book mix an author with a date in one human-written field. Splitting it into a `DATE` column would have produced a column that is wrong a good fraction of the time beside a `source` that had lost half its meaning — so the extractor stores the sentence the prototype prints. The resolution belongs with the ingest path above: a real capture flow records a photographer and a timestamp as two fields, and the seeded rows become the migration's problem at that point rather than a parse nobody can validate today.
